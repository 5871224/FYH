# 規格書

**專案名稱：** 福圓號排班系統（FYH）  
**文件版本：** 2026-07-11  
**文件狀態：** 唯一正式規格  
**文件負責人：** 專案維護者  
**最後全面檢查：** 2026-07-11  
**適用程式版本：** GitHub `main` 分支
**主要裝置：** 手機優先，支援電腦與平板  
**正式時區：** Asia/Taipei  
**取代文件：** 儲存庫內所有其他規格書、補充規格及 `規格補充` 資料夾文件  
**文件管理規則：** 後續所有需求、介面與技術規格一律直接更新本檔，不再新增獨立規格書或規格補充檔。

---

# 文件樹狀目錄

規格書  
├─ 第一章　打卡頁  
│  ├─ 1.1 頁面目的  
│  ├─ 1.2 分頁與功能區  
│  │  ├─ 1.2.1 今日打卡  
│  │  └─ 1.2.2 加班申請  
│  └─ 1.3 其他相關規則  
├─ 第二章　訂餐頁  
│  ├─ 2.1 頁面目的  
│  ├─ 2.2 分頁  
│  │  ├─ 2.2.1 今日訂餐  
│  │  ├─ 2.2.2 訂餐統計  
│  │  └─ 2.2.3 訂餐設定  
│  └─ 2.3 其他相關規則  
├─ 第三章　班表頁  
│  ├─ 3.1 頁面目的  
│  ├─ 3.2 檢視分頁  
│  │  ├─ 3.2.1 人員檢視  
│  │  ├─ 3.2.2 人員檢視－統計欄  
│  │  └─ 3.2.3 班別檢視  
│  └─ 3.3 設定、工具與其他規則  
├─ 第四章　紀錄頁  
│  ├─ 4.1 頁面目的  
│  ├─ 4.2 分頁  
│  │  ├─ 4.2.1 個人記錄  
│  │  ├─ 4.2.2 加班審核  
│  │  └─ 4.2.3 打卡管理  
│  └─ 4.3 其他相關規則  
├─ 第五章　系統環境、資料庫與其他規格  
│  ├─ 5.1 章節目的  
│  ├─ 5.2 執行環境與使用技術  
│  ├─ 5.3 登入、首頁與帳號規則  
│  ├─ 5.4 共用時間、權限與安全規則  
│  ├─ 5.5 後端服務、RPC 與交易  
│  ├─ 5.6 資料庫共通規格  
│  ├─ 5.7 資料表與欄位中文說明  
│  ├─ 5.8 稽核、驗收與發佈  
│  └─ 5.9 本版本不包含項目  
├─ 第六章　介面樣式統一規格  
│  ├─ 6.1 適用範圍  
│  ├─ 6.2 CSS 架構與載入順序  
│  ├─ 6.3 共用尺寸與間距  
│  ├─ 6.4 配色與狀態  
│  ├─ 6.5 表單控制項  
│  ├─ 6.6 按鈕與頁籤  
│  ├─ 6.7 表格  
│  ├─ 6.8 卡片、區塊與彈出視窗  
│  ├─ 6.9 手機版頁面配置  
│  ├─ 6.10 無障礙與實作規則  
│  └─ 6.11 驗收標準  
├─ 第七章　後端架構與平台遷移
│  ├─ 7.1 章節目的  
│  ├─ 7.2 現行架構與採用原因  
│  ├─ 7.3 前端、Edge Function 與資料庫分工  
│  ├─ 7.4 現行後端功能清單  
│  ├─ 7.5 遷移至其他後端平台的對應原則  
│  ├─ 7.6 建議遷移順序  
│  ├─ 7.7 遷移時不得破壞的規則  
│  └─ 7.8 驗收與切換原則  
├─ 第八章　共通互動、非功能性與設計交付  
│  ├─ 8.1 資訊架構與使用者流程  
│  ├─ 8.2 共通頁面狀態  
│  ├─ 8.3 表單、確認與未儲存資料  
│  ├─ 8.4 錯誤、重試與衝突處理  
│  ├─ 8.5 文案與名詞規範  
│  ├─ 8.6 裝置與瀏覽器支援  
│  ├─ 8.7 效能、容量與可用性目標  
│  ├─ 8.8 Wireframe 與設計交付  
│  └─ 8.9 無障礙與設計驗收  
└─ 第九章　API、資料生命週期、維運與測試  
   ├─ 9.1 API 契約  
   ├─ 9.2 共通錯誤碼  
   ├─ 9.3 版本與相容性  
   ├─ 9.4 資料分類  
   ├─ 9.5 保存、刪除與匿名化  
   ├─ 9.6 備份與災難復原  
   ├─ 9.7 發布與回滾  
   ├─ 9.8 監控與事件處理  
   ├─ 9.9 需求編號與追蹤矩陣  
   ├─ 9.10 測試案例與發布門檻  
   └─ 9.11 尚待決定事項

---

# 第一章　打卡頁

## 1.1 頁面目的

打卡頁用來讓已登入人員完成當日上班與下班打卡、查看伺服器判定的今日日期、今日班別與已完成的打卡資訊，並在符合條件時提出打卡加班申請。

本頁以手機操作為優先，但手機、平板與電腦必須讀取同一筆正式打卡資料。進入頁面時一律重新向受身分驗證的後端讀取今日狀態，不依賴瀏覽器快取或先前畫面狀態。

## 1.2 分頁與功能區

本頁目前沒有傳統頁籤，分為兩個功能區：

1. **今日打卡：** 顯示今日日期、班別、上班及下班打卡狀態並提供打卡按鈕。
2. **加班申請：** 以「加班申請」勾選框展開，顯示當日加班資格、申請時數、備註與申請狀態。

### 1.2.1 今日打卡

#### A. 按鈕使用說明

1. **上班打卡**
   - 按下後先顯示確認視窗。
   - 確認後才取得手機定位或送出電腦 IP 驗證。
   - 第一次成功時間為正式時間。
   - 已有上班時間後按鈕停用。
   - 已有下班時間時，上班按鈕也維持停用。

2. **下班打卡**
   - 按下後先顯示確認視窗。
   - 沒有上班時間時仍可下班打卡。
   - 第一次成功時間為正式時間。
   - 已有下班時間後按鈕停用。

3. **返回首頁**
   - 使用 X 圖示。
   - 顯示在姓名右側並靠右對齊。
   - 返回登入後首頁，不登出。

#### B. 顯示欄位與格式

1. 使用者姓名。
2. 伺服器判定的今日日期。
3. 今日班別名稱。
4. 今日班別上下班時間。
5. 上班打卡時間、單位與來源。
6. 下班打卡時間、單位與來源。
7. 讀取或打卡失敗訊息。

已打卡狀態顯示在按鈕內，例如：

```text
07:58在莊敬打卡(IP)
```

來源只使用：

```text
GPS
IP
管理員補登
```

#### C. 核心邏輯

1. 正式日期與時間由後端以 `Asia/Taipei` 判定。
2. 電腦版進入頁面時，透過 `attendance-clock` Edge Function、`action: today` 讀取今日資料。
3. 手機、平板與電腦都讀取同一個 `attendance_records` 正式來源。
4. 每人每天最多一個上班時間與一個下班時間。
5. 快速連按、網路重送或重複請求不得覆寫第一次成功時間。
6. 沒有排班仍可打卡。
7. 有請假資料仍可打卡。
8. 員工可在任何已啟用打卡的公司單位打卡；地點與班別適用單位不同時，打卡仍成立並列為異常。
9. 手機驗證順序：
   - 先取得 GPS。
   - GPS 距離公司地點不超過 300 公尺，且定位精準度不超過 300 公尺時，以 GPS 打卡。
   - GPS 拒絕、失敗、逾時、距離不符或精準度不符時，改驗證固定對外 IP。
   - 多個地點符合時，選擇距離最近的單位。
   - 前端定位等待上限至少 15 秒。
10. 電腦與平板：
    - 不要求 GPS。
    - 不呼叫瀏覽器定位。
    - 只驗證固定對外 IP。
11. 讀取今日狀態失敗時顯示錯誤，不把已有紀錄誤顯示為尚未打卡。
12. 單位日後改名、搬家或調整座標，不回寫歷史打卡快照。

#### D. 權限

- **員工：** 可查看自己的今日班別與打卡狀態，可完成自己的上班及下班打卡。
- **主管：** 具備員工全部能力。
- **管理員：** 具備主管全部能力，並可在紀錄頁的打卡管理分頁補登、修改或清除打卡資料與查看修改歷程。

### 1.2.2 加班申請

#### A. 按鈕、欄位與版面

1. **加班申請勾選框**
   - 預設只顯示勾選框。
   - 勾選後才載入完整申請區塊。
2. **申請日期**
   - 獨立一行顯示。
   - 以伺服器端 `Asia/Taipei` 日期判定期限。
3. **提早上班時數與延後下班時數**
   - 以 0.5 小時為單位。
   - 兩欄使用同一個雙欄群組，電腦版與手機版皆維持同一行。
   - 欄位不足寬時縮小欄位內容，不得拆成上下兩行。
   - 系統可估算時帶入預設值；員工可在送出前調整。
4. **加班備註**
   - 使用完整一行。
   - 可填寫原因或補充說明。
   - 與每日打卡備註、訂餐品項備註分開保存。
5. **送出加班申請**
   - 驗證時數、期限與同日有效申請後建立待審申請。
6. **刪除申請**
   - 待審或退回狀態可刪除。
   - 採邏輯刪除並保留歷程。
   - 刪除後在期限內可重新申請。

#### B. 申請資格與計算

1. 申請日期已有有效上班打卡與下班打卡，即可手動提出申請。
2. 不要求當日一定有班別；沒有班別或班別缺少上下班時間時，仍可手動填寫時數。
3. 系統若可取得有效班別時間，可依打卡時間估算提早上班與延後下班時數；估算值只作為預設值，不構成申請門檻。
4. 不要求提早上班或延後下班至少 30 分鐘。
5. 尚在工作日期後 5 個日曆天內。
6. 同一天沒有其他有效申請。
7. 每一時數欄不得為負數，必須是 0.5 的倍數，合計必須大於 0。
8. 下班打卡完成後永不自動跳出加班建議；員工需自行展開「加班申請」區塊。
9. 狀態：
   - `pending`：待審。
   - `approved`：核准。
   - `returned`：退回。
10. 員工送出後若要改填，採刪除待審或退回申請後重提的流程。
11. 打卡加班申請與班表格中的加班設定是兩套獨立資料，不互相同步。
12. 管理員修改當日打卡後，既有申請回到待審並顯示「打卡時間已異動」，原申請時數不自動重算。
13. 已送出的申請不因班表、班別時間或班別適用單位後續變更而自動改變。

#### C. 權限

- **員工：** 可查看自己的申請資格、手動填寫並送出申請、查看狀態，並在期限內刪除待審或退回申請後重新申請。
- **主管：** 具備員工全部能力。
- **管理員：** 具備主管全部能力，並可在紀錄頁審核、調整、批次處理或代為建立申請。

## 1.3 其他相關規則

### 1.3.1 打卡異常

異常依最新班表、班別設定、班別適用單位與目前有效打卡資料即時計算，不永久固定結果。

時間只比較到分鐘，忽略秒數：

- 09:00:59 相對 09:00 不算遲到。
- 09:01:00 相對 09:00 算遲到。
- 17:59:59 相對 18:00 算早退。

同一天可同時出現：

1. 未打上班。
2. 未打下班。
3. 遲到。
4. 早退。
5. 無排班但有打卡。
6. 上班時間晚於下班時間。
7. 打卡時間不完整或格式異常。
8. 上班地點不符。
9. 下班地點不符。
10. 班別缺少完整上下班時間。

### 1.3.2 單位打卡設定

每個單位保存：

- 單位名稱。
- 開始日期。
- 結束日期。
- 班表隱藏狀態。
- 地址。
- 緯度。
- 經度。
- 固定對外 IP。
- 是否啟用打卡。

停用單位打卡後，該單位立即停止 GPS 與 IP 匹配；歷史打卡維持不變。

### 1.3.3 管理員異動後的員工按鈕

1. 當日 23:59 前：
   - 清除上班時間且尚無下班時間時，上班按鈕可重新啟用。
   - 清除上班時間但已有下班時間時，由管理員補登。
   - 清除下班時間時，下班按鈕可重新啟用。
2. 當日 23:59 後：
   - 員工按鈕不重新啟用。
   - 後續由管理員補登或修改。
3. 清除採作廢或軟刪除概念，修改歷程永久保留。

---

# 第二章　訂餐頁

## 2.1 頁面目的

訂餐頁用來處理當日員工餐點下訂、主管與管理員的訂餐統計，以及商品、截止時間與公司補助設定。正式訂單以當日上班打卡單位為訂餐單位快照。

## 2.2 分頁

1. **今日訂餐：** 員工選擇品項、數量及各品項備註。
2. **訂餐統計：** 主管與管理員依條件查詢明細、品項與人員彙總並匯出 Excel。
3. **訂餐設定：** 主管與管理員管理商品、截止時間、公司補助與品項順序。

### 2.2.1 今日訂餐

#### A. 按鈕與欄位

1. **品項**
   - 顯示目前啟用商品。
   - 已訂購後才停用的商品仍保留在該員工畫面。

2. **價格**
   - 顯示商品目前適用價格。

3. **數量**
   - 只接受 0 或正整數。
   - 負數、小數、科學記號與其他非整數內容在輸入當下拒絕。
   - 停用品項最多保留原訂數量，只能減少或歸零。

4. **備註**
   - 每個品項各自保存一份備註。
   - 不使用整張訂單共用備註取代品項備註。

5. **儲存訂餐**
   - 新增或更新今日整張訂單。
   - 顯示即時計算的總份數與總金額。

6. **取消整張訂單**
   - 已有訂單且所有數量改成 0 時，顯示確認視窗。
   - 確認後刪除該訂單全部品項，不建立取消狀態。

7. **返回首頁**
   - 使用 X 圖示返回首頁。

#### B. 訂餐資格與邏輯

1. 今日已有有效上班打卡。
2. 上班打卡有明確單位。
3. 送出時尚未超過伺服器判定的每日截止時間。
4. 只處理今天，不處理未來日期。
5. 不要求今日有班表。
6. 請假或打卡地點與班別適用單位不同，不阻止訂餐。
7. 第一次下訂時全部數量為 0，系統不建立訂單。
8. 同一位員工同一天只有一個 `order_id`，不同商品各一列。
9. 修改訂單：
   - 維持原 `order_id`。
   - 維持第一次下訂時間 `submitted_at`。
   - 維持第一次訂餐單位快照。
   - 更新品項、數量、備註與最後修改時間。
10. 截止後停止新增、修改與取消。
11. 管理員補登當日上班卡後，符合截止時間時可訂餐。
12. 訂餐完成後管理員若清除上班卡，訂單保留並在個人記錄及統計備註顯示警告。
13. 商品停用後：
    - 尚未訂購者不再顯示。
    - 已訂購者仍顯示名稱與原數量。
    - 可減少或取消，不可增加。
14. 歷史訂單保存下訂時商品名稱、單價、員工與單位快照。

#### C. 權限

- **員工：** 可查看可訂商品、建立自己的今日訂單、在截止前修改或取消自己的今日訂單。
- **主管：** 具備員工全部能力。
- **管理員：** 具備主管全部能力。

### 2.2.2 訂餐統計

#### A. 查詢欄位與按鈕

1. 日期起日。
2. 日期迄日。
3. 單位。
4. 員工。
5. 報表類型：
   - 明細。
   - 品項。
   - 人員。
6. 日期、單位或員工條件變更後，立即重新查詢並更新統計結果。
7. 報表類型變更後，立即切換顯示內容。
8. **匯出**：依目前篩選條件匯出 Excel。
9. 上一頁、下一頁與筆數資訊。
10. 明細預設每頁 50 筆。
11. 預設日期為今天。
12. 本分頁不顯示查詢按鈕。

#### B. 報表欄位與邏輯

1. **明細**
   - 日期。
   - 訂餐單位。
   - 員工姓名。
   - 品項。
   - 數量。
   - 單價。
   - 小計。
   - 備註。
   - 上班打卡已被清除等警告併入備註，不另設警告欄。
   - 不顯示員工工號、第一次下訂時間與最後修改時間。

2. **品項**
   - 品項。
   - 數量。
   - 單價。
   - 小計。

3. **人員**
   - 姓名。
   - 訂餐日數。
   - 金額。
   - 自付額。

自付額：

```text
金額 -（訂餐日數 × 公司補助）
```

4. 頁面顯示期間總數量與期間總金額。
5. Excel 匯出使用單一「訂餐統計」工作表，每位員工一列。
6. 匯出欄位依序為：
   - 員工姓名：員工姓名。
   - 員工編號：員工工號。
   - 早餐金額：空白。
   - 午餐金額：依頁面相同公式計算的自付額。
   - 晚餐金額：空白。
   - 早餐份數：空白。
   - 午餐份數：訂餐日數，同一員工同一天不重複計算。
   - 晚餐份數：空白。
   - 總計：空白。
   - 日期：使用訂餐統計查詢條件的結束日期，格式為 `YYYYMMDD`。
7. 匯出內容依目前日期、單位及員工篩選條件產生。

#### C. 權限

- **員工：** 可使用今日訂餐功能。
- **主管：** 具備員工全部能力，並可查詢訂餐統計與匯出 Excel。
- **管理員：** 具備主管全部能力。

### 2.2.3 訂餐設定

#### A. 按鈕與欄位

1. **截止時間**
   - 全系統共用每日截止時間。
   - 預設 `10:30`。

2. **公司補助**
   - 全系統共用。
   - 預設 55。
   - 只接受大於 0 的正整數。

3. **新增商品**
   - 新增一筆空白商品列。

4. **儲存**
   - 儲存截止時間、公司補助、商品內容及排序。

5. **拖曳排序**
   - 每列最前方顯示拖曳圖示。
   - 只有圖示可開始拖曳。
   - 品項名稱、價格、啟用欄與操作按鈕不觸發拖曳。

6. **刪除**
   - 新增後尚未儲存的空白列直接移除。
   - 沒有任何歷史訂餐記錄的商品，在確認後可刪除。
   - 已有歷史訂餐記錄時，後端拒絕實體刪除並提示改用停用。

7. 商品欄位：
   - 品項名稱。
   - 價格。
   - 是否啟用。
   - 排序。
   - 操作。

#### B. 欄位與版面規則

1. 商品名稱不可重複。
2. 價格可為 0，且不得為負數。
3. 品項表格使用固定欄寬。
4. 品項名稱欄使用剩餘寬度。
5. 價格、啟用與操作欄必須完整留在頁面內。
6. 拖曳欄目前約 42px，圖示操作範圍約 28px；其他設定頁的共用拖曳欄約 30px。
7. 電腦與手機版都需避免操作欄超出可視範圍。
8. 截止時間修改後立即生效：
   - 延後且尚在新截止時間內時重新開放。
   - 提前且已超過新時間時立即關閉。
9. 截止前修改商品價格時，今日包含該商品的訂單以交易方式同步更新單價與小計。
10. 截止後修改價格不影響已截止今日訂單與歷史訂單。

#### C. 權限

- **員工：** 可使用今日訂餐。
- **主管：** 具備員工全部能力，並可新增、修改、排序、啟用、停用與刪除符合條件的商品，可修改截止時間與公司補助。
- **管理員：** 具備主管全部能力。

## 2.3 其他相關規則

1. 正式商品資料：`meal_products`。
2. 正式設定資料：`meal_settings`。
3. 正式訂餐資料：`meal_orders`。
4. 不建立訂單主檔；同日多項商品共用同一個 `order_id`。
5. 唯一識別原則：`user_id + order_date + product_id`。
6. 訂單總數量與總金額查詢時計算，不另存主檔總額。
7. 訂餐儲存、整張取消、價格同步更新必須使用後端交易或 RPC。
8. 重複送出相同訂單請求必須具有冪等性。

---

# 第三章　班表頁

## 3.1 頁面目的

班表頁用來顯示完整八週班表，讓所有已登入角色查看班別、假別與班表加班；主管與管理員另可編輯班表、設定單位與人員、維護班別／假別／加班／國定假日、執行例休檢查與自動排班。

## 3.2 檢視分頁

班表檢視下拉選單包含：

1. 人員檢視。
2. 人員檢視－統計欄。
3. 班別檢視。

### 3.2.1 人員檢視

#### A. 顯示欄位

1. 單位。
2. 人員。
3. 八週日期欄。
4. 每日格可同時顯示：
   - 班別。
   - 假別。
   - 班表加班。
5. 分段順序固定為班別、假別、加班。
6. 顏色、文字顏色與圓角依各設定項目顯示。
7. 今日欄位需有明顯標示。
8. 未在職日期顯示為不可排班格。

#### B. 按鈕與操作

1. 前八週、前一週、後一週、後八週。
2. 頂端與表格左右的週次導覽。
3. 單位顯示範圍篩選。
4. 工具列中的班別、假別、加班選擇。
5. 點選格子套用目前選擇項目。
6. 取消班別、取消假別、取消加班。
7. 多格拖曳選取。
8. 複製、剪下、貼上、刪除。
9. 復原與重做。
10. 雙擊人員或單位進入編輯。
11. 拖曳單位及同單位內人員調整班表顯示順序。

#### C. 邏輯

1. 每人每天最多一個班別。
2. 不處理跨日夜班、一天多班與一天多次上下班。
3. 一個班表格以 `member_id + work_date` 唯一識別。
4. 班別、假別與班表加班保存在同一列 `schedule_entries`。
5. 空白格不保留無內容資料列。
6. 班別適用單位是單選且必填。
7. 假別可保存整天或起迄時間與原因。
8. 班表加班可保存起迄時間、兩段休息與原因。
9. 班表加班與打卡加班申請彼此獨立。
10. 同一日期範圍、檢視模式與篩選條件下，員工、主管與管理員看到的人員列、單位分組、排序、PT 標記及班表格內容必須完全一致；角色差異只影響編輯工具與操作權限。
11. 人員是否出現在班表，統一依正式的啟用狀態、到職日與離職日判斷。班表日期範圍完全晚於離職日的人員不顯示；完全早於到職日的人員不顯示；歷史範圍與任職期間重疊時仍保留歷史班表。
12. 班表共同名錄必須向所有有效登入者提供完全相同的班表顯示必要欄位：人員 ID、姓名、所屬單位、到職日、離職日、日薪狀態、啟用狀態與排序。工號、角色、固定休假、休假目標、支援單位與可排班班別屬管理用途，改由管理名錄提供。
13. 手機版頂端控制列允許換行，不因單行擠壓造成水平溢出。

#### D. 權限

- **員工：** 可查看所有人員完整班表。
- **主管：** 具備員工全部能力，並可編輯班表、調整顯示順序及使用排班工具。
- **管理員：** 具備主管全部能力。

### 3.2.2 人員檢視－統計欄

#### A. 作用

在「人員檢視」全部功能上，額外顯示每位人員於目前範圍的統計欄，協助檢查班數、休假或排班配置。

#### B. 操作與邏輯

1. 排班格操作與人員檢視相同。
2. 統計依目前已載入的八週班表即時計算。
3. 變更班表後同步更新統計。
4. 統計欄只作顯示，不是獨立資料來源。

#### C. 權限

- **員工：** 可查看完整班表與統計欄。
- **主管：** 具備員工全部能力，並可編輯班表。
- **管理員：** 具備主管全部能力。

### 3.2.3 班別檢視

#### A. 作用與欄位

1. 以班別為列。
2. 顯示每日已排人員。
3. 顯示班別需求人數。
4. 顯示人數不足提示。
5. 依班別適用單位與營運日期判斷是否顯示。

#### B. 操作與邏輯

1. 主要用途是檢查各班別每日人力。
2. 此檢視以查看為主，排班編輯回到人員檢視操作。
3. 需求人數來自 `set_shift.required_staff_count`。
4. 當日排班人數低於需求時顯示缺員狀態。

#### C. 權限

- **員工：** 可查看班別人力配置。
- **主管：** 具備員工全部能力。
- **管理員：** 具備主管全部能力。

## 3.3 設定、工具與其他規則

### 3.3.1 共用工具列

1. 班別、假別、加班項目以彩色按鈕顯示。
2. 可依單位篩選班別。
3. 工具列可收合。
4. 浮動工具列右上角顯示「上一步」與「下一步」SVG 圖示按鈕。
5. 「上一步」對應 `Ctrl+Z`，「下一步」對應 `Ctrl+Y`，使用相同的班表操作歷程。
6. 沒有可復原或重做的操作時，對應按鈕停用。
7. 最多保留最近 20 次班表操作歷程；執行新的班表修改後清除下一步歷程。
8. 復原或重做後，立即更新畫面並同步保存變更的班表格。
9. 隱藏於工具列的設定項目仍保留歷史班表顯示。
10. 按 `Esc` 可取消目前選取的排班工具。
11. 「匯出休例假」、「匯出請假」及「匯出加班」按下後，先顯示日期範圍選擇視窗，再依選定期間匯出。
12. 日期範圍視窗包含開始日期、結束日期、取消與對應的匯出按鈕。
13. 日期範圍視窗的左右寬度與「自動排班期間」視窗相同。
14. 日期範圍視窗不顯示預設期間說明框。
15. 日期範圍預設帶入目前畫面顯示的八週起訖日，使用者可再自行調整。
16. 匯出前由受保護的後端查詢直接讀取選定期間的正式 `schedule_entries`，並關聯人員、假別與加班設定；不得只依賴目前畫面的記憶體資料或目前在職名錄。
17. 已離職、停用或不在目前班表人員列，但在選定期間具有正式班表資料的人員，仍須依歷史資料匯出。
18. 匯出檔名包含選定的開始日期與結束日期。

### 3.3.2 單位設定

欄位：

- 單位名稱。
- 開始日期。
- 結束日期。
- 是否從班表隱藏。
- 排序。
- 地址。
- 緯度。
- 經度。
- 固定對外 IP。
- 是否啟用打卡。

按鈕與操作：

- 新增、編輯、刪除。
- 設定列表與新增單位視窗的主要新增按鈕文字統一為「新增」。
- 匯入、匯出。
- 最前方拖曳排序圖示。
- 拖曳欄約 30px，只有圖示可開始拖曳。

邏輯：

1. 有人員、班別、打卡或訂餐歷史引用的單位由後端保護歷史關聯。
2. 一個單位保存一組目前打卡設定。
3. 主管寫入一般欄位時，前端與後端會排除打卡敏感欄位。
4. 單位新增、修改與排序使用受保護的單位一般欄位 RPC，不依賴對 `set_departments` 主表的直接讀寫權限；儲存失敗時必須在目前視窗顯示明確錯誤，不得只寫入不可見狀態。
5. 單位刪除由後端再次檢查人員、班別、班表、打卡與訂餐歷史關聯，有引用時拒絕刪除並顯示原因。

權限：

- **員工：** 可在班表與相關頁面查看必要的非敏感單位名稱。
- **主管：** 具備員工全部能力，並可管理單位名稱、有效日期、班表隱藏與排序。
- **管理員：** 具備主管全部能力，並可設定地址、座標、固定 IP 與打卡啟用狀態。

### 3.3.3 人員設定

欄位：

- 工號。
- 姓名。
- 所屬單位。
- 角色。
- 到職日。
- 離職日。
- 日薪人員。
- 固定休假星期。
- 每月休假天數。
- 可支援單位順序。
- 可排班班別順序。
- 排序。

按鈕與操作：

- 新增、編輯、刪除。
- 設定列表與新增人員視窗的主要新增按鈕文字統一為「新增」。
- 重設密碼。
- 匯入、匯出。
- 篩選姓名、單位、角色、在職狀態與薪資類型。
- 最前方拖曳排序圖示。
- 拖曳排序同步影響班表人員顯示順序並永久保存。

權限：

- **員工：** 可查看班表中的人員名稱與必要資訊。
- **主管：** 具備員工全部能力，並可建立員工帳號、維護員工與主管的非角色資料、重設員工與主管密碼，以及刪除符合條件的員工或主管帳號。
- **管理員：** 具備主管全部能力，並可建立員工、主管與管理員，修改角色及管理管理員帳號。

帳號刪除規則：

1. 有班表、打卡、打卡異動、加班、審核、訂餐或系統設定異動歷史時，後端拒絕刪除，提示所有非零的資料類型與筆數。
2. 刪除被歷史資料阻擋時，人員資料與登入帳號保持原狀，不得改為停用、不得從管理名錄消失，也不得刪除其既有班表。
3. 全量儲存或畫面名錄缺少某人員時，不得據此刪除人員或改變任職狀態；人員表不設停用欄位。
4. 確認完全沒有任何歷史關聯時，Supabase Auth 與 `set_employee` 必須在同一交易一致完成刪除。
5. 系統永遠保留至少一位有效管理員。
6. 主管或管理員刪除自己時需再次驗證目前密碼，完成後立即登出。
7. 人員離職應填寫離職日並保留歷史，不使用刪除或停用代替離職。

### 3.3.4 班別設定

欄位：

- 名稱。
- 適用單位。
- 背景色。
- 文字色。
- 自動文字色。
- 上班時間。
- 下班時間。
- 需求人數。
- 是否隱藏工具列。
- 排序。

規則：

1. 每個班別只能有一個適用單位。
2. 適用單位必填，保存於 `set_shift.applicable_department_id`。
3. 班別設定最前方顯示拖曳排序圖示。
4. 只有拖曳圖示可調整順序。
5. 可匯入及匯出設定。
6. 設定列表與新增班別視窗的主要新增按鈕文字統一為「新增」。

權限：

- **員工：** 可查看班表使用中的班別名稱、時間與顏色。
- **主管：** 具備員工全部能力，並可新增、修改、排序及管理班別。
- **管理員：** 具備主管全部能力。

### 3.3.5 假別設定

欄位：

- 代碼。
- 名稱。
- 背景色。
- 文字色。
- 自動文字色。
- 是否需要時間。
- 是否需要原因。
- 是否隱藏工具列。
- 排序。

規則：

1. 最前方顯示拖曳排序圖示。
2. 只有拖曳圖示可調整順序。
3. 可匯入及匯出設定。
4. 設定列表的主要新增按鈕文字為「新增」。
5. `0047 休息日` 與班別同格時，文字依班表既定灰色規則顯示。

權限：

- **員工：** 可查看班表使用中的假別。
- **主管：** 具備員工全部能力，並可新增、修改、排序及管理假別。
- **管理員：** 具備主管全部能力。

### 3.3.6 班表加班設定

欄位：

- 代碼。
- 名稱。
- 背景色。
- 文字色。
- 自動文字色。
- 預設起迄時間。
- 休息一是否啟用及起迄時間。
- 休息二是否啟用及起迄時間。
- 是否隱藏工具列。
- 排序。

規則：

1. 最前方顯示拖曳排序圖示。
2. 只有拖曳圖示可調整順序。
3. 班表加班只影響班表顯示與排班資料，不直接建立打卡加班申請。
4. 可匯入及匯出設定。
5. 設定列表的主要新增按鈕文字為「新增」。
6. 新增加班時名稱預設為「加班」，不得沿用假別預設名稱。

權限：

- **員工：** 可查看班表使用中的加班標記。
- **主管：** 具備員工全部能力，並可新增、修改、排序及管理班表加班設定。
- **管理員：** 具備主管全部能力。

### 3.3.7 國定假日與週期設定

1. 國定假日保存日期、名稱與排序。
2. 週期設定包括：
   - 八週起算日。
   - 每週起算日。
   - 每月起算日。
3. 班表預設顯示今天所在的八週週期，依起算日每 56 天前後推算。
4. 主管與管理員可維護週期與國定假日。
5. 匯出期間依「每月起算日」切分；例如每月起算日為 26 日，每期為上月 26 日至本月 25 日。
6. 三種班表匯出視窗的預設值一律使用「今天所在期間的上一期」。
7. 例如每月起算日為 26 日：
   - 今天是 7 月 28 日時，預設為 6 月 26 日至 7 月 25 日。
   - 今天是 7 月 25 日時，預設為 5 月 26 日至 6 月 25 日。
8. 使用者可在匯出前自行修改開始日期與結束日期，開始日期不得晚於結束日期。
9. 每月起算日為 29、30 或 31，而月份沒有該日期時，該月以最後一日作為起算日。

### 3.3.8 自動排班與例休檢查

自動排班採「先預覽、再套用」：

1. 只處理人員在職期間內的日期。
2. 每位人員每天最多只能安排一個班別。
3. 優先安排人員所屬單位，其次依可支援單位設定順序安排其他單位。
4. 排入班別時依人員可排班班別設定順序選擇有效班別。
5. 依各班別的需求人數補足人力。
6. 手動設定的班別與假別視為鎖定資料，自動排班不得覆寫。
7. 使用固定休假星期與每月休假天數；每月休假天數是固定目標。
8. 已排入的例假與休息日均計入每月休假天數。
9. 若需求人數無法補足，保留空白班表格，不強制安排不符合條件的人員。
10. 同一單位同一天有多個班別缺人時，依班別設定順序補足。
11. 產生結果時檢查例假、休息日與連續上班限制。
12. 預覽只保存在前端，不立即寫入正式班表。
13. 按「套用預覽」後才批次保存至 `schedule_entries`。
14. 可取消預覽，取消時不得修改正式班表。

例休檢查規則：

1. 每 7 天至少有 1 天例假。
2. 每 7 天至少有 1 天休息日。
3. 連續上班不得超過 6 天。
4. 連續上班檢查採滑動區間計算，並包含上一個月延續下來的上班天數。
5. 例休檢查依目前八週範圍，顯示每位人員的缺漏與待確認項目。

權限：

- **員工：** 可查看套用後的完整班表。
- **主管：** 具備員工全部能力，並可執行預覽、套用、取消與例休檢查。
- **管理員：** 具備主管全部能力。
### 3.3.9 排序共通規格

以下設定最前方統一有拖曳圖示：

1. 人員。
2. 單位。
3. 班別。
4. 假別。
5. 班表加班。
6. 訂餐商品。

共通規則：

- 資料列本身不作為拖曳把手。
- 只有最前方圖示可開始拖曳。
- 放開後立即更新畫面。
- 儲存後重新整理與重新登入仍維持順序。
- 一般設定頁拖曳欄約 30px，圖示約 22×26px。
- 訂餐商品依其表格版面使用較大的操作區。

### 3.3.10 自動補班預覽

#### 功能入口與日期

1. 班表頁「功能」選單提供「自動補班預覽」，僅主管與管理員可使用。
2. 與「自動排班預覽」共用「套用預覽」及「取消預覽」按鈕。
3. 啟動後先顯示開始日期與結束日期，預設為班表目前顯示的八週起訖日期。
4. 產生預覽前必須先讀取所選日期範圍的正式班表資料，避免把尚未載入的既有班表誤判為空白。

#### 自動補班規則

1. 只處理計薪方式為月薪的人員；日薪人員完全不處理。
2. 只處理人員在職期間內，且所屬單位有效並顯示於班表的日期。
3. 空白格定義為該格沒有班別、沒有假別、也沒有加班。
4. 已有班別、假別或加班的格子不得覆寫。
5. 補入班別為該人員「排班班別」設定順序中的第一個有效班別。
6. 人員未設定有效排班班別時略過，並顯示提醒。

#### 預覽、套用與復原

1. 產生結果只寫入前端預覽資料，不立即寫入 `schedule_entries`。
2. 預覽格沿用既有自動排班的綠色預覽樣式。
3. 按「套用預覽」並確認後，才將全部預覽格批次寫入正式班表。
4. 按「取消預覽」時清除全部自動補班預覽，不修改正式班表。
5. 套用後必須加入班表復原歷程，可使用上一步還原。
6. 自動補班預覽與自動排班預覽同一時間只能存在一種；新產生的預覽會取代先前預覽。

#### 驗收

1. 月薪人員空白格顯示其第一個排班班別的綠色預覽。
2. 日薪人員沒有任何預覽變更。
3. 已有班別、假別或加班的格子維持原資料。
4. 未按「套用預覽」前重新整理頁面，資料庫不得出現新增班表。
5. 按「套用預覽」後重新整理頁面，補入班別仍存在。
6. 按「取消預覽」後，預覽格消失且資料庫不變。

---

# 第四章　紀錄頁

## 4.1 頁面目的

紀錄頁集中提供個人歷史查詢，以及管理員專用的加班審核與打卡管理。訂餐統計放在訂餐頁，不放在紀錄頁。

## 4.2 分頁

1. **個人記錄：** 所有已登入角色查看自己的班表、打卡、異常、加班與訂餐歷史。
2. **加班審核：** 管理員查詢、調整、核准、退回、批次處理與代為申請。
3. **打卡管理：** 管理員查詢異常、補登或修改打卡並查看異動歷程。

### 4.2.1 個人記錄

#### A. 查詢與按鈕

1. 日期起日。
2. 日期迄日。
3. 使用者變更開始日期或結束日期後，立即重新查詢並更新表格。
4. 本分頁不顯示查詢按鈕。
5. 上一頁。
6. 下一頁。
7. 每頁 50 筆。
8. 可查詢全部歷史，不限最近 30 天。
9. 不顯示操作欄。

查詢欄位版面：

- 開始日期與結束日期放在同一個篩選卡片內。
- 每個日期選擇器上方分別顯示「開始日期」與「結束日期」標題。
- 手機版兩個日期選擇器固定同列、平均分配寬度。
- 日期變更後立即查詢，因此不顯示查詢按鈕。

#### B. 顯示欄位

依序顯示：

1. 日期。
2. 圖示。
3. 班別。
4. 打卡時間。
5. 異常。
6. 加班。
7. 打卡備註。
8. 加班備註。
9. 訂餐。

#### C. 圖示欄規則

1. 直接重現班表同一天的格子內容。
2. 顏色、文字顏色、圓角、字級與分段方式與班表一致。
3. 同一天有多種資料時依序上下疊加：
   - 班別。
   - 假別。
   - 加班。
4. 最多三個分段。
5. `0047 休息日` 與班別同時存在時沿用班表灰色文字規則。
6. 沒有班別、假別與加班時保持空白。

#### D. 打卡時間欄規則

上班與下班合併為單一欄，分兩行顯示：

```text
07:58 莊敬
11:25 莊敬
```

1. 第一行：上班時間及單位。
2. 第二行：下班時間及單位。
3. 缺少其中一筆時該行顯示 `-`。
4. 時間以 `Asia/Taipei` 顯示。
5. 個人頁只顯示必要的時間、單位與狀態，不顯示原始 GPS、精準度、距離或 IP。

#### E. 加班、異常與訂餐

1. 加班欄顯示狀態與總時數。
2. 異常欄可同時顯示多種即時計算異常。
3. 打卡備註、加班備註分欄顯示。
4. 訂餐例如：`雞腿×1、排骨×2`。
5. 訂餐文字使用醒目粉紅樣式。
6. 訂餐所依據的上班卡被清除時顯示警告。

#### F. 權限

- **員工：** 可查詢自己的個人歷史記錄。
- **主管：** 具備員工全部能力。
- **管理員：** 具備主管全部能力。

### 4.2.2 加班審核

#### A. 查詢、按鈕與分頁

1. 日期起日。
2. 日期迄日。
3. 人員。
4. 狀態：待審、核准、退回、全部。
5. 日期、人員或狀態變更後，立即重新查詢並更新審核清單。
6. 本分頁不顯示查詢按鈕。
7. 代為申請。
8. 批次核准。
9. 批次退回。
10. 全選核取方塊與逐筆核取方塊。
11. 上一頁、下一頁。
12. 每頁 20 筆。

手機版篩選與按鈕固定排列：

- 第一列：開始日期／結束日期。
- 第二列：人員／狀態。
- 第三列：代為申請／批次核准／批次退回，三個按鈕平均分配寬度。
- 所有篩選欄位顯示欄位名稱，選單與日期控制項等高。

#### B. 表格欄位

依序顯示：

1. 勾選。
2. 日期。
3. 員工姓名。
4. 班別名稱與班表上下班時間。
5. 打卡時間。
6. 加班時數。
7. 備註。
8. 狀態。
9. 操作。

版面：

- 勾選欄寬約 40px，左右保留少量空間。
- 日期欄寬約 112px，警告顯示於日期下方。
- 狀態欄寬約 62px。
- 操作欄允許按鈕換行。

加班時數格式：

```text
提早＋延後=合計
2＋0.5=2.5
```

#### C. 操作邏輯

1. **調整**
   - 修改提早時數。
   - 修改延後時數。
   - 修改員工加班備註。
   - 儲存為待審。

2. **核准**
   - 將申請設為核准。

3. **退回**
   - 將申請設為退回。
   - 退回原因可留空。

4. **批次核准／退回**
   - 依勾選項目處理。
   - 執行前再次確認。

5. **代為申請**
   - 選擇人員與日期。
   - 輸入提早、延後與備註。
   - 可建立待審。
   - 可建立並直接核准。

6. 時數不得為負數，必須是 0.5 的倍數，可為 0。
7. 打卡異動後：
   - 狀態回待審。
   - 原時數保留。
   - 顯示「打卡時間已異動」。
   - 再次核准或退回後清除警告。
8. 班表異動不改變既有申請。
9. 每次審核與時數調整寫入審核歷程。

#### D. 權限

- **員工：** 可在打卡頁與個人記錄查看自己的申請狀態。
- **主管：** 具備員工全部能力。
- **管理員：** 具備主管全部能力，並可查看所有人的申請、調整、核准、退回、批次處理及代為申請，包含處理自己的申請。

### 4.2.3 打卡管理

#### A. 查詢與按鈕

1. 日期起日。
2. 日期迄日。
3. 人員。
4. 異常類型下拉選單依序顯示「全部顯示」、「全部異常」及各個異常類型。
5. 「全部顯示」包含正常與異常資料；「全部異常」顯示所有異常資料；特定異常類型只顯示該類異常資料。
6. 日期、人員或異常類型變更後，立即重新查詢並更新清單。
7. 本分頁不顯示「只顯示異常」核取方塊及查詢按鈕。
8. 上一頁、下一頁。
9. 每頁 50 筆。
10. 編輯。
11. 歷程。

手機版篩選固定排列：

- 第一列：開始日期／結束日期。
- 第二列：人員／異常類型。
- 異常類型下拉選單以「全部顯示」作為第一個選項，取代原「只顯示異常」核取方塊。
- 所有篩選欄位顯示欄位名稱，選單與日期控制項等高。
- 篩選條件變更後立即查詢，因此不顯示額外的查詢按鈕。

#### B. 表格欄位

1. 日期。
2. 員工姓名與工號。
3. 班別與班表時間。
4. 上班時間與單位。
5. 下班時間與單位。
6. 異常。
7. 每日打卡備註。
8. 操作。

#### C. 編輯視窗

可修改：

1. 上班時間。
2. 上班單位。
3. 下班時間。
4. 下班單位。
5. 每日打卡備註。
6. 本次異動原因；此欄選填並保存於歷程。

可用於：

- 補登上班。
- 補登下班。
- 修改時間。
- 修改單位。
- 清除時間。

儲存後重新計算異常，並依規則把相關加班申請改回待審。

#### D. 歷程

顯示：

1. 操作時間。
2. 欄位或操作類型。
3. 原值。
4. 新值。
5. 原因。
6. 操作人。

同時保存修改前與修改後完整 JSON 快照，供稽核使用。

#### E. 權限

- **員工：** 可在個人記錄查看自己的打卡顯示資料。
- **主管：** 具備員工全部能力。
- **管理員：** 具備主管全部能力，並可查詢全體打卡、查看原始稽核資料、補登、修改、清除與查看歷程。

## 4.3 其他相關規則

1. 個人記錄由 `personal-records-v2` Edge Function 讀取。
2. 個人記錄後端同時讀取班別、假別與班表加班，回傳圖示分段資料。
3. 加班審核清單由 `attendance-overtime-admin-list` 讀取。
4. 加班審核操作由 `attendance-overtime-admin-action` 處理。
5. 打卡管理清單由 `attendance-admin-list-v2` 讀取。
6. 打卡管理寫入及歷程由 `attendance-admin-action-v2` 處理。
7. 查詢結果依日期倒序顯示。
8. 任何管理員異動都需由後端再次驗證角色與帳號有效期間。

---

# 第五章　系統環境、資料庫與其他規格

## 5.1 章節目的

本章集中整理不屬於前四個頁面的共用規格，包括執行環境、程式語言、部署方式、登入與首頁、角色繼承、後端服務、資料庫結構、欄位中文說明、安全、稽核、驗收與版本範圍。

## 5.2 執行環境與使用技術

### 5.2.1 前端

- 型態：瀏覽器靜態網站。
- 原始碼：`src/renderer/`。
- GitHub Pages 發佈目錄：`docs/`。
- 語言：HTML5、CSS3、原生 JavaScript。
- Excel：ExcelJS 4.4.0。
- 頁面設計：手機優先，支援桌面與平板。
- 正式資料來源：Supabase，不使用舊 JSON 文件作為正式儲存。

### 5.2.2 後端

- 平台：Supabase。
- 身分驗證：Supabase Auth。
- 資料庫：PostgreSQL。
- API：Supabase REST、PostgreSQL RPC、Supabase Edge Functions。
- Edge Functions 語言：TypeScript。
- Edge Functions 執行環境：Deno／Supabase Runtime。
- 後端函式使用 `@supabase/server` 的身分驗證包裝。

### 5.2.3 開發與發佈

- Node.js 用於本機預覽、檢查與發佈腳本。
- 主要指令：

```text
npm run web
npm run web:check
npm run web:publish
npm run v2:check
```

- `npm run web`：啟動本機靜態預覽。
- `npm run web:check`：檢查公開 Supabase 設定。
- `npm run web:publish`：將 `src/renderer/` 同步到 `docs/` 並更新資源版本。
- `npm run v2:check`：檢查 V2 檔案與發佈版本對齊。
- 前端修改完成後，`src/renderer/` 與 `docs/` 必須同步。
- 正式程式碼與規格書由 GitHub `main` 分支管理。

## 5.3 登入、首頁與帳號規則

### 5.3.1 未登入

1. 登入介面使用全畫面。
2. 未登入不顯示班表、員工、打卡、加班與訂餐資料。
3. 敏感資料不得先載入後只用 CSS 隱藏。

### 5.3.2 登入後首頁

顯示：

- 使用者姓名。
- 使用者角色。
- 大型打卡入口。
- 班表入口。
- 訂餐入口。
- 紀錄入口。
- 修改密碼。
- 登出。

手機版：

1. 修改密碼與登出在姓名右側靠右。
2. 四個主入口採精簡尺寸，文字置中。
3. 打卡、訂餐、紀錄頁的返回首頁使用 X 圖示。

Android 返回鍵：

1. 有可關閉視窗時關閉視窗。
2. 沒有視窗時回首頁。
3. 已在首頁時保持首頁。

### 5.3.3 角色繼承

- **員工 `employee`：** 使用自己的打卡、加班申請、訂餐與個人記錄，並查看完整班表。
- **主管 `manager`：** 包含員工全部權限，另管理班表、一般單位、人員非角色資料、商品、截止時間、補助、訂餐統計與匯出。
- **管理員 `admin`：** 包含主管全部權限，另管理角色、管理員帳號、單位打卡設定、全體打卡、稽核資料與加班審核。

### 5.3.4 任職有效期間

帳號有效期間：

```text
到職日 00:00
至離職日後第 5 日 23:59
```

1. 沒有離職日代表持續有效。
2. 到職日在未來時尚未生效。
3. 帳號停用、刪除或超過有效期間後，下一次受驗證操作由後端拒絕並清除登入狀態。
4. 最後一位有效管理員受到資料庫或後端交易保護。

### 5.3.5 Session

1. 手機：
   - 使用可跨關閉保留的儲存方式。
   - 48 小時滑動閒置期限。
2. 電腦、Android 平板、iPad、其他平板與觸控筆電：
   - 以分頁階段儲存為主。
   - 30 分鐘滑動閒置期限。
   - 關閉分頁或瀏覽器後需重新登入。
3. 裝置類型無法確認時以電腦規則處理。
4. 每次受驗證操作與使用者互動更新最後活動時間。
5. 閒置逾時後的第一個操作不得成功送出。

## 5.4 共用時間、權限與安全規則

1. 正式判定一律使用伺服器 `Asia/Taipei`：任職有效期間、打卡日期與時間、23:59 限制、加班申請期限、訂餐日期與截止時間、管理員操作時間。
2. 前端顯示可以使用瀏覽器格式化，但不得以使用者本機時間作為正式判定。
3. 所有角色、有效期間、打卡位置、截止時間、時數格式與歷史關聯都由後端或資料庫再次驗證。
4. 單位打卡設定中的地址、座標與固定對外 IP 是公司設定資料，不視為密碼或秘密金鑰；但只有實際需要顯示單位打卡設定頁的管理員才讀取，其他頁面不重複載入。
5. 個別打卡紀錄的原始 GPS、來源 IP、定位精準度、距離與完整稽核快照屬個人行為與稽核資料，只提供管理員及後端服務。
6. 一般員工、主管與管理員依目前功能取得最少但完整的資料；不得因過度遮罩使同一功能在不同角色下產生不同結果。
7. 寫入操作使用 RLS、Edge Function、RPC 或 Service Role 的適當組合。
8. 任何前端隱藏按鈕都不是唯一權限控制。

### 5.4.1 頁面與資料權限矩陣

| 功能 | 員工 | 主管 | 管理員 | 正式資料介面 |
|---|---|---|---|---|
| 登入、首頁、修改自己的密碼 | 本人 | 本人 | 本人 | `get_my_profile_v2()` 與 Supabase Auth |
| 班表查看 | 查看全體相同班表 | 同員工 | 同員工 | `get_schedule_directory_v2()`、`schedule_entries` |
| 班表編輯、排班工具、一般設定 | 不可 | 可 | 可 | 管理名錄、RLS 與班表 RPC |
| 打卡與打卡加班申請 | 僅本人 | 僅本人 | 僅本人 | 打卡及加班 Edge Functions |
| 今日訂餐 | 僅本人 | 僅本人 | 僅本人 | `meal-order` |
| 訂餐統計與訂餐設定 | 不可 | 可 | 可 | 訂餐管理 Edge Functions |
| 個人記錄 | 僅本人 | 僅本人 | 僅本人 | `personal-records-v2` |
| 加班審核 | 不可 | 不可 | 可 | 加班管理 Edge Functions |
| 打卡管理與完整稽核 | 不可 | 不可 | 可 | 打卡管理 Edge Functions |
| 人員設定與一般帳號管理 | 不可 | 可，不能管理管理員角色 | 可 | `get_employee_admin_directory_v2()`、`member-auth-admin` |
| 單位一般設定 | 不可 | 可 | 可 | 單位安全名錄與 RLS |
| 單位打卡地址、座標、固定 IP 與啟用狀態 | 不可 | 不可修改且不需載入 | 可 | `department-attendance-v2` |

同一頁面同一功能的讀取結果，不得因角色而使用不同欄位推測；角色差異只用於是否能進入管理功能、是否能操作及後端是否接受寫入。

## 5.5 後端服務、RPC 與交易

### 5.5.1 主要 Edge Functions

- `attendance-clock`：今日打卡狀態、上班打卡、下班打卡、GPS／IP 驗證。
- `attendance-overtime-employee`：員工加班日期、狀態、送出與刪除。
- `attendance-overtime-admin-list`：管理員加班審核清單。
- `attendance-overtime-admin-action`：審核、調整、批次處理與代為申請。
- `meal-order`：今日訂餐狀態、儲存與設定。
- `meal-report-v2`：訂餐統計。
- `meal-cancel-v2`：取消今日訂餐。
- `personal-records-v2`：個人記錄。
- `attendance-admin-list-v2`：打卡管理清單。
- `attendance-admin-action-v2`：打卡補登、修改與歷程。
- `member-auth-admin`：人員帳號建立、更新、重設密碼與刪除。
- `member-order-v2`：人員排序讀取與保存。
- `department-attendance-v2`：管理員讀取單位敏感打卡設定。

### 5.5.2 主要 RPC

- `get_my_profile_v2()`：只回傳目前登入者本人的完整登入與任職資料，供登入驗證、首頁及本人功能使用。
- `get_schedule_directory_v2()`：回傳所有有效登入者共同使用的班表顯示名錄；所有角色欄位與排序一致。
- `get_employee_admin_directory_v2()`：只允許主管與管理員取得人員設定、帳號管理及自動排班所需欄位。
- `get_department_directory_v2()`：回傳所有有效登入者可使用的一般單位名錄，不含打卡管理設定。
- `save_schedule_entries_bulk(entries jsonb)`：批次保存班表格。
- `save_attendance_clock(...)`：原子保存上班或下班打卡。
- 單位打卡欄位、人員排序、訂餐與管理異動依正式 migration 定義使用 RPC 或交易函式。

### 5.5.3 必須具備交易一致性的操作

1. 上班與下班打卡。
2. 管理員修改打卡並重設加班狀態。
3. 新增、修改與取消整張訂單。
4. 商品價格同步更新今日訂單。
5. 批次加班審核。
6. 帳號與 Auth 資料刪除。
7. 最後一位管理員保護。
8. 批次班表保存。

任一步驟失敗時，不留下部分完成資料。

### 5.5.4 冪等性

以下相同請求重送時不得建立重複資料或改寫第一次有效結果：

- 上班打卡。
- 下班打卡。
- 新增與修改訂單。
- 送出加班申請。
- 批次審核。
- 批次班表保存。

## 5.6 資料庫共通規格

1. 資料庫：PostgreSQL。
2. 主鍵多數使用 UUID。
3. 時間戳使用 `timestamp with time zone`。
4. 日期使用 `date`。
5. 每日班表與打卡以 `Asia/Taipei` 工作日期對應。
6. 目前正式班表來源：`schedule_entries`。
7. 目前正式打卡來源：`attendance_records`。
8. 打卡異動歷程：`attendance_action_logs`。
9. 打卡加班申請：`attendance_overtime_requests`。
10. 加班審核歷程：`overtime_review_logs`。
11. 訂餐商品、設定與訂單：`meal_products`、`meal_settings`、`meal_orders`。
12. 舊 JSON 文件不再作為正式資料來源。
13. 舊的 `attendance_logs` 與 `clock_locations` 已於 2026-07-08 移除；正式打卡流程只使用 `attendance_records` 與 `set_departments` 的打卡設定。
14. 外鍵與唯一限制用來保護歷史關聯及每日唯一資料。
15. 資料表名稱與欄位採英文 snake_case；本規格提供中文意義。


### 5.6.1 現行 SQL 建置檔

1. 全新資料庫固定依序執行：
   - `supabase/001_current_schema.sql`
   - `supabase/002_current_updates.sql`
2. `001_current_schema.sql` 建立目前系統的基準資料表、索引、RLS、權限保護與核心 RPC。
3. `002_current_updates.sql` 依原始 migration 順序整併基準結構後所有仍有效的正式更新，包含班表批次儲存、訂餐、任職與角色保護、打卡與稽核、加班審核、帳號刪除、人員排序、唯一限制及私密資料存取強化。
4. Edge Function 部署不會自動執行 SQL；兩份 SQL 必須先成功套用。
5. SQL Editor 只要出現錯誤即停止，不可略過錯誤繼續執行。

### 5.6.2 已淘汰資料物件

下列舊物件不再是正式資料來源，不得在新功能中恢復使用：

- `leave_requests`
- `overtime_requests`
- `request_status`
- `request_type`
- `public.get_public_schedule_requests()`
- `clock_locations`
- `attendance_logs`

### 5.6.3 一般名錄與敏感欄位

1. 人員資料不得再由單一混合用途 RPC 同時負責登入、班表與管理；必須分為本人資料、共同班表名錄與管理名錄。
2. 班表共同名錄是公司內部共同作業資料，所有有效登入者取得相同的姓名、單位、任職有效區間、PT 狀態與排序。
3. 本人工號、本人角色與本人任職資料由 `get_my_profile_v2()` 提供；其他人員的工號、角色、休假規則、支援順序與可排班班別只由 `get_employee_admin_directory_v2()` 提供給主管或管理員。
4. 單位地址、座標與固定對外 IP 屬打卡設定，不是秘密金鑰；但一般頁面沒有使用需求，因此只在管理員進入打卡設定功能時透過 `department-attendance-v2` 讀取。
5. 個別打卡紀錄的原始 GPS、來源 IP、精準度、距離、管理稽核資料及完整快照不得透過一般名錄或一般 REST 查詢提供。
6. 前端不得以攔截 `fetch`、刪除 JSON 欄位或依載入順序覆寫函式作為主要權限邊界；資料介面本身必須只回傳該用途需要的欄位。
7. 管理名錄採依頁面延遲載入：主管或管理員停留在首頁、打卡、訂餐或個人記錄時，不讀取完整人員管理欄位；只有進入班表管理、人員設定或單位設定時才讀取。

## 5.7 資料表與欄位中文說明

### 5.7.1 `set_employee`－人員與登入對應

- `id`：人員 UUID，同時對應 Supabase Auth 使用者。
- `employee_code`：工號，唯一。
- `full_name`：姓名。
- `role`：角色，`employee`、`manager`、`admin`。
- `home_department_id`：所屬單位。
- `position_name`：職稱或職務名稱。
- `hire_date`：到職日。
- `leave_date`：離職日。
- `pay_by_day`：是否為日薪人員。
- 人員不另設停用狀態；任職與登入資格統一依 `hire_date`、`leave_date` 及離職後五日登入寬限判斷。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。
- `schedule_department_ids`：可支援單位順序。
- `monthly_rest_days`：每月目標休假天數。
- `fixed_rest_weekday`：固定休假星期，0 至 6。
- `schedule_shift_ids`：可排班班別順序。
- `sort_order`：人員顯示排序。

### 5.7.2 `set_departments`－單位與打卡地點設定

- `id`：單位 UUID。
- `name`：單位名稱。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。
- `start_date`：單位開始日期。
- `end_date`：單位結束日期。
- `hidden_from_schedule`：是否從班表隱藏。
- `sort_order`：單位排序。
- `address`：打卡地址。
- `latitude`：公司地點緯度。
- `longitude`：公司地點經度。
- `public_ip`：允許打卡的固定對外 IP，可保存多值文字。
- `attendance_enabled`：是否啟用打卡。
- `attendance_settings_updated_at`：打卡設定最後修改時間。
- `attendance_settings_updated_by`：打卡設定最後修改人。

### 5.7.3 `set_shift`－班別設定

- `id`：班別 UUID。
- `name`：班別名稱。
- `applicable_department_id`：班別唯一適用單位。
- `color`：背景色。
- `start_time`：上班時間。
- `end_time`：下班時間。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。
- `required_staff_count`：每日需求人數。
- `text_color`：文字色。
- `auto_text_color`：是否自動計算文字色。
- `hidden_from_toolbar`：是否從班表工具列隱藏。
- `sort_order`：班別排序。

### 5.7.4 `set_leave`－假別設定

- `id`：假別 UUID。
- `code`：假別代碼。
- `name`：假別名稱。
- `color`：背景色。
- `requires_time`：是否需要填寫起迄時間。
- `requires_reason`：是否需要填寫原因。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。
- `text_color`：文字色。
- `auto_text_color`：是否自動計算文字色。
- `hidden_from_toolbar`：是否從工具列隱藏。
- `sort_order`：假別排序。

### 5.7.5 `set_overtime`－班表加班類型

- `id`：班表加班類型 UUID。
- `code`：類型代碼。
- `name`：類型名稱。
- `color`：背景色。
- `start_time`：預設開始時間。
- `end_time`：預設結束時間。
- `use_rest_1`：是否使用第一段休息。
- `rest_1_start_time`：第一段休息開始時間。
- `rest_1_end_time`：第一段休息結束時間。
- `use_rest_2`：是否使用第二段休息。
- `rest_2_start_time`：第二段休息開始時間。
- `rest_2_end_time`：第二段休息結束時間。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。
- `text_color`：文字色。
- `auto_text_color`：是否自動計算文字色。
- `hidden_from_toolbar`：是否從工具列隱藏。
- `sort_order`：類型排序。

### 5.7.6 `schedule_entries`－每日班表格

- `id`：班表格 UUID。
- `member_id`：人員。
- `work_date`：工作日期。
- `support_department_id`：支援單位。
- `shift_type_id`：班別。
- `leave_type_id`：假別。
- `leave_all_day`：是否整天請假。
- `leave_start_time`：請假開始時間。
- `leave_end_time`：請假結束時間。
- `leave_reason`：請假原因。
- `overtime_type_id`：班表加班類型。
- `note`：班表格備註。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。
- `overtime_start_time`：班表加班開始時間。
- `overtime_end_time`：班表加班結束時間。
- `overtime_use_rest_1`：是否使用第一段休息。
- `overtime_rest_1_start_time`：第一段休息開始。
- `overtime_rest_1_end_time`：第一段休息結束。
- `overtime_use_rest_2`：是否使用第二段休息。
- `overtime_rest_2_start_time`：第二段休息開始。
- `overtime_rest_2_end_time`：第二段休息結束。
- `overtime_reason`：班表加班原因。

唯一限制：`member_id + work_date`。

### 5.7.7 `scheduler_settings`－班表共用設定

- `id`：設定識別值，正式值為 `default`。
- `current_year`：目前年份。
- `current_month`：目前月份，0 至 11。
- `dept_filter`：工具列單位篩選。
- `table_view`：班表檢視模式。
- `table_dept_scope_filter`：班表顯示範圍。
- `table_stats_visible`：是否顯示統計欄。
- `schedule_start_date`：班表目前起始日期。
- `week_start`：每週起算星期。
- `month_start_day`：每月起算日。
- `eight_week_start_date`：八週週期基準日。
- `updated_at`：最後修改時間。

### 5.7.8 `holidays`－國定假日

- `id`：假日 UUID。
- `holiday_date`：假日日期，唯一。
- `name`：假日名稱。
- `sort_order`：排序。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。

### 5.7.9 `attendance_records`－目前有效打卡

- `id`：打卡日記錄 UUID。
- `user_id`：人員。
- `work_date`：工作日期。
- `clock_in_at`：上班打卡時間。
- `clock_out_at`：下班打卡時間。
- `clock_in_department_id`：上班打卡單位。
- `clock_in_department_name_snapshot`：上班單位名稱快照。
- `clock_in_address_snapshot`：上班地址快照。
- `clock_in_source`：上班來源，GPS、IP 或管理員補登。
- `clock_in_latitude`：上班實際緯度。
- `clock_in_longitude`：上班實際經度。
- `clock_in_accuracy`：上班定位精準度。
- `clock_in_distance`：上班距離公司地點公尺數。
- `clock_in_ip`：上班來源 IP。
- `clock_out_department_id`：下班打卡單位。
- `clock_out_department_name_snapshot`：下班單位名稱快照。
- `clock_out_address_snapshot`：下班地址快照。
- `clock_out_source`：下班來源。
- `clock_out_latitude`：下班實際緯度。
- `clock_out_longitude`：下班實際經度。
- `clock_out_accuracy`：下班定位精準度。
- `clock_out_distance`：下班距離公司地點公尺數。
- `clock_out_ip`：下班來源 IP。
- `attendance_note`：每日打卡備註。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。
- `employee_code_snapshot`：工號快照。
- `employee_name_snapshot`：姓名快照。
- `clock_in_company_latitude`：上班時公司緯度快照。
- `clock_in_company_longitude`：上班時公司經度快照。
- `clock_out_company_latitude`：下班時公司緯度快照。
- `clock_out_company_longitude`：下班時公司經度快照。

唯一限制：`user_id + work_date`。

### 5.7.10 `attendance_action_logs`－打卡異動歷程

- `id`：歷程 UUID。
- `attendance_record_id`：對應打卡記錄。
- `action_type`：操作類型。
- `field_name`：異動欄位。
- `old_value`：原值。
- `new_value`：新值。
- `operator_user_id`：操作人。
- `operator_name_snapshot`：操作人姓名快照。
- `created_at`：操作時間。
- `reason`：本次異動原因。
- `old_record`：修改前完整 JSON 快照。
- `new_record`：修改後完整 JSON 快照。

### 5.7.11 `attendance_overtime_requests`－打卡加班申請

- `id`：申請 UUID。
- `user_id`：申請人。
- `work_date`：工作日期。
- `status`：待審、核准或退回。
- `early_overtime_hours`：提早上班時數。
- `late_overtime_hours`：延後下班時數。
- `total_overtime_hours`：合計時數。
- `employee_note`：員工加班備註。
- `attendance_changed_warning`：打卡時間已異動警告。
- `created_by_type`：建立來源，員工或管理員。
- `created_by_user_id`：建立操作人。
- `submitted_at`：送出時間。
- `reviewed_at`：最後審核時間。
- `reviewed_by`：最後審核人。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。
- `is_deleted_by_employee`：是否由員工邏輯刪除。
- `deleted_at`：刪除時間。
- `deleted_by`：刪除操作人。
- `review_note`：審核備註。

### 5.7.12 `overtime_review_logs`－加班審核歷程

- `id`：審核歷程 UUID。
- `overtime_request_id`：對應加班申請。
- `old_status`：原狀態。
- `new_status`：新狀態。
- `old_early_hours`：原提早時數。
- `new_early_hours`：新提早時數。
- `old_late_hours`：原延後時數。
- `new_late_hours`：新延後時數。
- `operator_user_id`：操作管理員。
- `created_at`：操作時間。

備註調整仍保存於申請的 `employee_note`；需要逐次稽核時，後端應把備註變更納入稽核快照或擴充歷程欄位。

### 5.7.13 `meal_products`－訂餐商品

- `id`：商品 UUID。
- `name`：商品名稱，唯一。
- `price`：價格。
- `is_active`：是否啟用。
- `sort_order`：商品排序。
- `created_at`：建立時間。
- `updated_at`：最後修改時間。

### 5.7.14 `meal_settings`－訂餐共用設定

- `id`：設定識別值，正式值為 `default`。
- `daily_cutoff_time`：每日截止時間。
- `updated_by`：最後修改人。
- `updated_at`：最後修改時間。
- `company_subsidy`：每人每日公司補助。

### 5.7.15 `meal_orders`－訂餐品項明細

- `id`：訂餐品項 UUID。
- `order_id`：同一人同一天整張訂單識別值。
- `user_id`：訂餐人。
- `employee_code_snapshot`：工號快照。
- `employee_name_snapshot`：姓名快照。
- `order_date`：訂餐日期。
- `department_id`：訂餐單位。
- `department_name_snapshot`：單位名稱快照。
- `clock_location_id`：訂餐所依據的上班打卡單位識別；欄名為相容命名，目前外鍵指向 `set_departments.id`。
- `product_id`：商品。
- `product_name_snapshot`：商品名稱快照。
- `quantity`：數量。
- `unit_price`：單價。
- `subtotal`：小計。
- `note`：品項備註。
- `submitted_at`：第一次下訂時間。
- `updated_at`：最後修改時間。

唯一限制：`user_id + order_date + product_id`。

## 5.8 稽核、驗收與發佈

### 5.8.1 稽核

至少保存：

- 角色修改。
- 到職與離職日修改。
- 密碼重設。
- 帳號刪除。
- 單位打卡設定修改。
- 打卡補登、修改與清除。
- 加班審核及時數調整。
- 操作者、時間、操作類型、修改前與修改後資料。

### 5.8.2 必要驗收

登入：

- 手機使用 48 小時滑動閒置。
- 平板與電腦使用 30 分鐘滑動閒置。
- 超過任職有效期間後，第一個受驗證操作被拒絕。

打卡：

- 手機 GPS 299 公尺且精準度 299 公尺可通過。
- 301 公尺或精準度 301 公尺時改驗證 IP。
- 電腦不要求 GPS。
- 無上班卡仍可打下班卡。
- 重複請求不改寫第一次時間。
- 手機完成上班打卡後，同帳號在電腦進入打卡頁需立即顯示時間、單位與來源。

加班：

- 提早 29 分鐘不產生 0.5 小時。
- 提早 30 分鐘產生 0.5 小時。
- 第五日 23:59 前可申請。
- 打卡異動後回待審但時數不變。
- 調整視窗可修改時數與備註。
- 批次操作前有確認。

訂餐：

- 無上班卡時不建立訂單。
- 無班表但有上班卡可訂餐。
- 首次全部 0 不建立訂單。
- 已有訂單全部改 0 需確認。
- 數量只接受 0 或正整數。
- 停用品項只可減少或取消。
- 截止後停止新增、修改與取消。
- 上班卡清除後訂單保留並顯示警告。

班表：

- 所有登入角色可看完整班表。
- 主管及管理員可編輯。
- 人員、單位、班別、假別、加班與訂餐品項排序在重新整理後保留。
- 個人記錄圖示與班表同日內容、順序與顏色一致。

### 5.8.3 發佈

1. 前端原始碼位於 `src/renderer/`，CSS 唯一原始碼位於 `src/renderer/css/`。
2. `src/renderer/app.css`、`src/renderer/app.js` 與 `docs/` 均為建置／發布產物，不直接手動修改。
3. 修改前端後執行 `npm run web:publish`，由腳本建立 CSS 與 JavaScript bundle、清理並重建 `docs/`。
4. CSS 或 JavaScript 內容變更時更新靜態資源版本，避免瀏覽器使用舊快取。
5. Edge Function 修改後部署到同一個 Supabase 專案。
6. 資料庫結構修改使用現行正式 SQL，執行順序依 README 與第五章規格。
7. GitHub Pages 由 `.github/workflows/deploy-pages.yml` 自動部署。
8. Pages 工作流程使用 `ubuntu-latest`，並在上傳 artifact 前設定 Node.js、執行 `npm run web:publish` 與必要檢查。
9. Pages Action 使用 `actions/checkout@v5`、`actions/setup-node@v4`、`actions/configure-pages@v5`、`actions/upload-pages-artifact@v5`、`actions/deploy-pages@v5`。
10. 發佈前必須確認入口只載入單一 `app.css`，本機 JavaScript 只載入 `app-config.js` 與單一 `app.js`，且語法、bundle、V2 對齊及主要驗收案例通過。

### 5.8.4 JavaScript 架構與漸進式整理

1. 前端採「原始碼分檔、正式發布單一化」。既有功能模組維持獨立原始檔，`scripts/build-js.js` 依固定順序產生 `src/renderer/app.js`，發布後為 `docs/app.js`。
2. `app-config.js` 保存公開部署設定，必須在 `app.js` 前獨立載入；除外部 ExcelJS 外，入口不得再直接載入其他本機 JavaScript。
3. `app.js` 為自動產生檔，不得直接修改。`docs/` 不發布個別 JavaScript 原始模組。
4. 禁止在執行期間以 `document.createElement("script")` 補載本機功能檔，也禁止依靠不同快取版本或載入時機重複載入模組。
5. 第一階段為相容性 bundle：保留既有全域函式、全域狀態與原始載入順序，不在同一次異動全面改寫成 ES Modules。
6. 後續應逐步把共用 API 呼叫、日期格式、驗證、狀態管理與各功能頁拆成明確責任模組，並消除後載入檔案覆寫前一版本函式的做法。
7. 不得新增 `fix`、`refinement`、`final`、`v3` 等補丁型 JavaScript；需求應修改責任正確的現有模組，或建立具明確功能名稱的新模組並加入固定建置清單。
8. 每次修改模組順序、移除覆寫或轉換為 ES Modules 時，必須分階段執行並通過 `npm run js:check`、`npm run web:publish`、`npm run v2:check` 與相關功能驗收。

## 5.9 本版本不包含項目

1. 跨日夜班。
2. 一天多班。
3. 一天多次上下班。
4. 未來日期訂餐。
5. 依星期設定不同截止時間。
6. 特定日期例外截止時間。
7. 二級加班審核。
8. 主管執行加班審核。
9. 主管執行打卡管理。
10. 網頁取得電腦 MAC 位址。
11. 新的密碼錯誤鎖定、驗證碼或雙因素驗證。
12. 將打卡加班申請同步回班表加班。
13. 以舊 JSON 文件取代正規化資料表。

---

# 第六章　介面樣式統一規格

## 6.1 適用範圍

本章定義目前前端介面的統一樣式，適用於登入、首頁、打卡、訂餐、班表、紀錄、設定視窗、管理視窗及共用彈出視窗。

介面採手機優先，同一類型元件在不同頁面使用相同高度、圓角、配色、間距、聚焦狀態與停用狀態。

## 6.2 CSS 架構與載入順序

前端採「原始碼模組化、發布檔單一化」：開發時依責任維護多個 CSS 模組，瀏覽器只載入由建置腳本產生的單一 `app.css`。

### 6.2.1 原始碼模組

正式 CSS 原始碼位於 `src/renderer/css/`，固定順序與責任如下：

1. **`foundation.css`**
   - 全域基礎、主要頁面結構及班表既有結構。
   - 不再新增共用按鈕、表單、卡片或一般表格規則。
2. **`schedule.css`**
   - 班表導覽、浮動工具列、凍結欄及水平捲動框架。
3. **`components.css`**
   - 共用設計變數、表單控制項、按鈕、頁籤、卡片、彈窗及一般資料表格。
   - 同類共用元件以此檔為唯一正式樣式來源。
4. **`responsive.css`**
   - 跨頁面的手機與平板響應式規則。
   - 五個主要頁面的統一安全間距及手機外層配置。
5. **`pages.css`**
   - 登入、打卡等無法抽成共用元件的頁面專屬最終規則。

不得新增 `fix.css`、`refinement.css`、`final.css` 等依靠載入順序修補前檔的零碎樣式；新規則必須回到責任正確的模組。

### 6.2.2 Bundle 與入口

1. `scripts/build-css.js` 依上述固定順序合併模組。
2. 產生檔為 `src/renderer/app.css`，發布後為 `docs/app.css`。
3. `app.css` 為自動產生檔，不得直接修改，也不得使用 `@import`。
4. `index.html` 只載入一個 CSS：`app.css`。
5. 建置時依 bundle 內容產生快取版本；發布時再更新正式靜態資源版本。
6. `npm run css:check` 必須能確認 bundle、模組及入口版本一致。

### 6.2.3 漸進式整理原則

本次先保持原有 CSS 宣告順序與最終視覺結果，再逐步把 `foundation.css` 中仍與共用元件重疊的舊宣告移至 `components.css`。後續整理每次都需以視覺回歸與既有驗收規格為準，不得一次大量刪除而改變正式介面。

## 6.3 共用尺寸與間距

### 6.3.1 一般尺寸

| 項目 | 變數 | 目前值 |
|---|---|---:|
| 一般控制項高度 | `--ui-control-height` | 40px |
| 精簡控制項高度 | `--ui-control-height-compact` | 34px |
| 一般控制項圓角 | `--ui-control-radius` | 12px |
| 圖示按鈕圓角 | `--ui-icon-radius` | 10px |
| 表格外框圓角 | `--ui-table-radius` | 14px |
| 內容區塊圓角 | `--ui-section-radius` | 18px |
| 主卡片與彈窗圓角 | `--ui-page-radius` | 24px |

### 6.3.2 手機尺寸

手機寬度不超過 640px 時使用：

| 項目 | 變數 | 目前值 |
|---|---|---:|
| 頁面外距 | `--ui-mobile-page-gutter` | 8px |
| 主卡片內距 | `--ui-mobile-card-padding` | 10px |
| 次級區塊內距 | `--ui-mobile-section-padding` | 9px |
| 一般元件間距 | `--ui-mobile-gap` | 6px |
| 緊密元件間距 | `--ui-mobile-gap-tight` | 4px |
| 一般控制項高度 | `--ui-control-height` | 42px |

手機版縮減視覺留白，但不縮小輸入框、下拉選單及主要按鈕的觸控高度。五個主要頁面的最外層卡片依 6.9 移除額外內距；此處主卡片內距變數適用其他卡片與彈窗。

## 6.4 配色與狀態

1. 介面使用暖色系表面色、棕色主操作色及中性文字色。
2. 共用語意變數包含：`--ui-surface`、`--ui-surface-raised`、`--ui-surface-muted`、`--ui-surface-hover`、`--ui-border`、`--ui-text`、`--ui-muted`、`--ui-accent`、`--ui-accent-strong`、`--ui-danger`、`--ui-focus-ring`、`--ui-row-hover`。
3. 班別、假別與加班顏色由設定資料決定。
4. 聚焦元件顯示主色邊框與清楚的 focus ring。
5. 停用與唯讀元件使用淡色背景、較淡文字及不可操作游標。
6. 錯誤欄位使用危險色邊框並顯示文字訊息。

## 6.5 表單控制項

1. 文字、數字、密碼、日期、時間及下拉選單使用相同高度與 12px 圓角。
2. 一般左右內距為 12px；手機版為 8px。
3. 同一表單列中的輸入框、日期、時間與下拉選單等高。
4. `.form-row` 內控制項預設使用可用寬度。
5. 多行文字框最小高度為 88px；編輯打卡備註最小高度為 96px。
6. 核取方塊與單選按鈕為 18px，使用系統主色。
7. 日期與時間欄保留瀏覽器原生選擇器。
8. 表單標籤與控制項之間使用緊密間距，手機版原則上為 3px 至 4px。

## 6.6 按鈕與頁籤

### 6.6.1 按鈕

1. 主要操作使用 `.primary-btn` 或 `.btn-primary`。
2. 次要操作使用 `.ghost-btn`、`.nav-btn` 或 `.btn-cancel`。
3. 一般按鈕高度為 40px、圓角為 12px、字級為 14px。
4. `.compact-btn` 高度為 34px，適用於表格與密集工具列。
5. 手機版按鈕左右內距為 8px；精簡按鈕為 6px。
6. 圖示按鈕使用 10px 圓角；首頁返回按鈕及明確圓形功能可使用圓形。
7. 圖示按鈕提供 `title` 或 `aria-label`。
8. 停用按鈕不顯示位移效果及操作陰影。

### 6.6.2 頁籤

1. 訂餐與紀錄頁使用相同頁籤結構。
2. 選取頁籤使用主操作色背景與白色文字。
3. 訂餐與紀錄頁的膠囊頁籤文字統一為 15px、粗體 800。
4. 手機版頁籤外距與內距使用 4px 至 8px 的緊密規格。
5. 頁籤切換不得改變整列高度。

## 6.7 表格

### 6.7.1 一般資料表格

1. 今日訂餐、訂餐統計、訂餐設定、個人記錄、加班審核及打卡管理六頁使用同一套表格表頭與資料列樣式。
2. `.records-table`、`.meal-order-table`、`.meal-settings-table` 的表頭、資料列、分隔線、懸停狀態及表頭圓角由同一組共用 CSS 規則控制；功能專屬 CSS 只設定欄寬與必要對齊。
3. 表格外框圓角為 14px。
4. 表頭使用不透明的 `--ui-surface-muted` 背景。
5. 表頭高度為 42px，資料列最小高度為 44px；表頭文字為 13px、粗體 800、行高 1.35。
6. 電腦版表頭與儲存格水平內距原則上為 12px。
7. 手機版一般表頭內距為 6px，資料儲存格為 5px 6px。
8. 表格寬度超過容器時，只在表格容器內水平捲動。
9. 空資料狀態在表格內顯示文字。

### 6.7.2 設定表格

1. 人員、單位、班別、假別及加班設定的上方操作列使用相同排列。
2. 手機版第一列顯示標題與關閉按鈕；第二列顯示匯出、匯入與新增按鈕，三個按鈕平均分配寬度。
3. 設定表格使用內部橫向捲動與不透明黏性表頭。
4. 目前最小表格寬度：單位設定 720px、班別設定 1040px、假別設定 880px、加班設定 1060px。
5. 單位設定欄位順序固定為拖曳、單位、所屬人員、操作。
6. 捲動容器本身提供外框，不使用會跟著內容移動的重複偽元素外框。

### 6.7.3 訂餐表格

1. 今日訂餐欄位為商品、價格、數量、備註。
2. 今日訂餐與訂餐設定使用相同的共用表頭、外框、列分隔線及表格內滑動容器，不使用卡片式資料列。
3. 商品欄電腦版寬度為 150px 至 220px，使用約 22% 表格寬度。
4. 商品欄手機版寬度為 82px。
5. 商品欄左內距電腦版為 14px，手機版為 6px。
6. 價格與數量欄使用固定窄欄，備註欄使用剩餘寬度。
7. 手機版每個商品維持單列表格；表頭內距為 6px，資料儲存格為 5px 6px，資料列之間不留卡片間距。
8. 訂餐統計與訂餐設定的上方控制列使用「欄位區＋操作區」結構；日期、時間、單位、人員、報表內容與公司補助均顯示欄位名稱。
9. 電腦版控制欄位靠左、操作按鈕與輸入控制項等高。
10. 訂餐統計手機版固定使用三列雙欄：開始日期／結束日期、單位／人員、報表內容／匯出 Excel。
11. 訂餐統計的總數量與總金額卡片固定顯示於同一列，標題使用「總數量」及「總金額」，不加「期間」字樣。
12. 訂餐設定手機版的新增商品與儲存設定按鈕平均分配可用寬度。

### 6.7.4 班表表格

1. 班表主表格保留專用格線、凍結欄、八週日期欄與拖曳選取樣式。
2. 電腦版日期標題上方的同步水平捲軸不顯示外框、圓角或陰影。
3. 手機版隱藏日期標題上方的同步水平捲軸，使用班表內容本身左右滑動。
4. 日期標題框線與班表左右、下方框線畫在固定容器上，不跟著水平內容移動。

## 6.8 卡片、區塊與彈出視窗

1. 電腦與平板版的首頁、打卡、訂餐、紀錄、班表與登入主卡片使用 24px 圓角；寬度不超過 640px 的五個主要頁面依 6.9 移除最外層卡片樣式。
2. 次級內容區塊使用 18px 圓角。
3. 訂餐與紀錄頁的分頁內容標題電腦版為 18px、手機版為 17px，行高 1.3。
4. 除五個主要頁面的手機版最外層卡片外，手機版主卡片內距為 10px；次級區塊內距為 9px。
5. 一般彈窗使用 24px 圓角，標題、內容與底部操作區使用一致分隔線及間距。
6. 手機版彈窗外距為 4px，最大高度為 `100dvh - 8px`。
7. 內容過高時由彈窗內容區捲動。
8. 編輯打卡視窗電腦版最大寬度約 560px，時間與單位欄位使用兩欄；手機版使用單欄。
9. 彈窗底部取消按鈕位於主要按鈕之前。

## 6.9 手機版頁面配置

### 6.9.1 適用頁面與斷點

1. 五個主要頁面為首頁、打卡頁、班表頁、訂餐頁與紀錄頁。
2. 手機版定義為畫面寬度不超過 640px；頁面上方與左右保留 8px 安全間距。
3. 641px 至 960px 的窄螢幕，最外層 `.app-shell` 上方外距統一為 12px。
4. 320px 至 960px 寬度下不得因本節規格產生整頁水平捲動或內容溢出。

### 6.9.2 主要頁面垂直排列與外層卡片

1. 五個主要頁面的內容一律由畫面上方開始排列，不得使用上下置中。
2. 即使內容高度小於可視高度，也不得在內容上方留下因垂直置中產生的大面積空白。
3. 首頁與打卡頁在手機及窄螢幕必須使用 `justify-content: flex-start`。
4. 寬度不超過 640px 時，五個主要頁面的最外層內容卡片不顯示背景、邊框、圓角與陰影，且不占用額外內距。
5. 頁面內部功能卡片、表格、輸入框、按鈕與狀態區塊仍保留各自的視覺區隔。
6. 班表浮動班別／假別／加班工具列不屬於移除範圍，仍維持浮動視窗樣式，距螢幕邊緣 4px、工具列內距 6px。
7. 平板與電腦版維持原有外層卡片樣式。

### 6.9.3 首頁入口按鈕

1. 首頁「打卡、班表、訂餐、記錄」四個入口按鈕採單欄排列。
2. 每個入口按鈕最低高度為 104px，垂直間距為 16px。
3. 四個入口按鈕使用相同背景色、文字色、邊框與陰影。
4. 「打卡」按鈕不得使用與其他三個入口不同的深色主按鈕樣式。
5. 按鈕文字水平與垂直置中。

### 6.9.4 共用手機版排版

1. 一般區塊間距為 6px，緊密操作列間距為 4px。
2. 篩選列、按鈕列與導覽列可換行，不得造成整頁水平溢出。
3. 個人記錄、加班審核與打卡管理的篩選欄位使用共用卡片與固定雙欄網格；開始／結束日期同列，人員／狀態或異常類型同列，操作按鈕使用獨立等寬網格。
4. 一般兩欄表單可改為單欄，但明定使用雙欄的緊湊型篩選列及加班申請時數欄除外。
5. 表格過寬時由表格容器滑動，不壓縮所有欄位到同一畫面。
6. 記錄頁姓名下方不顯示額外說明文字。

### 6.9.5 班表手機字級與今日標示

1. 班表頁頂部「檢視方式」與「顯示範圍」下拉選單文字為 15px，最小高度 42px。
2. 班表浮動工具列所有下拉選單文字為 15px，最小高度 38px；不得為縮小工具列將字級降至 12px 或更小。
3. 班表固定欄與日期欄標題字級為 13px，星期文字為 11px；標題列高度至少 38px，文字垂直及水平置中。
4. 今天日期欄使用高對比深橘漸層底色、白色文字、深色內框與輕微陰影。
5. 今天日期樣式優先於星期六日及交錯週底色，不得被其他日期背景覆蓋。
6. 電腦與平板版既有字級與版面不受影響。

### 6.9.6 實作與發佈

1. 跨頁面手機配置來源為 `src/renderer/css/responsive.css`。
2. 共用控制項與卡片樣式來源為 `src/renderer/css/components.css`。
3. 涉及入口或資源載入時更新 `src/renderer/index.html`，發布檔由 `npm run web:publish` 產生。
4. 修改 CSS 模組後重新產生 `app.css`，不得直接在 bundle 末端追加修補規則。
5. 發布時更新靜態資源版本參數，避免手機瀏覽器沿用舊快取。

## 6.10 無障礙與實作規則

1. 所有可操作元件可使用鍵盤取得焦點。
2. 聚焦狀態清楚可見。
3. 狀態不得只靠顏色表示，需搭配文字、圖示、邊框或形狀差異。
4. 主要文字與背景保持足夠對比。
5. 固定外觀不得使用大量行內 `style`。
6. 固定 CSS 不得由 JavaScript 動態建立 `<style>` 標籤。
7. 動態業務顏色及資料產生的尺寸可由 JavaScript 或行內變數設定。
8. 新元件優先使用現有共用類別與設計變數。
9. 功能專屬類別只設定該功能必要的欄寬、排列及特殊限制。
10. 靜態樣式修改時同步更新 `src/renderer/`、`docs/` 與資源版本參數。

### 拖曳排序與捲動位置統一規則

1. 適用於單位排序、單位內人員排序與跨單位移動、班別／假別／加班設定清單、訂餐商品、班表人員與單位，以及後續新增的所有拖曳排序功能。
2. 開始拖曳時記錄頁面及所在容器的垂直與水平捲動位置。
3. 放下後即使重新渲染、重建表格或重新開啟設定視窗，也必須恢復拖曳前的捲動位置。
4. 不得因排序儲存、畫面刷新或 DOM 重建跳到頁面或彈出視窗最上方。
5. 同時存在彈出視窗內容區、表格捲動區與頁面捲動區時，分別保存與恢復各自位置；橫向捲動表格也保留水平位置。
6. 排序完成後，使用者留在原本瀏覽的資料附近，可立即繼續下一次拖曳。
7. 排序結果立即顯示並沿用現有保存流程寫入後端；保留捲動位置不得影響拖曳預覽、跨單位移動、修改或刪除功能。
8. 所有拖曳排序功能共用同一套捲動位置保存與恢復機制；新增拖曳排序頁面時預設即須符合本規則。

## 6.11 驗收標準

1. 同類型輸入框、下拉選單、日期、時間與按鈕在不同頁面外觀一致。
2. 手機版五個主要頁面的上方與左右安全間距為 8px，內容由上方開始排列，最外層卡片不使用額外內距；其他主卡片內距為 10px，次級區塊內距為 9px。
3. 手機版主要控制項高度至少 42px。
4. 人員、單位、班別、假別及加班設定的標題列與操作按鈕排列一致。
5. 設定表格左右滑動後，表頭底色、欄位對齊與外框保持正確。
6. 訂餐商品欄不占用過多寬度，備註欄使用主要剩餘空間。
7. 班表水平滑動時，固定外框不消失；手機版不顯示日期標題上方的額外捲軸。
8. 編輯打卡視窗的欄位與按鈕使用共用圓角、高度及手機單欄配置。
9. 鍵盤操作時所有互動元件都有可見焦點。
10. `src/renderer/` 與 `docs/` 的前端檔案保持一致。

---

# 第七章　後端架構與平台遷移

## 7.1 章節目的

本章說明目前採用「GitHub Pages 靜態前端＋Supabase 後端服務」的原因、各層責任與安全邊界，並定義未來遷移至具備自有後端執行環境的平台時的對應方式。

本章的目的不是限定未來一定使用 Supabase，而是確保更換託管平台、後端語言或身分驗證服務時，現有權限、安全、交易、稽核與資料一致性規則不會遺失。

## 7.2 現行架構與採用原因

### 7.2.1 GitHub Pages 的定位

1. GitHub Pages 只負責託管 `docs/` 內的 HTML、CSS、JavaScript、圖片與其他靜態檔案。
2. GitHub Pages 不提供可執行伺服器端程式的常駐後端環境，不能直接執行 Node.js、PHP、Python、.NET 或其他後端程式。
3. 發布到 GitHub Pages 的 JavaScript 會下載到使用者瀏覽器，程式碼與其中的公開設定均可被查看或修改。
4. 因此前端 JavaScript 不得保存高權限金鑰，也不得作為角色、權限、正式時間、IP、GPS、交易及稽核的唯一判定者。

### 7.2.2 Supabase 在目前架構中的角色

目前由 Supabase 提供：

- Supabase Auth：登入與使用者身分。
- PostgreSQL：正式資料庫。
- RLS 與資料庫限制：資料列權限、唯一性、外鍵與基本安全邊界。
- PostgreSQL RPC：原子寫入、批次保存及需交易一致性的資料庫操作。
- Edge Functions：伺服器端 API、角色驗證、敏感資料處理、請求資訊判斷與多步驟業務流程。

`supabase/functions/` 內的每個子資料夾代表一個 Edge Function 的原始碼；部署後在 Supabase 執行，不會由 GitHub Pages 直接執行。

### 7.2.3 與 GitHub Pages 的關係

1. 目前需要 Edge Functions，主要原因是 GitHub Pages 本身沒有後端執行環境，而系統又需要可信任的伺服器端處理。
2. 但角色驗證、敏感資料保護、伺服器時間、交易一致性、冪等性與稽核等需求，不會因未來改用具備後端的平台而消失。
3. 未來若遷移至自有後端平台，Edge Functions 的程式可改寫成該平台的 API Controller、Route、Service 或 Serverless Function；不能單純把這些邏輯移回瀏覽器 JavaScript。

## 7.3 前端、Edge Function 與資料庫分工

### 7.3.1 前端 JavaScript

前端負責：

- 顯示頁面、表單、按鈕及操作狀態。
- 收集使用者輸入與手機 GPS 資料。
- 執行方便使用者操作的格式檢查與提示。
- 帶入登入憑證呼叫正式 API。
- 顯示後端回傳的結果與錯誤。

前端不得：

- 保存 `service_role`、資料庫密碼或其他伺服器 Secret。
- 只靠隱藏按鈕或前端變數判定權限。
- 相信前端自行傳入的角色、使用者 ID、正式日期、來源 IP 或核准狀態。
- 直接執行可繞過 RLS 的管理操作。

### 7.3.2 Edge Functions

Edge Functions 負責：

- 驗證登入 Token 並取得真正的使用者身分。
- 重新查詢角色、帳號啟用狀態及任職有效期間。
- 使用伺服器端 `Asia/Taipei` 日期與時間進行正式判定。
- 取得平台收到的來源 IP、請求 Header 與其他可信任請求資訊。
- 使用伺服器端 Secret 或管理權限存取必要資料。
- 隔離固定 IP、原始 GPS、定位精準度、距離及稽核資料等敏感內容。
- 協調多個查詢、RPC 或外部服務，形成完整業務流程。
- 回傳前先過濾欄位，只提供目前角色必要的資料。
- 統一錯誤格式、操作結果及安全紀錄。

### 7.3.3 PostgreSQL、RLS 與 RPC

資料庫層負責：

- 正式資料保存。
- 主鍵、外鍵、唯一限制及欄位限制。
- RLS 與資料列層級權限。
- 需要原子性的新增、修改、刪除及批次操作。
- 防止重複請求改寫第一次有效結果。
- 交易失敗時完整回復，不留下部分完成資料。
- 保存稽核、歷史快照及不可由前端任意變更的結果。

### 7.3.4 標準資料流程

```text
瀏覽器前端
  ↓ 登入憑證＋使用者輸入
Edge Function／正式後端 API
  ↓ 驗證身分、角色、時間、位置與業務規則
PostgreSQL RPC／資料庫交易
  ↓ 原子保存、限制、RLS 與稽核
Edge Function／正式後端 API
  ↓ 過濾敏感欄位
瀏覽器前端
```

## 7.4 現行後端功能清單

正式 Edge Function 部署清單以 `scripts/deploy-v2-final.ps1` 的 `$functions` 陣列為準。`supabase/functions/` 中未列入該腳本的資料夾，不視為目前正式部署端點。

| 現行 Edge Function | 主要責任 | 未來後端對應 |
|---|---|---|
| `member-auth-admin` | 建立、修改帳號與重設密碼 | 帳號管理 API＋身分服務介接 |
| `catalog-admin` | 班別、假別與班表加班設定的受保護管理操作 | 設定管理 API＋目錄 Service |
| `report-records` | 共用個人記錄、管理查詢與報表資料整合 | 記錄／報表查詢 API |
| `attendance-clock` | 今日打卡狀態、GPS／IP 驗證、上下班打卡 | 打卡 Controller＋位置驗證 Service |
| `attendance-clock-safe` | 代理打卡請求並過濾敏感回傳欄位 | 安全 API Gateway／回應過濾層 |
| `meal-order` | 今日訂餐、商品設定讀取與訂單保存 | 訂餐 API＋訂單 Service |
| `attendance-overtime-employee` | 員工加班資格、狀態、送出與刪除 | 員工加班 API＋加班 Service |
| `attendance-overtime-admin-list` | 管理員加班審核清單 | 管理端加班查詢 API |
| `attendance-overtime-admin-action` | 調整、核准、退回、批次處理與代為申請 | 管理端命令 API＋交易 Service |
| `attendance-admin-list-v2` | 管理員打卡資料與異常查詢 | 打卡管理查詢 API |
| `attendance-admin-action-v2` | 補登、修改、清除與異動歷程 | 打卡管理命令 API＋稽核 Service |
| `department-attendance-v2` | 管理員讀取及維護單位打卡敏感設定 | 單位打卡設定管理 API |
| `member-delete-v2` | 帳號刪除、自刪驗證及最後管理員保護 | 帳號刪除 API＋交易 Service |
| `member-order-v2` | 人員排序讀取與保存 | 排序設定 API |
| `personal-records-v2` | 個人班表、打卡、加班與訂餐記錄 | 個人記錄彙整 API |
| `meal-report-v2` | 訂餐統計與管理查詢 | 訂餐報表 API |
| `meal-cancel-v2` | 整張取消今日訂餐 | 訂單取消 API＋交易 Service |

新增、移除或改名正式 Edge Function 時，部署腳本、本節與根目錄 README 必須同步更新。

## 7.5 遷移至其他後端平台的對應原則

| 現行元件 | 遷移後可對應為 | 必須保留的能力 |
|---|---|---|
| GitHub Pages `docs/` | 新平台靜態檔案、CDN 或前端網站 | 相同前端功能與 HTTPS |
| `src/renderer/web-api.js` | 新後端 API Client／Gateway | 集中設定 API Base URL 與認證方式 |
| Supabase Edge Function | Controller、Route、Service、Serverless Function | 身分、角色、時間、安全與業務驗證 |
| Supabase Auth | 保留 Supabase Auth或改用其他身分服務 | 唯一使用者 ID、登入、Token／Session、停用與密碼管理 |
| Supabase JWT | 新平台 JWT 或安全 Session Cookie | 後端可驗證且不可由前端偽造 |
| `service_role` | 後端 Secret、資料庫服務帳號 | 只能存在伺服器環境，不得送到瀏覽器 |
| PostgreSQL RPC | 後端 Service＋交易、Stored Procedure 或保留 RPC | 原子性、冪等性、限制與錯誤回復 |
| RLS | 保留 RLS，或改由後端授權層加資料庫權限 | 每次讀寫均依使用者及角色限制 |
| Supabase PostgreSQL | 新平台 PostgreSQL 或相容資料庫 | 資料型別、關聯、唯一限制、歷史與稽核完整 |

遷移後可更換程式語言與框架，但前端不應直接依賴某個後端框架的內部實作。前端與後端之間應以穩定的 API 請求及回應格式連接。

## 7.6 建議遷移順序

1. 盤點 `supabase/functions/`、RPC、RLS、資料表、Secret 與排程。
2. 整理每個 API 的網址、HTTP 方法、請求欄位、回應欄位、錯誤格式及角色限制。
3. 讓 `src/renderer/web-api.js` 集中處理 API Base URL、Token／Session 及錯誤格式，避免頁面直接寫死 Supabase Function 網址。
4. 在新平台先建立身分驗證、使用者 ID 對應、角色與任職有效期間驗證。
5. 依功能逐項建立相容 API，優先保持原本前端呼叫格式，降低同時修改前後端的風險。
6. 搬移或連接 PostgreSQL 資料庫，重建外鍵、唯一限制、索引、RLS 或等效授權規則。
7. 搬移 RPC 與交易邏輯，逐項驗證重複請求、失敗回復與併發操作。
8. 以測試帳號同時比較舊後端與新後端的回應及資料結果。
9. 完成完整備份、切換計畫與回復計畫後，才將正式前端 API 位址切至新平台。
10. 舊 Supabase 後端保留至新平台通過正式驗收及觀察期，再停止服務。

## 7.7 遷移時不得破壞的規則

1. 不得把伺服器 Secret、高權限金鑰或資料庫密碼移到前端。
2. 不得只依靠前端隱藏按鈕、角色文字或使用者傳入的 ID 判定權限。
3. 正式日期、時間、截止時間及任職有效期間仍由後端以 `Asia/Taipei` 判定。
4. 來源 IP 必須由後端根據可信任 Proxy Header 取得，不接受前端自行填入。
5. GPS 可由前端取得，但距離、精準度與打卡資格必須由後端重新計算及驗證。
6. 角色與帳號狀態必須在受保護操作時由後端重新確認。
7. 原有交易一致性、冪等性、唯一限制、歷史快照與稽核紀錄必須保留。
8. 一般員工與主管不得取得固定 IP、原始 GPS、定位精準度、距離或管理稽核資料。
9. 帳號、打卡、加班、訂餐及班表的歷史關聯不得因遷移而中斷。
10. 新後端錯誤時不得留下部分完成資料，也不得把內部錯誤、SQL 或 Secret 回傳前端。
11. 遷移過程不得同時讓兩個後端對同一筆資料進行無協調寫入，避免重複或衝突。
12. API 改版應保留相容層或版本化端點，避免舊前端在快取期間呼叫失敗。

## 7.8 驗收與切換原則

遷移至其他後端平台前，至少完成：

- 所有第五章既有登入、打卡、加班、訂餐、班表與帳號管理驗收。
- 員工、主管、管理員三種角色的允許與拒絕測試。
- 帳號停用、未到職、離職超過有效期間及最後一位管理員保護測試。
- GPS、IP、伺服器時間與 `Asia/Taipei` 日期邊界測試。
- 重複送出、快速連按、網路重送與併發修改測試。
- 任一步驟失敗時的交易回復測試。
- 舊資料筆數、關聯、快照、稽核與報表結果比對。
- 前端不得出現任何高權限 Secret 或資料庫連線資訊。
- 正式切換前完成資料庫備份、DNS／API 位址切換方式及可執行的回復程序。

完成切換後，`規格書.md`、`AGENTS.md`、部署文件、環境變數說明及驗證腳本必須同步更新，不得繼續把已停用的 Supabase Edge Function 當成正式後端來源。

---

# 第八章　共通互動、非功能性與設計交付

## 8.1 資訊架構與使用者流程

### 8.1.1 主要導覽

```text
登入
└─ 登入後首頁
   ├─ 打卡
   │  ├─ 今日打卡
   │  └─ 加班申請
   ├─ 班表
   │  ├─ 人員檢視
   │  ├─ 人員檢視－統計欄
   │  ├─ 班別檢視
   │  └─ 主管／管理員設定與工具
   ├─ 訂餐
   │  ├─ 今日訂餐
   │  ├─ 訂餐統計（主管／管理員）
   │  └─ 訂餐設定（主管／管理員）
   └─ 紀錄
      ├─ 個人記錄
      ├─ 加班審核（管理員）
      └─ 打卡管理（管理員）
```

1. 員工、主管與管理員使用同一個登入入口及首頁。
2. 未授權功能不顯示入口，且後端仍必須拒絕直接呼叫。
3. 每個主要頁面都提供明確返回首頁方式；關閉彈出視窗不得同時離開目前主要頁面。
4. 瀏覽器重新整理後，依有效 Session 回到可安全重建的首頁或目前正式入口，不依賴未保存的記憶體狀態。
5. 使用者流程不得要求先進入無關頁面才能完成主要工作。

### 8.1.2 角色流程

- **員工：** 登入後可直接完成打卡、查看完整班表、今日訂餐及查看個人記錄。
- **主管：** 除員工流程外，可進入班表管理、一般設定、訂餐統計與訂餐設定。
- **管理員：** 除主管流程外，可執行角色管理、單位打卡設定、加班審核與全體打卡管理。

## 8.2 共通頁面狀態

所有主要頁面、分頁、表格與彈出視窗至少處理以下狀態，不得以空白畫面代替：

| 狀態 | 顯示與行為 |
|---|---|
| 初次載入 | 顯示載入文字或骨架；主要操作停用，不顯示錯誤的空資料結論。 |
| 背景重新整理 | 保留已顯示資料並標示更新中；成功後替換，失敗時保留舊資料並提示。 |
| 空資料 | 顯示原因與下一步，例如「目前沒有訂餐記錄」；有新增權限時提供主要操作。 |
| 無權限 | 不顯示受限資料，顯示可理解的權限訊息並提供返回入口。 |
| 讀取失敗 | 顯示錯誤摘要與重試操作；不得把失敗誤顯示成零筆資料。 |
| 儲存中 | 送出按鈕停用並顯示進度，防止快速連按或重複請求。 |
| 儲存成功 | 更新畫面正式資料並顯示短暫成功提示；不得只顯示提示卻保留舊資料。 |
| 儲存失敗 | 保留使用者輸入與目前畫面，顯示可採取的修正或重試方式。 |
| Session 失效 | 取消未送出的受保護操作、清除敏感畫面資料並顯示重新登入。 |
| 資料衝突 | 不靜默覆寫他人較新的資料；提示重新讀取或重新確認。 |
| 網路離線 | 不建立假的成功狀態；可保留尚未送出的輸入，但恢復連線後仍需重新確認。 |

共通規則：

1. 狀態訊息使用文字搭配圖示或樣式，不只靠顏色。
2. 同一操作在不同頁面使用一致的載入、成功、警告及錯誤呈現。
3. 讀取與寫入錯誤不得直接顯示 SQL、堆疊、Token、Secret 或內部服務細節。
4. 超過 500 毫秒仍未完成的互動應提供可見進度；極短操作避免閃爍式載入提示。
5. 同一表單只允許一個正式送出流程，Enter、點擊及觸控不得造成重複提交。

## 8.3 表單、確認與未儲存資料

1. 必填欄位在標籤或欄位附近明確標示；選填欄位不得用錯誤訊息暗示必填。
2. 前端格式驗證用於即時提示，後端必須重新驗證相同業務規則。
3. 驗證失敗時將焦點移到第一個錯誤欄位，並在欄位附近顯示具體原因。
4. 儲存失敗後保留有效輸入，不清空整張表單。
5. 關閉有未儲存修改的彈出視窗、返回首頁或切換頁面時，必須先確認是否放棄修改。
6. 刪除、取消整張訂單、批次審核、清除打卡及其他不可逆或高影響操作必須二次確認。
7. 確認訊息包含「操作對象＋結果」，避免只顯示「確定嗎」。
8. 成功保存後才清除未儲存狀態；後端失敗不得讓前端誤以為已完成。
9. 表單重複送出應使用按鈕鎖定、請求識別或後端冪等性共同防護。

## 8.4 錯誤、重試與衝突處理

1. 可重試錯誤包括暫時性網路中斷、逾時及伺服器暫時不可用；重試不得重複建立資料。
2. 權限不足、資料格式錯誤、已截止及任職失效等不可直接重試錯誤，需顯示原因及可行下一步。
3. 自動重試僅適用安全的讀取或有冪等保護的操作，最多 2 次並採延遲；不得無限重試。
4. 任何寫入結果不明時，先重新讀取正式資料再決定是否重送。
5. 多人同時修改同一筆資料時，後端應使用資料列鎖、版本欄位、更新時間或交易條件避免遺失更新。
6. 前端偵測到資料版本衝突時，提供「重新載入」或「重新確認後送出」，不得直接覆寫。
7. 大量批次操作需回傳整批成功或可明確辨認的逐筆結果；交易型操作不得留下無法判斷的部分完成狀態。

## 8.5 文案與名詞規範

### 8.5.1 固定名詞

| 正式用詞 | 不使用或僅限技術內容 |
|---|---|
| 人員 | 成員、使用者（一般介面） |
| 員工 | 僅指角色或勞務身分時使用 |
| 單位 | 部門、門市（除非資料名稱本身） |
| 班表 | 排班表、班次表 |
| 記錄 | 紀錄；頁面正式名稱仍為「紀錄」時維持既有名稱 |
| 上班打卡／下班打卡 | 簽到／簽退 |
| 儲存 | 保存、確定（作為保存動作） |
| 新增 | 新增單位、新增人員等冗長按鈕文字；標題已說明對象時按鈕只顯示「新增」 |

### 8.5.2 文案原則

1. 按鈕使用明確動詞：新增、儲存、取消、刪除、匯入、匯出、重試。
2. 成功訊息說明完成的對象，例如「班表已儲存」。
3. 錯誤訊息使用「發生什麼＋如何處理」，避免只顯示「錯誤」。
4. 日期統一顯示為 `YYYY-MM-DD` 或畫面既定中文格式；同一表格內不得混用。
5. 時間統一為 24 小時制 `HH:mm`。
6. 金額顯示新臺幣整數時使用千分位；儲存仍使用數值，不保存格式字串。
7. 確認與警告文案不得使用技術欄位名、RPC 名稱或資料表名稱。

## 8.6 裝置與瀏覽器支援

| 平台 | 正式支援範圍 |
|---|---|
| Android Chrome | 最近兩個主要版本 |
| iPhone Safari | 最近兩個 iOS 主要版本 |
| iPad Safari | 最近兩個 iPadOS 主要版本 |
| Windows Chrome | 最近兩個主要版本 |
| Windows Edge | 最近兩個主要版本 |
| 其他 Chromium 瀏覽器 | 基本功能可用，不保證所有裝置特例 |
| 舊版 IE 或內嵌舊 WebView | 不支援 |

1. 手機直向為主要驗收方向；班表及大型表格同時驗收橫向或容器內水平捲動。
2. 瀏覽器縮放 100% 至 200% 時，主要操作不得被永久遮住。
3. 無痕模式、禁用儲存或清除網站資料後，可重新登入；不保證保留 Session。
4. 不以 User-Agent 作為唯一安全判斷；裝置判斷只用於介面、Session 與 GPS 流程分流。
5. 每次正式發布至少抽測 Android Chrome、iPhone Safari 與 Windows Chrome／Edge。

## 8.7 效能、容量與可用性目標

以下為正式驗收目標，不包含使用者網路完全中斷或第三方平台重大事故：

1. 一般讀取 API 在正常公司 Wi-Fi 或穩定 4G／5G 下，95% 應於 3 秒內回應。
2. 一般單筆寫入在正常網路下，95% 應於 3 秒內完成並顯示正式結果。
3. 報表、Excel 匯出與大型班表讀取應於 15 秒內完成；超過 3 秒需顯示持續進度。
4. 系統至少支援 100 名有效人員、8 週班表範圍、5,000 個班表格及 20 名同時在線使用者，不發生瀏覽器無回應。
5. 清單查詢不得無限制載入全部歷史資料；超過 500 筆時應使用日期範圍、分頁、延遲載入或伺服器篩選。
6. 前端 bundle、主要查詢回傳量或首頁請求數增加超過前一正式版本 20% 時，PR 必須說明原因與影響。
7. 靜態資源使用版本參數或內容雜湊避免舊快取；不得要求使用者每次手動清除快取。
8. 正式服務可用性目標為每月 99.5%；已公告維護時間不計入。
9. 關鍵操作錯誤率、逾時率或回應時間明顯惡化時應停止繼續發布並調查。

## 8.8 Wireframe 與設計交付

1. 重大頁面新增或布局重整在實作前，至少提供手機版與桌面版低擬真 Wireframe 或清楚區塊圖。
2. Wireframe 必須標示：頁面標題、返回方式、主要操作、次要操作、表格或清單、載入／空資料／錯誤狀態、彈出視窗與捲動區。
3. 響應式設計需說明桌面轉手機時的欄位順序、換行、隱藏、固定及水平捲動方式。
4. PR 涉及明顯視覺變更時，應附修改前後截圖或可重現的畫面說明。
5. Wireframe 用於確認資訊層級，不取代第六章的元件、尺寸、顏色與無障礙規格。
6. 規格書保存穩定的結構規則；一次性的討論稿或截圖可保留在 PR，不另建立正式補充規格。

## 8.9 無障礙與設計驗收

1. 目標符合 WCAG 2.1 AA 的核心要求：鍵盤可操作、可見焦點、足夠對比、標籤關聯及非純顏色辨識。
2. 觸控主要操作區建議不小於 44×44 CSS px；既有 42px 控制項不得因壓縮布局再縮小。
3. 表單錯誤需能被螢幕閱讀器辨認，並以 `aria-describedby` 或等效方式關聯訊息。
4. 彈出視窗開啟後焦點進入視窗，關閉後回到觸發元件；焦點不得落到背景不可操作區。
5. 動態成功、錯誤與載入狀態使用適當的 live region 或等效方式通知。
6. 圖示按鈕必須有可讀名稱；純裝飾圖示不得被重複朗讀。
7. 發布前至少完成鍵盤操作、200% 縮放、亮度較低手機畫面與一種螢幕閱讀器抽測。

---

# 第九章　API、資料生命週期、維運與測試

## 9.1 API 契約

### 9.1.1 共通原則

1. Edge Function、REST 與 RPC 必須使用 HTTPS。
2. 受保護端點從有效 Token 取得真正身分，不接受前端自行指定角色或操作人。
3. 日期與正式時間判斷使用伺服器 `Asia/Taipei`。
4. Request 欄位必須明確區分必填、選填、型別、允許值及最大長度。
5. 未知欄位不得默默改變權限或正式結果；高風險端點應拒絕不支援欄位。
6. 回應不得包含 Secret、完整 SQL、內部堆疊或未授權欄位。
7. 寫入端點應回傳保存後的正式識別值、版本或必要摘要，前端不得只以本機輸入推測成功結果。
8. 大量查詢需提供日期、篩選或分頁邊界；不得以無上限查詢作為一般畫面資料來源。

### 9.1.2 Edge Function 成功回應

新建或重構的 Edge Function 優先使用：

```json
{
  "ok": true,
  "data": {},
  "requestId": "可選的請求識別值"
}
```

既有端點可在相容期間維持目前資料形狀，但前端 API adapter 必須統一轉成可預期的內部結果，不得讓頁面直接依賴多種錯誤格式。

### 9.1.3 錯誤回應

```json
{
  "ok": false,
  "code": "MEAL_CUTOFF_PASSED",
  "message": "今日訂餐已截止",
  "details": {},
  "requestId": "可供查詢紀錄的識別值"
}
```

1. `code` 為穩定機器代碼，前端可用於選擇操作流程。
2. `message` 為可直接顯示或經前端轉換的繁體中文摘要。
3. `details` 只放安全且有助修正的欄位資訊。
4. `requestId` 用於後端查詢，不包含使用者個資或 Secret。
5. PostgreSQL RPC 可維持型別化列或物件回傳；前端 adapter 需把 RPC 錯誤轉成同一內部錯誤模型。

### 9.1.4 HTTP 狀態碼

| 狀態碼 | 用途 |
|---:|---|
| 200／201 | 查詢或寫入成功 |
| 400 | 格式、欄位或業務輸入錯誤 |
| 401 | 未登入、Token 無效或 Session 失效 |
| 403 | 已登入但角色或資料權限不足 |
| 404 | 正式資源不存在或不可見 |
| 409 | 重複、版本衝突、截止狀態或資料狀態不允許 |
| 422 | 欄位格式正確但不符合業務驗證 |
| 429 | 請求過於頻繁 |
| 500 | 未預期的伺服器錯誤 |
| 503 | 暫時無法提供服務，可稍後重試 |

## 9.2 共通錯誤碼

| 代碼 | 意義 | 前端處理 |
|---|---|---|
| `AUTH_REQUIRED` | 尚未登入 | 清除敏感畫面並開啟登入 |
| `SESSION_EXPIRED` | Session 已失效 | 提示重新登入，不自動重送寫入 |
| `ACCOUNT_INACTIVE` | 帳號停用或任職期間無效 | 登出並顯示聯絡管理者 |
| `FORBIDDEN` | 角色或資料權限不足 | 顯示無權限並返回安全入口 |
| `VALIDATION_FAILED` | 欄位驗證失敗 | 標示欄位並保留輸入 |
| `RESOURCE_NOT_FOUND` | 資料不存在 | 重新整理清單或返回上一層 |
| `CONFLICT` | 資料版本或狀態衝突 | 重新讀取並要求使用者確認 |
| `DUPLICATE_REQUEST` | 重複請求 | 重新讀取正式結果，不重複建立 |
| `RATE_LIMITED` | 操作過於頻繁 | 暫停按鈕並稍後重試 |
| `NETWORK_TIMEOUT` | 網路或後端逾時 | 保留輸入並提供重試 |
| `ATTENDANCE_LOCATION_REJECTED` | 打卡位置與網路均不符 | 顯示一般位置不符訊息，不暴露完整驗證細節 |
| `MEAL_CUTOFF_PASSED` | 訂餐已截止 | 停用新增、修改與取消 |
| `OVERTIME_DEADLINE_PASSED` | 加班申請期限已過 | 停用送出並顯示期限 |
| `INTERNAL_ERROR` | 未預期錯誤 | 顯示 requestId 並提供重試或聯絡方式 |

新增錯誤碼時必須更新本表、相關 API 驗收及前端對應；不得以比對中文錯誤全文作為唯一流程判斷。

## 9.3 版本與相容性

1. 靜態前端可能因瀏覽器快取與後端短暫存在不同版本，API 異動必須預留相容期。
2. 資料庫及 API 採「擴充－遷移－收縮」：先新增相容欄位或端點，再切換呼叫端，確認後才移除舊物件。
3. 破壞性 API 改版使用新端點名稱、路徑版本或明確版本欄位，不在原端點靜默改變必要欄位意義。
4. 刪除 RPC、Edge Function、欄位或資料表前，必須搜尋前端、部署腳本、文件與驗證是否仍有引用。
5. 舊前端相容期至少涵蓋一次正式靜態資源快取更新；切換期間新舊端點不得對同一請求產生不同資料規則。
6. 版本切換順序原則：新增後端能力 → 驗證 → 發布新前端 → 觀察 → 停用舊呼叫 → 移除舊後端。
7. 回滾時不得要求先還原不可逆資料庫刪除；高風險 migration 必須先有可回復方案。

## 9.4 資料分類

| 等級 | 例子 | 讀取原則 |
|---|---|---|
| 公開部署資料 | Supabase URL、anon key、靜態資源版本 | 可存在前端，但不得具管理權限 |
| 公司內部共同資料 | 姓名、單位、班表、班別、公司打卡地點設定 | 依功能提供給有效登入者 |
| 管理資料 | 工號、角色、休假規則、帳號管理欄位 | 主管或管理員依權限取得 |
| 個人與稽核資料 | 個人打卡、原始 GPS、來源 IP、加班申請、異動快照 | 本人摘要或管理員依職責取得 |
| Secret | service role、資料庫密碼、部署 Token | 僅伺服器或 CI Secret，不得進入前端與日誌 |

1. 資料是否容易從其他途徑得知，不等於所有頁面都必須載入；以功能用途與最少完整資料為原則。
2. 權限保護重點是限制未授權讀取、修改與批次取得，不把一般公司設定誤當成密碼。
3. 日誌、錯誤訊息、匯出檔與瀏覽器快取都必須遵守相同資料分類。

## 9.5 保存、刪除與匿名化

1. 現行系統不自動刪除班表、打卡、加班、訂餐及稽核歷史；正式保留年限由公司政策及適用法令決定。
2. 人員離職或帳號刪除不得破壞既有班表、打卡、加班、訂餐與稽核關聯；使用姓名、工號及單位快照保留歷史可讀性。
3. 軟刪除資料必須在一般查詢中排除，但仍依管理與稽核權限保存。
4. 真正清除或匿名化資料必須是獨立管理流程，包含影響預覽、二次確認、操作者、時間、理由及執行結果。
5. 匯出檔由使用者裝置保存，系統應提醒妥善處理；正式網站不得無期限保存臨時匯出檔。
6. 測試資料與正式資料分離；不得使用可識別的正式 GPS、IP、姓名或打卡快照建立公開測試資料。
7. 公司確認保留年限後，需更新 9.11 並建立可驗證的封存、匿名化或清除程序。

## 9.6 備份與災難復原

### 9.6.1 最低目標

- **RPO：** 最多可接受遺失 24 小時內資料。
- **RTO：** 重大故障後 8 小時內恢復核心登入、班表、打卡及訂餐功能。
- 正式資料庫至少每日備份一次；方案不具自動備份時，需建立等效匯出或排程。

### 9.6.2 執行規則

1. 高風險 SQL、批次清理、主鍵／外鍵調整及大規模資料轉換前，必須完成可還原備份。
2. 備份必須涵蓋資料庫結構、正式資料、必要 Auth 對應與部署設定；Secret 以安全系統另行保存。
3. 至少每六個月執行一次還原演練，確認備份可讀、關聯完整及核心驗收可通過。
4. 還原後需檢查人員數、班表筆數、打卡唯一性、訂餐金額、稽核歷程與角色權限。
5. 備份檔不得放入公開 GitHub 儲存庫或一般前端發布目錄。

## 9.7 發布與回滾

1. 正式發布前必須完成程式檢查、資料庫相容性確認、角色權限測試及關鍵冒煙測試。
2. 前端回滾以 Git commit／已知良好 Pages artifact 為準，不手動修改 `docs/` 補救。
3. Edge Function 回滾重新部署上一個已知良好版本，並確認其仍相容目前資料庫。
4. 資料庫 migration 優先使用向前修正；不得在不確認資料影響下直接執行破壞性逆向 SQL。
5. 新舊前端切換期間，後端需同時支援必要相容介面；確認新前端普遍生效後才移除舊端點。
6. 發布後至少驗證登入、首頁、打卡今日狀態、班表顯示、今日訂餐與各角色主要入口。
7. 發現權限外洩、資料毀損、無法打卡或無法登入時，立即停止後續發布並依影響選擇回滾或向前修復。
8. 每次高風險發布在 PR 或發布紀錄中寫明：變更內容、資料庫順序、驗證結果、回滾方法及負責人。
9. GitHub Actions 使用單一『驗證後部署』流程：Pull Request 只建置與驗證；推送至 `main` 時，只有同一 workflow 的完整驗證成功後才能部署 GitHub Pages。
10. 不得另建同時監聽 `main` 且重複執行相同 V2 檢查或 Pages 部署的 workflow；新增檢查應整合至共用 `ci:check` 或單一流程的 `validate` 工作。

## 9.8 監控與事件處理

1. Edge Function 與正式後端錯誤應記錄時間、端點、狀態碼、requestId、角色及安全的錯誤摘要。
2. 日誌不得記錄密碼、Token、service role、完整 GPS、完整 IP 或不必要的個人輸入；需要稽核的敏感資料保存於受控資料表。
3. 至少監控登入失敗率、API 5xx、逾時、打卡失敗率、訂餐失敗率及資料庫容量異常。
4. 同一錯誤短時間大量發生時應可由 requestId、端點與版本定位。
5. 事件分級：
   - **P1：** 無法登入、核心資料錯誤、未授權資料外洩或大範圍無法打卡。
   - **P2：** 主要功能部分失效、報表錯誤或特定裝置大範圍失效。
   - **P3：** 非阻斷介面、文案或單一低影響問題。
6. P1 事件優先恢復服務與阻止擴大，再保存證據、分析原因及補上防回歸測試。
7. 修復完成後記錄影響範圍、根因、時間線、修復與預防措施；不把事故細節散落成另一份正式規格。

## 9.9 需求編號與追蹤矩陣

### 9.9.1 編號規則

| 前綴 | 範圍 |
|---|---|
| `AUTH` | 登入、Session、帳號與角色 |
| `ATT` | 打卡與打卡管理 |
| `OT` | 打卡加班申請與審核 |
| `MEAL` | 訂餐、統計與設定 |
| `SCH` | 班表、設定與自動排班 |
| `REC` | 個人記錄與報表 |
| `UI` | 共用介面、響應式與無障礙 |
| `API` | API 契約、錯誤與相容性 |
| `DATA` | 資料模型、保存、備份與稽核 |
| `OPS` | 發布、監控、回滾與事件處理 |

新建或重大修改的關鍵需求使用穩定編號，例如 `ATT-001`、`MEAL-004`。編號一旦發布不得改作其他意義；廢止時標記已淘汰，不重複使用。

### 9.9.2 初始追蹤矩陣

| 需求編號 | 正式要求 | 主要實作 | 驗收 |
|---|---|---|---|
| `AUTH-001` | 未登入不得載入受保護資料 | Auth、RLS、前端入口 | 未登入資料保護測試 |
| `AUTH-002` | 任職有效期間由後端判定 | `is_effective_user`、Edge Functions | 未到職／離職邊界測試 |
| `ATT-001` | 第一次成功打卡時間不得被覆寫 | `attendance-clock`、打卡 RPC | 重複請求與快速連按 |
| `ATT-002` | 無上班卡仍可下班打卡 | 打卡 RPC | 無上班卡下班測試 |
| `OT-001` | 加班以 0.5 小時為單位且有五日申請期限 | 加班 Edge Functions | 29／30 分鐘與期限邊界 |
| `MEAL-001` | 訂餐數量只接受 0 或正整數 | 前端驗證、`meal-order` | 鍵盤、貼上與後端驗證 |
| `MEAL-002` | 截止後不得新增、修改或取消 | `meal-order` | 截止前後測試 |
| `SCH-001` | 所有角色看到相同完整班表 | 共同名錄、班表查詢 | 三角色結果比對 |
| `SCH-002` | 每人每日最多一筆正式班表格 | 唯一限制、批次保存 RPC | 重複保存測試 |
| `UI-001` | 五個主要頁面手機安全間距一致 | 共用 CSS | 640px 以下視覺驗收 |
| `API-001` | 錯誤使用穩定代碼且不暴露內部資訊 | Edge Functions、API adapter | 錯誤回應測試 |
| `DATA-001` | 帳號刪除不得破壞歷史關聯 | 快照、外鍵、刪除 RPC | 刪除後歷史查詢 |
| `OPS-001` | 高風險發布具有可執行回滾方案 | Git、部署腳本、備份 | 發布檢查表 |

後續新增功能時，PR 至少指出受影響需求編號、程式模組、資料物件與測試案例。

## 9.10 測試案例與發布門檻

### 9.10.1 測試案例格式

```text
測試編號：TC-MEAL-001
對應需求：MEAL-001
角色／裝置：員工／Android Chrome
前置條件：已登入、今日有上班卡、尚未截止
操作步驟：在數量欄輸入或貼上小數
預期畫面：拒絕無效內容並保留最後有效整數
預期後端：不建立無效訂單，資料庫不變
```

每個關鍵測試至少包含：

1. 測試編號與對應需求。
2. 角色、裝置及瀏覽器。
3. 前置資料與時間條件。
4. 操作步驟與輸入。
5. 預期畫面、API 結果及資料庫結果。
6. 清理或恢復方式。

### 9.10.2 測試層級

- **靜態檢查：** 語法、bundle、文件及來源／發布對齊。
- **單元或純函式測試：** 日期、格式、時數、金額、距離與資料轉換。
- **API／資料庫整合測試：** 權限、交易、冪等、唯一限制及錯誤碼。
- **角色驗收：** 員工、主管、管理員的允許及拒絕流程。
- **裝置驗收：** Android、iPhone／iPad、Windows Chrome／Edge。
- **回歸測試：** 登入、打卡、班表、訂餐與個人記錄核心流程。
- **發布後冒煙測試：** 正式環境使用低風險帳號確認主要讀取與入口。

### 9.10.3 發布門檻

1. 必要自動檢查全部通過，不得以刪除安全或驗收檢查換取成功。
2. 資料庫異動已在相容順序下套用，並確認新舊前端過渡期。
3. 權限異動至少測試員工、主管、管理員與未登入。
4. 前端異動完成 `web:publish`，`src/renderer/` 與 `docs/` 一致。
5. 高風險操作完成交易失敗、重複送出及回滾驗證。
6. 視覺異動完成手機、桌面、鍵盤及主要瀏覽器抽測。
7. 已知未解問題有明確影響與接受人，不得隱藏於成功結果中。

## 9.11 尚待決定事項

下列事項需由公司管理者或專案負責人確認；確認後直接更新本章，不另建補充規格：

| 項目 | 現行暫定規則 | 需確認內容 |
|---|---|---|
| 法定及營運保存年限 | 不自動刪除正式歷史 | 打卡、加班、訂餐、稽核各自保存年限 |
| 備份能力 | 目標每日備份、RPO 24 小時、RTO 8 小時 | Supabase 方案、自動備份與外部備份方式 |
| 效能基準 | 依 8.7 目標 | 正式環境量測基準與告警門檻 |
| 瀏覽器版本 | 最近兩個主要版本 | 是否需支援特定舊手機或企業 WebView |
| 事件通知 | 由維護者人工發現與處理 | 是否建立自動告警、通知管道及值班人員 |
| 個資匿名化 | 僅在正式流程執行 | 離職後何時匿名化及哪些快照必須保留 |

尚待決定不代表可以忽略安全與資料一致性；在決定前採取本規格中較保守、可回復且不自動刪除的做法。

## 班表與設定頁寬度、設定刪除穩定性

- 電腦版班表頁的外層卡片不得撐寬瀏覽器頁面；八週班表超出可視區時，只能由班表內部的同步水平捲動區處理。
- 電腦版人員設定、班別設定、假別設定及加班設定列表，欄位必須依彈出視窗可用寬度分配，不得設定大於彈出視窗的固定最小寬度。
- 設定列表的拖曳欄與操作欄使用共用 CSS 變數；同一頁不得再加入第二套覆蓋欄寬。
- 班別、假別、加班刪除 API 的請求格式固定為 `{ action: "delete", category, itemId }`，前端只有在後端刪除成功後才能移除本機資料。
- 刪除失敗時保留目前設定清單與資料，不得先關閉視窗或先移除畫面資料。
