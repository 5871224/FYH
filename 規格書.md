# 規格書

**專案名稱：** 福圓號排班系統（FYH）  
**文件版本：** 2026-08-08  
**文件狀態：** 唯一正式規格  
**主要裝置：** 手機優先，支援電腦與平板  
**正式時區：** `Asia/Taipei`  
**適用程式版本：** GitHub `main` 分支  
**文件管理規則：** 後續需求、介面與技術規格直接更新本檔，不另建規格補充檔。

> 本系統尚未正式上線，只維護單一正式版本。本文件及兩個正式 SQL 定義唯一正式資料結構；舊資料表、舊欄位、舊端點、舊 payload、雙軌讀寫與補丁式覆寫均不得保留，且不進行資料遷移。

---

# 一、系統範圍與導覽

## 1.1 系統目的

系統提供：

1. 人員登入、自訂角色、功能權限及適用群組。
2. 多群組八週班表查看、編輯、設定、匯出與封存。
3. 每日上下班打卡、上班時數、加班時數、備註與簽到審核。
4. 依群組開關的今日訂餐、訂餐統計與商品設定。
5. 個人歷史、管理稽核及正式資料匯出。

不包含跨日夜班、一天多班、一天多次上下班、二級簽到審核或未來日期訂餐。

## 1.2 主要導覽

```text
登入
└─ 首頁
   ├─ 簽到簿
   │  ├─ 個人記錄
   │  └─ 簽到審核
   ├─ 班表
   │  ├─ 群組設定
   │  ├─ 權限設定
   │  └─ 班表封存
   └─ 訂餐
      ├─ 今日訂餐
      ├─ 訂餐統計
      └─ 訂餐設定
```

首頁不顯示獨立「打卡」入口；打卡整合至簽到簿今日資料列。原「加班申請」、「加班審核」及「打卡管理」不再作為獨立頁面或資料流程。

---

# 二、群組、角色與權限

## 2.1 群組定位

群組是班表、單位、人員、簽到與訂餐資料的最上層範圍，例如「門市」、「工廠」。

1. 每個單位只能屬於一個群組。
2. 每位人員有一個所屬群組。
3. 班別綁定單位，群組由所屬單位決定；系統同步保存班別群組。
4. 假別為公司共用規則，所有群組共用。
5. 加班設定為公司共用規則，目前功能隱藏，權限併入假別設定。
6. 每筆班表直接保存群組 ID，不只依單位或人員即時計算。
7. 打卡、簽到與訂餐保存群組 ID 及必要名稱快照。

系統升級時建立預設群組：

```text
群組代碼：STORE
群組名稱：門市
可否訂餐：是
狀態：啟用
```

既有單位、人員、班別、班表、簽到與訂餐資料全部歸入「門市」。

## 2.2 人員所屬群組

人員新增及編輯頁只增加「所屬群組」下拉選單，不增加常駐摘要、說明或提醒。

1. 所屬群組只影響可選的所屬單位及排班班別，與權限無關。
2. 選擇群組後，所屬單位只顯示該群組單位。
3. 排班班別只顯示該群組且符合所屬單位的班別。
4. 變更所屬群組時，清空原所屬單位及排班班別，必須重新選擇。
5. 若人員仍有未封存班表，不允許變更群組；已封存班表不構成限制。
6. 人員權限只由「權限」下拉選單所選角色決定。

## 2.3 角色管理

系統先建立三個角色：

- 管理員。
- 主管。
- 員工。

可新增自訂角色。角色列表欄位為：

| 欄位 | 規則 |
|---|---|
| 角色名稱 | 不可空白或重複 |
| 適用群組 | 顯示已勾選群組，不提供「全部群組」選項 |
| 權限項目 | 主要寬欄；列出已勾選權限，每個權限使用獨立膠囊標籤顯示，空間不足時自動換行 |
| 操作 | 編輯、刪除 |

角色不設狀態。若角色仍有人員使用，刪除前必須先更換角色。系統至少保留一個具有「權限設定」權限的角色及至少一名可使用該角色的人員。

新增群組成功後，自動把新群組加入執行新增者角色的適用群組；其他角色不自動取得權限。

## 2.4 權限項目

角色的功能權限如下：

| 功能 | 權限規則 |
|---|---|
| 班表 | 同一行顯示「查看」及「管理」 |
| 群組設定 | 可進入後即可執行該頁全部功能 |
| 單位設定 | 可進入後即可執行該頁全部功能 |
| 人員設定 | 可進入後即可執行該頁全部功能 |
| 假別設定 | 包含目前隱藏的加班設定能力，畫面不另列加班設定 |
| 權限設定 | 可管理角色、適用群組與權限 |
| 簽到審核 | 可查詢、編輯、審核、退回及匯出適用群組資料 |
| 訂餐管理 | 可查看訂餐統計及訂餐設定頁 |

不列入權限設定：

- 個人記錄：所有登入者都可查看自己的個人記錄。
- 打卡管理：頁面已移除。
- 加班設定：目前隱藏，不單獨顯示。

班表權限規則：

1. 勾選「管理」時自動勾選「查看」。
2. 取消「查看」時自動取消「管理」。
3. 只有查看權限時，只能查看及切換檢視，不能修改、清空、複製貼上、批次操作、復原、重做或匯出。
4. 管理權限包含班別設定、班表修改、鍵盤複製貼上、批次操作、上一步、下一步及班表匯出。

## 2.5 適用群組

適用群組決定角色的功能可作用在哪些群組，與人員自己的所屬群組無關。

受適用群組限制的功能：

- 班表查看與管理。
- 群組設定。
- 單位設定。
- 人員設定。
- 簽到審核。
- 訂餐管理。

公司共用設定不受群組範圍限制：

- 假別設定。
- 權限設定。

前端隱藏不是安全邊界；REST、RPC 及 Edge Function 都必須再次驗證權限及適用群組。

## 2.6 共用排班人員目錄

1. 所有有效登入者都可取得角色適用群組內人員的工號、姓名、所屬單位、任職日期、計薪方式、排序及排班班別 ID。
2. 工號與排班班別屬班表共同顯示及排班所需資料，不另外要求「人員設定」權限。
3. 開放欄位不等於開放全公司資料；人員目錄仍由後端依角色適用群組限制資料列。
4. 頁面入口與修改能力仍依功能權限控制；沒有「人員設定」權限者不得新增、編輯、刪除人員或變更排班班別。
5. 不得只靠前端隱藏頁面限制資料範圍，RPC、RLS 及 Edge Function 仍須執行群組與操作權限檢查。

---

# 三、登入、首頁與帳號

## 3.1 未登入

1. 登入介面使用全畫面。
2. 未登入不顯示班表、員工、打卡、加班與訂餐資料，也不載入簽到或權限資料。
3. 敏感資料不得先載入再以 CSS 隱藏。
4. Token 無效或 Session 逾時時，清除敏感畫面並重新登入。

## 3.2 首頁

首頁顯示使用者姓名、角色、簽到簿、班表、修改密碼及登出。

訂餐按鈕規則：

1. 人員所屬群組已啟用且勾選「可否訂餐」時顯示。
2. 群組未勾選、已停用或人員未設定群組時不顯示。
3. 隱藏按鈕之外，後端新增訂餐仍須驗證群組訂餐開關。
4. 關閉訂餐不刪除既有訂餐資料；訂餐管理者仍可查閱歷史。

## 3.3 任職與刪除

帳號有效期間為到職日 00:00 至離職日後第 5 日 23:59，以伺服器 `Asia/Taipei` 判定。

1. 人員不以 `is_active` 判斷任職。
2. 超過離職寬限期後不得使用受保護功能。
3. 人員介面的「刪除」採邏輯刪除，資料庫保存 `deleted_at`。
4. 人員仍有未封存班表時不可刪除。
5. 人員沒有未封存班表，或全部班表均已封存時，可以刪除。
6. 刪除後不出現在人員頁、排班名錄及登入流程，但簽到、訂餐、稽核及封存快照仍保留。
7. 系統永遠保留至少一位具有最高管理能力的有效人員。

## 3.4 Session

1. 手機使用可跨關閉保存的登入狀態，48 小時滑動閒置期限。
2. 電腦、平板及觸控筆電使用分頁階段儲存，30 分鐘滑動閒置期限。
3. 裝置無法判斷時採電腦規則。
4. 閒置逾時後第一個受保護操作不得成功。

---

# 四、簽到簿

## 4.1 頁籤

簽到簿使用 Chrome 式頁籤：

1. 個人記錄：每位登入者查看自己的班表、打卡、時數、備註、異常、訂餐與審核狀態。
2. 簽到審核：具有「簽到審核」權限者管理角色適用群組內資料。

## 4.2 個人記錄

1. 預設最近 50 日，每頁 50 筆，日期倒序。
2. 日期條件變更立即查詢。
3. 欄位包含日期、班表圖示、班別、上下班時間與地點、上班時數、加班時數、異常、備註、訂餐及審核狀態。
4. 只有今日未審資料可修改上班時數、加班時數及備註。
5. 打卡、輸入或重繪期間必須保留視窗垂直位置及表格水平／垂直捲動位置。
6. 一般使用者只看地點摘要，不取得原始 GPS、IP、定位精準度、距離或完整 JSON。

## 4.3 今日打卡

1. 只有今日列顯示上班及下班打卡。
2. 每人每天最多一筆上班時間與一筆下班時間。
3. 沒有上班卡仍允許下班打卡。
4. 第一次成功時間為正式時間，重送不得覆寫。
5. 已審核資料不可再由員工修改。
6. 手機優先 GPS，距公司不超過 300 公尺且精準度不超過 300 公尺時通過；失敗時改驗證固定對外 IP。
7. 電腦及平板只驗證固定對外 IP。

## 4.4 簽到審核

篩選列在結束日期後增加「群組」下拉選單：

```text
開始日期　結束日期　群組　人員　異常　狀態
```

1. 群組選項只顯示角色適用群組。
2. 「全部群組」只代表該角色適用群組的全集。
3. 原班別欄改為「群組－單位」，例如「門市－大安」、「工廠－生產一課」。
4. 查詢、編輯、審核、退回、歷程與匯出都依角色權限及適用群組驗證。
5. 管理異動寫入 `attendance_audit_logs`，保存操作人、原因、修改前及修改後快照。
6. `attendance-review-groups` 提供群組化簽到審核查詢與寫入橋接，不得只依前端篩選。
7. 日期、人員、異常與狀態等篩選欄位整齊排列，功能按鈕另列靠右排列。
8. 操作欄固定完整顯示編輯、審核／退回與歷程，三個 SVG 圖示固定同一列顯示。
9. 簽到審核匯出的正式唯一格式為 12 欄；加班時數採四碼 `HHMM`，例如 2.5 小時輸出 `0230`。

## 4.5 出勤資料模型

正式出勤只使用：

- `attendance_days`：每人每日一列，另保存群組 ID、群組名稱快照及單位名稱快照。
- `attendance_audit_logs`：管理異動完整稽核。

不建立舊打卡表、舊加班申請表或舊審核紀錄表。

---

# 五、訂餐

## 5.1 今日訂餐資格

1. 人員所屬群組勾選「可否訂餐」。
2. 今日已有上班打卡及明確打卡單位。
3. 尚未超過伺服器每日截止時間。
4. 不要求今日有班表。
5. 只處理今天，不處理未來日期。

## 5.2 商品與訂單

1. 數量只接受 0 或正整數。
2. 第一次全部為 0 不建立訂單。
3. 已有訂單全部改為 0 時需確認後取消整張訂單。
4. 同一人、日期、商品唯一；同日多商品共用 `order_id`。
5. 保存商品名稱、單價、人員、單位及群組快照。
6. 停用品項對已訂者仍顯示，只能減少或取消。
7. 截止後停止新增、修改及取消。

## 5.3 訂餐管理

具有「訂餐管理」權限者只能查看及操作角色適用群組內的訂餐統計與設定資料。本人所屬群組未開放訂餐，不影響其管理其他已授權群組歷史資料。

---

# 六、班表、群組與設定

## 6.1 班表頁群組選單

在預設為「人員檢視」的下拉選單左側增加群組下拉選單：

```text
群組：[門市 ▼]　檢視：[人員檢視 ▼]
```

切換群組時：

1. 重新載入該群組的單位、人員、班別及班表。
2. 原單位篩選恢復預設值。
3. 清除目前班表選取範圍。
4. 復原、重做及剪貼簿狀態與其他群組隔離。
5. 記住使用者最後查看的群組。
6. 群組下拉選單只顯示角色適用且啟用中的群組。

## 6.2 班表資料與操作

1. 預設顯示今天所在八週週期。
2. 檢視包含人員檢視、人員檢視－統計欄及班別檢視。
3. 每日格可同時保存班別、假別及班表加班，順序為班別、假別、加班。
4. 唯一限制為 `member_id + work_date`；空白格不保留資料列。
5. 每筆班表保存群組 ID，且必須與人員、支援單位及班別群組一致。
6. 現有複製貼上只使用鍵盤快速鍵，不新增「複製上週」或獨立複製按鈕。
7. 清空排班屬一般編輯，可由上一步復原，不顯示額外刪除確認。
8. 上一步、下一步最多保存最近 20 次操作，且不得跨群組作用。
9. 從一個群組複製的班表內容不得直接貼入不相符群組；後端仍需驗證。
10. 自動排班及批次操作只作用於目前群組。

## 6.3 功能選單

班表頁「功能」選單增加：

- 群組設定。
- 權限設定。
- 班表封存。

其他現有功能與排列保持原規格。

## 6.4 群組設定頁

版面與單位設定表格一致。欄位依序為：

| 欄位 | 規則 |
|---|---|
| 拖曳排序 | 每列最前方，只有把手可拖曳 |
| 群組代碼 | 不可重複，使用英文字母、數字或底線；欄寬採緊湊顯示 |
| 群組名稱 | 不可空白；欄寬採緊湊顯示 |
| 單位 | 單位欄為主要寬欄；每個單位使用獨立膠囊標籤顯示，空間不足時自動換行 |
| 可否訂餐 | 勾選後該群組人員才看到首頁訂餐按鈕；欄寬採緊湊顯示 |
| 狀態 | 啟用或停用；欄寬採緊湊顯示 |
| 操作 | 編輯、停用／啟用、刪除，按鈕維持同列 |

群組設定彈窗應優先提供單位欄可用寬度，群組代碼、群組名稱、可否訂餐、狀態與操作欄只保留足夠內容的固定寬度。單位膠囊標籤使用細框、圓角及單行文字，標籤之間保留間距並可多列排列。

不提供複製功能。

停用群組：

1. 一般班表群組選單不顯示。
2. 不可新增排班或將新單位、人員指定至該群組。
3. 歷史與封存資料保留，可由管理頁重新啟用。

刪除群組：

1. 不限制是否已有相關資料，但必須列出影響筆數並要求輸入群組名稱確認。
2. 未封存班表及現行群組關聯資料一併清除或邏輯刪除。
3. 人員主檔保留，清除其群組、單位及排班班別關聯。
4. 已封存班表、簽到、稽核及必要歷史快照保留。
5. 群組使用邏輯刪除；角色與已刪除群組的歷史關聯保留，供封存班表權限判斷。
6. 整個操作使用資料庫交易，任一步失敗全部復原。

## 6.5 單位與班別

1. 單位只能屬於一個群組。
2. 有人員、未封存班表或現行班別關聯時，不允許直接跨群組移動。
3. 單位與班別刪除一律採 `deleted_at` 邏輯刪除，不實體刪除主檔。
4. 單位或班別仍有未封存班表時不可刪除；只有封存班表歷史時才允許刪除。
5. 班別綁定單位，群組由單位自動同步。
6. 已刪除單位、班別不再出現在設定頁及新增選單；解除舊班表封存後若仍被歷史排班引用，必須可讀取並顯示「（已刪除）」。
7. 已刪除單位或班別不得重新指定給新資料；原有歷史引用未變更時可保留。
8. 班別、單位或群組改名只影響未封存及之後資料。

## 6.6 假別與加班設定

1. 假別為全公司共用，不增加群組欄位。
2. 加班設定為全公司共用，目前功能保持隱藏。
3. 加班設定權限併入假別設定，權限頁不另列。
4. 假別與加班設定刪除一律採 `deleted_at` 邏輯刪除。
5. 仍有未封存班表引用時不可刪除；只有封存班表歷史時才允許刪除。
6. 已刪除項目不出現在設定頁及新增選單；解除舊班表封存後若被歷史資料引用，仍可讀取並顯示「（已刪除）」，但不得重新選用。

---

# 七、班表封存

## 7.1 封存範圍

封存以「群組＋開始日期＋結束日期」為單位。

1. 封存前顯示群組、日期範圍、人員數及資料筆數。
2. 同一群組的封存日期範圍不可重疊。
3. 封存後不可新增、修改、清空、貼上、批次操作、復原或重做。
4. 班表畫面以唯讀狀態顯示封存範圍，不重複跳出提示。
5. 封存資料可查看及匯出。
6. 具有班表管理權限且角色適用該群組者，可從封存清單執行「解除封存」。
7. 解除封存後，該日期範圍恢復一般班表，可修改後再次封存。
8. 解除封存不另留解除紀錄；解除時移除該次封存鎖定與快照。
9. 群組已刪除時不可解除封存。

## 7.2 快照資料

封存不能只在原班表增加 `is_archived`，必須建立獨立快照：

- `schedule_archives`：群組代碼與名稱快照、日期範圍、封存人、封存時間及筆數。
- `schedule_archive_entries`：人員工號與姓名、單位、班別、上下班時間、顏色、假別、加班、日期、排序及班表內容快照。

封存畫面只讀快照，不重新查詢現行名稱。因此人員、單位、班別或群組之後改名、調動、停用或刪除，都不影響封存班表。

## 7.3 封存與刪除

1. 人員、單位、班別、假別及加班設定仍有未封存班表時不可刪除。
2. 所有相關班表均已封存後，可將現行主檔設為 `deleted_at`；不得以實體 `DELETE` 清除主檔。
3. 封存明細保留原始 ID，同時保存完整顯示快照。
4. 被刪除的人員、單位、班別、假別或加班設定不再出現在現行設定及新增選單，但封存班表仍使用快照正常顯示。
5. 已刪除群組的封存班表仍依角色保存的歷史適用群組關聯判斷查看權限。
6. 解除封存後，若舊班表引用已刪除主檔，畫面仍保留原排班並標示「（已刪除）」。
7. 已刪除人員的舊排班只能清除，不得新增、換班或重新指定；已刪除單位、班別、假別及加班設定不得重新選用。
8. 修改完成後可再次封存；解除封存本身不建立另外的歷程紀錄。

---

# 八、介面共用規格

## 8.1 手機優先

1. 寬度不超過 640px 時，上方與左右安全間距為 8px。
2. 內容由畫面上方開始，不垂直置中留下大面積空白。
3. 最外層卡片移除背景、邊框、圓角、陰影及額外內距；內部功能卡片保留。
4. 641px 至 960px 的窄螢幕，上方外距為 12px。
5. 表格過寬只在表格容器內水平捲動，不造成整頁水平捲動。
6. 主要觸控區建議不小於 44×44 CSS px。

## 8.2 共用元件

1. 一般控制項高度 40px，手機至少 42px。
2. 一般控制項圓角 12px，表格 14px，內容區 18px，主卡片及彈窗 24px。
3. 輸入框、日期、時間及下拉選單同列時等高。
4. 圖示按鈕提供 `title` 或 `aria-label`。
5. 狀態不得只靠顏色表示。
6. Chrome 式頁籤固定單行，不足寬度時只讓頁籤列水平捲動。

## 8.3 班表既有樣式

1. 頂端單位下拉選單右側保留上一步、下一步圖示。
2. 浮動工具列收合按鈕加大並醒目。
3. 浮動班表加班區塊預設隱藏，功能保留。
4. 日期欄週六日文字與平日相同使用黑色。
5. 前一週、後一週不得跨出目前八週範圍。
6. 例假同日有排班時只將例假小區塊改黃色，不改整格。

## 8.4 共通狀態

所有頁面處理載入、背景更新、空資料、無權限、讀取失敗、儲存中、儲存成功、儲存失敗、Session 失效、資料衝突及網路離線。

1. 讀取失敗不得顯示為空資料。
2. 儲存失敗保留輸入與目前畫面。
3. 寫入結果不明時先重讀正式資料，再決定是否重送。

---

# 九、後端、安全與資料規則

## 9.1 架構分工

- 前端：畫面、輸入、一般格式提示及 API 呼叫。
- Edge Function：驗證 Token、角色、適用群組、有效期間、伺服器時間、位置及業務規則。
- PostgreSQL／RLS／RPC：正式資料、群組隔離、限制、交易、冪等性、封存及稽核。

前端不得保存 `service_role`、資料庫密碼或其他高權限 Secret。

## 9.2 群組資料安全

1. `set_departments`、`set_employee`、`set_shift` 及 `schedule_entries` 的 RLS 依角色適用群組限制。
2. 班表寫入 Trigger 強制人員、班別及支援單位群組一致。
3. 班表封存 Trigger 同時檢查更新前及更新後日期與群組，禁止把封存資料移出封存範圍。
4. 訂餐管理只能讀取角色適用群組訂單；一般使用者只能讀取本人訂單。
5. 安全限制不得只實作在前端。

## 9.3 頁面與資料權限矩陣

頁面與資料權限矩陣如下；畫面入口、API、RPC、RLS 與 Edge Function 必須套用相同規則：

| 頁面／資料 | 一般登入者 | 具功能權限角色 | 群組範圍 |
|---|---|---|---|
| 首頁與個人記錄 | 查看本人資料 | 同一般登入者 | 本人資料 |
| 共用排班人員目錄 | 查看工號、姓名、單位、任職資料、計薪方式及排班班別 | 同一般登入者；人員修改另依 `member_settings` | 角色適用群組 |
| 班表 | 依 `schedule_view` 查看 | 依 `schedule_manage` 編輯及匯出 | 角色適用群組 |
| 群組、單位、人員設定 | 無權限不顯示且 API 拒絕 | 依對應設定權限管理 | 角色適用群組 |
| 假別與權限設定 | 無權限不顯示且 API 拒絕 | 依對應設定權限管理 | 公司共用 |
| 簽到審核 | 只能看本人個人記錄 | 依 `attendance_review` 查詢、編輯、審核、退回及匯出 | 角色適用群組 |
| 今日訂餐 | 依本人群組訂餐開關及今日打卡資格 | 同一般登入者 | 本人群組 |
| 訂餐統計與設定 | 無權限不顯示且 API 拒絕 | 依 `meal_admin` 查看及管理 | 角色適用群組 |
| 封存班表 | 依 `schedule_view` 唯讀 | 依 `schedule_manage` 建立封存 | 角色適用群組及歷史關聯 |

## 9.4 正式 SQL

全新資料庫固定依序執行：

```text
1. supabase/001_current_schema.sql
2. supabase/002_current_updates.sql
```

`001_current_schema.sql` 建立正式基礎模型；`002_current_updates.sql` 建立仍有效且可重複執行的群組、角色權限、封存、RLS、Trigger 及 RPC 更新。SQL Editor 任何區段失敗時立即停止，不可略過。

## 9.5 權限與資料存取架構

正式系統採單一路徑，不保留舊版通用存取方式：

1. 瀏覽器不得直接對核心資料表執行 `SELECT / INSERT / UPDATE / DELETE`。
2. 班表、人員、單位、班別、假別、群組、角色與系統設定，只能透過具名 RPC 或對應 Edge Function 存取。
3. 需要讀寫受保護主檔的 RPC 使用 `SECURITY DEFINER`，並在函式內先以 `auth.uid()` 驗證功能權限與適用群組。
4. `authenticated` 是 Supabase/PostgreSQL 的登入角色，不等同系統「管理員」；最高權限也不得靠資料表 GRANT 繞過應用權限。
5. 應用權限唯一來源為 `set_employee.access_role_id` → `access_roles.permissions`，再搭配 `access_role_groups` 判斷適用群組。
6. 舊欄位 `set_employee.role` 只保留必要的歷史／顯示相容資料，不作為功能授權依據。
7. RLS 保留為第二層資料庫防護，但不是前端功能授權來源；`anon` 與 `authenticated` 不授予核心資料表直接權限。
8. 不使用通用 `restSelect/restInsert/restUpdate/restDelete`、整包 `saveState/syncCatalogs`、動態資料表名稱 CRUD 或 runtime monkey patch。
9. 不保留已被正式 API 取代的舊 RPC／Edge Function；每個領域只有一個正式寫入入口。
10. 人員登入帳號新增、修改、重設密碼與刪除統一由 `member-auth-admin` 負責；其中權限角色、適用群組、單位與排班班別皆由伺服器驗證。
11. 班表讀取使用 `get_scheduler_bootstrap_v3` 與 `get_schedule_entries_v3`；班表寫入使用 `save_schedule_entries_v3`。
12. 單位、班別、假別／加班、排序、週期設定與假日各使用明確的具名 RPC，不再透過全狀態同步間接寫入。

目前 Edge Functions：

- `member-auth-admin`
- `attendance-clock`
- `attendance-ledger`
- `attendance-review-groups`
- `attendance-ledger-export`
- `meal-order`
- `meal-report-v2`
- `meal-cancel-v2`

簽到審核與匯出以 `attendance_review` 權限及角色適用群組限制資料範圍，不以 `admin/manager` 字串判斷。

## 9.6 交易與冪等

下列操作不得因重送產生重複資料或改寫第一次有效結果：

- 上班及下班打卡。
- 每日簽到修改與批次審核。
- 訂單新增、修改與取消。
- 班表批次保存。
- 群組排序及角色群組關聯更新。
- 班表封存。

下列操作使用交易：

- 打卡與稽核。
- 管理員修改與稽核。
- 訂單新增、修改及整張取消。
- 群組刪除。
- 角色更新及適用群組重建。
- 班表封存主檔與全部快照明細。

## 9.7 備份與復原目標

1. 正式資料庫至少每日自動備份一次，並定期驗證可還原性。
2. RPO（可接受資料遺失上限）為 24 小時；涉及人員、班表、簽到或訂餐的大量異動前應額外建立可還原備份。
3. RTO（服務中斷後的復原目標）為 8 小時；資料庫故障、錯誤部署或重大資料異常均以恢復核心登入、班表、簽到及訂餐功能為優先。
4. 復原後必須核對角色權限、群組隔離、封存快照、班表筆數、簽到筆數與訂餐金額。

## 9.8 頁面資料按需載入

1. 登入階段只取得 Session、本人基本資料、角色權限、所屬群組、適用群組與首頁必要的訂餐開關；不得預載完整班表、簽到審核、訂餐統計或管理設定。
2. 第一次進入班表時，先讀 `scheduler_settings` 的正式週期設定，再以 `eight_week_start_date` 與今天計算今天所在的 56 天週期；首次畫面不得使用或相容舊的 `schedule_start_date` 瀏覽位置。
3. 確定八週起點後，單位目錄、人員目錄、班別、假別、加班設定、假日、人員排序與該八週含 7 日緩衝的班表資料平行讀取；同一首次載入不得再對同一可視區間補查第二次 `schedule_entries`。
4. 首頁已取得的角色／適用群組 bundle 在進入班表時直接沿用；班表只補載群組實體對照，不重複讀取相同權限 bundle。
5. 單位地址、GPS、固定 IP 與打卡開關只在管理員真正進入「單位設定」時載入，不列入班表首次載入。
6. 人員完整管理欄位只在真正進入「人員設定」時載入；共用班表人員目錄仍只提供班表需要的正式欄位。
7. 簽到簿只載入目前頁籤；預設個人記錄不預載簽到審核。訂餐今日、統計與設定各自於進入該頁籤時載入。
8. 同一登入階段已成功載入的頁面資料可在記憶體沿用；資料寫入後只失效或刷新相關資料範圍。登出或 Session 失效時清除全部頁面快取。
9. 按需載入是效能與資料最小化機制，不取代安全控制；RPC、RLS 與 Edge Function 仍必須驗證功能權限及適用群組。
10. 第一次進入班表且資料尚未完成時，畫面中央顯示圓形載入動畫；成功或失敗後自動收起。

# 十、發布與測試

## 10.1 發布流程

1. 修改正式來源。
2. 執行 `npm run web:publish`。
3. 執行 `npm run ci:check`。
4. 依順序套用正式 SQL。
5. 執行 `scripts/deploy-edge-functions.ps1` 部署 Edge Functions。
6. 提交至 `main`。
7. GitHub Pages 由 `.github/workflows/deploy-pages.yml` 發布 `main/docs`。
8. 以不同角色及不同適用群組抽測主要流程。

## 10.2 必要測試

### 群組與權限

- 班表群組下拉選單只顯示角色適用群組。
- 人員所屬群組只連動單位及班別，不影響角色。
- 共用人員目錄對所有有效登入者回傳工號及排班班別，且資料列不超出角色適用群組。
- 班表查看與管理連動正確。
- 新增群組只自動加入建立者角色。
- 指定群組角色無法從 REST、RPC 或 Edge Function 存取其他群組。
- 群組訂餐關閉後首頁按鈕及後端新增訂餐都被禁止。

### 班表與封存

- 不同群組的班表、復原、重做及剪貼簿互不影響。
- 人員、班別及支援單位群組不一致時，資料庫拒絕寫入。
- 封存範圍不可重疊。
- 封存日期不可新增、修改、移動或刪除班表。
- 人員、單位、班別及群組改名或刪除後，封存畫面仍顯示原快照。
- 未封存班表阻止人員或單位調動及刪除；只有封存歷史時允許邏輯刪除。

### 簽到與訂餐

- 個人只能查看自己的簽到資料。
- 簽到審核只顯示角色適用群組。
- 「群組－單位」顯示正確。
- 訂餐統計及設定只顯示角色適用群組。

### 資料載入生命週期

- 登入完成只取得身分、權限及首頁必要群組資料，不呼叫完整班表、人員管理、簽到審核或訂餐管理資料。
- 第一次點班表才讀取完整班表資料；同一登入 Session 再次進入班表不因首頁切換而重新初始化。
- 進入簽到簿預設只查個人記錄；未點「簽到審核」前不得發出審核資料請求。
- 沒有 `attendance_review`、`meal_admin`、`member_settings` 等功能權限時，不發出對應管理頁的資料請求。
- 登出或 Session 失效後，前一位使用者已載入的頁面資料與載入旗標全部清除。
- 即使前端未顯示或未呼叫功能，直接呼叫 RPC／Edge Function 仍會依 RLS、功能權限與適用群組拒絕未授權操作。

### 介面與建置

- 手機首頁、簽到簿、班表及訂餐上方間距一致。
- 群組設定的單位欄最寬，各單位以可換行膠囊標籤顯示。
- 權限設定的權限項目欄為主要寬欄，各權限以可換行膠囊標籤顯示。
- 第一次載入班表期間顯示圓形載入動畫，完成或失敗後消失。
- 週六日標題字色為黑色。
- 前一週、後一週不跨八週週期。
- `src/renderer/`、bundle 與 `docs/` 完全同步。
- 正式 SQL、Edge Function 資料夾、部署清單、README 與本規格互相一致。

## 10.3 發布門檻

1. 自動檢查全部通過。
2. 群組、角色、封存表及 RPC 已存在於正式 SQL。
3. 資料庫群組一致性檢查為零筆異常。
4. Edge Function 正式部署清單完整。
5. 員工、主管、管理員及自訂角色完成跨群組權限抽測。
6. 高風險操作完成重複送出、交易失敗及回滾驗證。
