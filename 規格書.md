# 規格書

**專案名稱：** 福圓號排班系統（FYH）  
**文件版本：** 2026-08-05  
**文件狀態：** 唯一正式規格  
**主要裝置：** 手機優先，支援電腦與平板  
**正式時區：** `Asia/Taipei`  
**適用程式版本：** GitHub `main` 分支  
**文件管理規則：** 後續需求、介面與技術規格直接更新本檔，不另建規格補充檔。

> 本系統尚未正式上線，舊出勤資料不保留。舊打卡、加班申請與其稽核測試資料已清空，舊資料表及舊 RPC 已移除；正式出勤資料只使用每日簽到簿模型。

---

# 一、系統範圍與資訊架構

## 1.1 系統目的

系統提供：

1. 人員登入與角色權限。
2. 八週班表查看、編輯、設定與匯出。
3. 每日上下班打卡、上班時數、加班時數、備註與審核。
4. 今日訂餐、訂餐統計與商品設定。
5. 個人歷史、管理稽核與資料匯出。

不包含跨日夜班、一天多班、一天多次上下班、二級簽到審核或未來日期訂餐。

## 1.2 主要導覽

```text
登入
└─ 首頁
   ├─ 簽到簿
   │  ├─ 個人記錄
   │  └─ 簽到審核（管理員）
   ├─ 班表
   └─ 訂餐
      ├─ 今日訂餐
      ├─ 訂餐統計（主管／管理員）
      └─ 訂餐設定（主管／管理員）
```

首頁不再顯示獨立「打卡」入口；原打卡功能整合至簽到簿的今日資料列。原「加班申請」、「加班審核」及「打卡管理」不再作為獨立頁面或獨立資料流程。

## 1.3 頁面與資料權限矩陣

| 功能 | 員工 | 主管 | 管理員 | 正式資料介面 |
|---|---|---|---|---|
| 登入、首頁、修改自己的密碼 | 本人 | 本人 | 本人 | Supabase Auth、`get_my_profile_v2()` |
| 簽到簿個人記錄 | 僅本人 | 僅本人 | 僅本人 | `attendance-ledger` |
| 今日上下班打卡 | 僅本人 | 僅本人 | 僅本人 | `attendance-clock`、`save_attendance_clock()` |
| 修改今日上班／加班時數與備註 | 僅本人未審資料 | 僅本人未審資料 | 僅本人未審資料 | `attendance-ledger` |
| 簽到審核 | 不可 | 不可 | 全體 | `attendance-ledger` |
| 簽到簿匯出 | 不可 | 依正式需求 | 可 | `attendance-ledger-export` |
| 班表查看 | 全體相同 | 全體相同 | 全體相同 | `get_schedule_directory_v2()`、`schedule_entries` |
| 班表編輯與一般設定 | 不可 | 可 | 可 | 班表 RPC、管理名錄 |
| 今日訂餐 | 本人 | 本人 | 本人 | `meal-order` |
| 訂餐統計與商品設定 | 不可 | 可 | 可 | 訂餐管理端點 |
| 人員一般管理 | 不可 | 可，不能管理管理員角色 | 可 | `member-auth-admin` |
| 單位打卡地址、座標、IP 與啟用 | 不可 | 不可修改 | 可 | `department-attendance-v2` |

前端隱藏按鈕不是權限控制；所有受保護讀寫都必須由後端重新驗證登入身分、角色與任職有效期間。

---

# 二、登入、首頁與共通帳號規則

## 2.1 未登入

1. 登入介面使用全畫面。
2. 未登入不顯示班表、員工、打卡、加班與訂餐資料。
3. 敏感資料不得先載入再用 CSS 隱藏。
4. Token 無效或 Session 逾時時，清除敏感畫面並重新登入。

## 2.2 首頁

首頁顯示：

- 使用者姓名。
- 使用者角色。
- 簽到簿入口。
- 班表入口。
- 訂餐入口。
- 修改密碼。
- 登出。

手機版主要入口採單欄排列，按鈕文字置中，入口下方不得留下大面積空白。主要頁面皆提供右上 X 圖示返回首頁。

## 2.3 角色

- `employee`：員工。
- `manager`：主管，包含員工能力及班表、一般設定、訂餐統計與商品設定。
- `admin`：管理員，包含主管能力及角色管理、敏感打卡設定與簽到審核。

## 2.4 任職有效期間

帳號有效期間為到職日 00:00 至離職日後第 5 日 23:59；正式日期由伺服器以 `Asia/Taipei` 判定。

1. 未到職帳號不可登入受保護功能。
2. 超過離職寬限期後，下一次受驗證操作被拒絕。
3. 人員不設 `is_active`；任職狀態以到職日與離職日判定。
4. 系統永遠保留至少一位有效管理員。
5. 有班表、簽到、簽到稽核、訂餐或設定歷史的人員不可實體刪除，應填寫離職日保留歷史。

## 2.5 Session

1. 手機使用可跨關閉保存的登入狀態，48 小時滑動閒置期限。
2. 電腦、iPad、Android 平板與觸控筆電使用分頁階段儲存，30 分鐘滑動閒置期限。
3. 裝置無法判斷時採電腦規則。
4. 閒置逾時後的第一個受保護操作不得成功。

---

# 三、簽到簿

## 3.1 頁面目的

簽到簿是出勤功能唯一正式入口，使用 Chrome 式頁籤呈現：

1. **個人記錄：** 所有登入者查看自己的每日班表、打卡、時數、備註、異常與訂餐；今日列可直接打卡及修改未審欄位。
2. **簽到審核：** 管理員查看全體每日簽到、補登或修改、批次審核、批次退回與查看歷程。

主管不具全體簽到審核權限。

## 3.2 個人記錄

### 3.2.1 查詢與分頁

1. 顯示開始日期與結束日期。
2. 日期變更後立即查詢，不顯示額外查詢按鈕。
3. 預設查詢最近 50 日，可查全部歷史。
4. 每頁 50 筆，依日期倒序。
5. 查詢失敗不得誤顯示為零筆資料。

手機版兩個日期欄固定同列、平均分配寬度。

### 3.2.2 欄位

個人記錄至少包含：

1. 日期。
2. 班表圖示：班別、假別、班表加班，順序及顏色與班表一致。
3. 班別名稱與上下班時間。
4. 上班打卡時間及地點摘要。
5. 下班打卡時間及地點摘要。
6. 上班時數。
7. 加班時數。
8. 異常。
9. 備註。
10. 訂餐摘要。
11. 審核狀態。

個人頁只顯示必要地點名稱與來源，不顯示原始 GPS、IP、定位精準度、距離或完整 JSON。

個人記錄表格顯示規則：

1. 未打卡且非今日的打卡時間欄保持空白，不顯示「-」。
2. 打卡地點與對應時間顯示在同一行。
3. 只有今日未審資料顯示上班時數、加班時數及備註輸入框；其他日期只顯示純文字值或空白。
4. 備註使用單行輸入框，且不得超出備註欄或覆蓋訂餐欄。
5. 審核欄採固定窄欄呈現。

### 3.2.3 今日列打卡

1. 只有伺服器判定的今日列顯示「上班打卡」與「下班打卡」。
2. 按下後先確認，再取得定位或進行 IP 驗證。
3. 上班與下班可獨立存在；沒有上班卡仍允許下班打卡。
4. 每人每天最多一筆上班時間與一筆下班時間。
5. 第一次成功時間為正式時間；快速連按、網路重送或重複請求不得覆寫。
6. 已審核的每日資料不得再由員工打卡或修改。
7. 非今日列不得打卡，前端顯示「只能在今天的紀錄列打卡」，後端仍需拒絕非今日請求。
8. 打卡成功後重新載入簽到簿與今日訂餐資格。

### 3.2.4 打卡地點驗證

1. 手機先取得 GPS，定位等待上限 15 秒，要求即時高精準度位置。
2. GPS 距公司地點不超過 300 公尺且精準度不超過 300 公尺時通過。
3. GPS 拒絕、失敗、逾時、距離不符或精準度不符時，改驗證固定對外 IP。
4. 多個地點符合時使用距離最近單位。
5. 電腦及平板不要求 GPS，只驗證固定對外 IP。
6. 位置不符時只顯示一般錯誤，不回傳允許 IP、完整座標或內部判斷細節。
7. 單位後續改名、搬家或調整座標，不回寫歷史地點快照。

### 3.2.5 上班時數、加班時數與備註

1. `regular_minutes` 與 `overtime_minutes` 以分鐘保存，前端以小時顯示。
2. 輸入以 0.5 小時為單位，不可為負數。
3. 員工只能修改自己的今日未審資料。
4. 加班時數直接屬於每日簽到資料，不再建立獨立加班申請、待審申請或刪除重提流程。
5. 備註屬每日簽到資料，修改需寫入稽核。
6. 管理員審核後，員工不可再修改；需由管理員退回後才可調整。

### 3.2.6 異常

異常依當前班表、班別、地點及簽到資料即時計算，可同時存在：

- 未打上班。
- 未打下班。
- 無排班但有打卡。
- 遲到。
- 早退。
- 上班時間晚於下班時間。
- 上班地點不符。
- 下班地點不符。
- 打卡時間格式異常。
- 班別缺少完整上下班時間。

時間比較到分鐘，忽略秒數；異常不另外永久保存為單一結果。

## 3.3 簽到審核

### 3.3.1 權限與查詢

1. 僅管理員可進入。
2. 查詢條件：開始日期、結束日期、人員、審核狀態、異常類型。
3. 條件變更後立即重新查詢。
4. 預設顯示未審資料，每頁 50 筆。
5. 「全部顯示」包含正常與異常；「全部異常」只顯示異常資料。

### 3.3.2 欄位

至少包含：

1. 勾選。
2. 日期。
3. 工號與姓名。
4. 班表圖示。
5. 班別與班表時間。
6. 上班時間及地點。
7. 下班時間及地點。
8. 上班時數。
9. 加班時數。
10. 備註。
11. 異常。
12. 審核狀態。
13. 操作與歷程。

### 3.3.3 管理操作

1. 管理員可補登、修改或清除上下班時間。
2. 管理員可修改上班時數、加班時數與備註。
3. 手動補登地點使用「管理員補登」摘要，不偽造 GPS 或 IP。
4. 編輯後資料回到未審狀態。
5. 可逐筆審核、逐筆退回、批次審核及批次退回。
6. 批次操作前需再次確認。
7. 審核保存 `reviewed_at` 與 `reviewed_by`。
8. 退回清除審核欄位，但保留簽到內容與歷程。
9. 管理員任何異動都必須寫入 `attendance_audit_logs`，包含操作、操作者、原因、修改前與修改後完整快照。

## 3.4 簽到簿匯出

1. `attendance-ledger-export` 依日期、人員及正式權限匯出。
2. 匯出必須直接讀取正式資料，不依賴目前前端頁面記憶體。
3. 匯出時間使用 `Asia/Taipei`。
4. 大型匯出顯示進度，逾時不得產生假成功檔案。

## 3.5 正式 Edge Function 契約

- `attendance-clock`
  - `today`：取得本人今日狀態。
  - `clock_in`：上班打卡。
  - `clock_out`：下班打卡。
- `attendance-ledger`
  - `personal_list`：本人每日記錄。
  - `personal_save`：修改本人今日上班時數、加班時數或備註。
  - `review_list`：管理員審核清單。
  - `review_save`：管理員補登／修改。
  - `review_set`：審核或退回，支援批次。
  - `history`：管理員查看稽核歷程。
- `attendance-ledger-export`
  - 依查詢條件產生正式簽到匯出。

前端不得再呼叫已淘汰的個人記錄、加班或打卡管理端點。

---

# 四、每日簽到資料模型

## 4.1 `attendance_days`

每人每天最多一列，唯一限制為 `user_id + work_date`。

| 欄位 | 意義 |
|---|---|
| `id` | 每日簽到 UUID |
| `user_id` | 人員 UUID |
| `work_date` | 工作日期 |
| `clock_in_at` | 上班時間 |
| `clock_in_location` | 上班地點與來源 JSON 快照 |
| `clock_out_at` | 下班時間 |
| `clock_out_location` | 下班地點與來源 JSON 快照 |
| `regular_minutes` | 上班時數分鐘，0.5 小時倍數 |
| `overtime_minutes` | 加班時數分鐘，0.5 小時倍數 |
| `note` | 每日備註 |
| `reviewed_at` | 審核時間 |
| `reviewed_by` | 審核管理員 |
| `created_at` | 建立時間 |
| `updated_at` | 最後修改時間 |

地點 JSON 可保存單位 ID、名稱、地址、來源、座標、精準度、距離與 IP；一般使用者回應必須過濾敏感欄位。

## 4.2 `attendance_audit_logs`

| 欄位 | 意義 |
|---|---|
| `id` | 稽核 UUID |
| `attendance_day_id` | 對應每日簽到 |
| `action` | 操作類型 |
| `changed_by` | 操作人 |
| `before_data` | 修改前完整 JSON |
| `after_data` | 修改後完整 JSON |
| `reason` | 異動原因 |
| `created_at` | 操作時間 |

稽核只由受保護後端寫入；一般員工與主管不得取得完整快照。

## 4.3 舊結構清理決策

1. 本系統尚未正式上線，舊出勤資料不保留。
2. 舊測試資料已全部清空。
3. 正式資料庫不得存在：
   - `attendance_records`
   - `attendance_action_logs`
   - `attendance_overtime_requests`
   - `overtime_review_logs`
4. 舊管理 RPC 及舊加班審核 RPC 必須移除。
5. 舊 Edge Function 不列入正式部署清單；平台暫時無法實體刪除時只回傳 HTTP 410。
6. 不進行雙寫、資料同步、相容代理或舊欄位猜測。

## 4.4 SQL 建置順序

```text
1. supabase/001_current_schema.sql
2. supabase/002_current_updates.sql
3. supabase/003_attendance_ledger.sql
4. supabase/004_remove_legacy_attendance.sql
```

Edge Function 部署不會自動執行 SQL；SQL 任何一步失敗時立即停止，不可跳過錯誤。

---

# 五、訂餐

## 5.1 分頁

1. 今日訂餐。
2. 訂餐統計（主管／管理員）。
3. 訂餐設定（主管／管理員）。

使用與簽到簿相同的 Chrome 式頁籤；選取頁籤與內容區緊密相接，不重複顯示同名內容標題。

## 5.2 今日訂餐資格

1. 今日 `attendance_days.clock_in_at` 已存在。
2. `clock_in_location.departmentId` 明確存在。
3. 尚未超過伺服器每日截止時間。
4. 不要求今日有班表。
5. 只處理今天，不處理未來日期。
6. 管理員補登今日上班卡後，在截止時間內可訂餐。

## 5.3 商品與訂單

1. 數量只接受 0 或正整數。
2. 第一次全部為 0 不建立訂單。
3. 已有訂單全部改為 0 時需確認後取消整張訂單。
4. 同一人同一天同一商品唯一；同日多商品共用 `order_id`。
5. 修改維持第一次下訂時間、原 `order_id` 與第一次訂餐單位快照。
6. 歷史訂單保存商品名稱、單價、員工與單位快照。
7. 停用品項對已訂者仍顯示，只能減少或取消，不可增加。
8. 截止後停止新增、修改與取消。
9. 今日訂餐需讀取 `attendance_days`，不得依賴舊打卡表。

## 5.4 訂餐統計

查詢條件為日期起迄、單位、人員與報表類型；條件變更即重新查詢。提供明細、品項、人員彙總與 Excel 匯出。

自付額：

```text
金額 -（訂餐日數 × 公司補助）
```

總數量與總金額固定同列、標題與數值不換行。

## 5.5 訂餐設定

1. 截止時間全系統共用，預設 `10:30`。
2. 公司補助預設 55，只接受正整數。
3. 商品名稱不可重複，價格不得為負數。
4. 商品最前方使用拖曳把手，只有把手可排序。
5. 無歷史訂餐商品可刪除；有歷史時後端拒絕並提示停用。
6. 截止前修改價格時，以交易同步更新今日訂單；截止後不影響已截止或歷史訂單。

---

# 六、班表與設定

## 6.1 班表

1. 預設顯示今天所在八週週期。
2. 檢視包含人員檢視、人員檢視－統計欄與班別檢視。
3. 每日格可同時保存班別、假別及班表加班，順序固定為班別、假別、加班。
4. 唯一限制為 `member_id + work_date`；空白格不保留資料列。
5. 所有有效登入角色看到相同人員列、單位分組、排序、PT 標記與班表內容；角色差異只影響操作權限。
6. 員工只查看；主管及管理員可編輯、拖曳、複製、剪下、貼上、刪除、復原及重做。
7. 復原與重做最多保存最近 20 次操作。
8. 自動排班與自動補班採先預覽、再套用；未套用前不得寫入資料庫。
9. 自動補班只處理月薪人員完全空白格，使用其第一個有效排班班別。

## 6.2 工具列與日期

1. 頂端預設顯示「全部顯示」下拉選單，右側提供上一步及下一步圖示。
2. 浮動工具列收合按鈕加大並醒目；收合後仍保留收合、上一步、下一步。
3. 浮動班表加班區塊預設隱藏，但設定、歷史顯示與功能保留。
4. 隱藏加班區塊後，班別與假別兩欄撐滿可用寬度。
5. 日期欄週六日文字與平日相同使用黑色。
6. 前一週及後一週只能在目前八週範圍移動，不得跨週期。
7. 日期欄左右的前一週／後一週圖示預設隱藏，但功能保留。
8. 例假同日有排班時，只將「例假」小區塊改黃色；不得整格變黃或加額外圖示。

## 6.3 單位、人員及目錄設定

1. 新增單位、人員、班別、假別、加班與商品的主要按鈕文字統一為「新增」。
2. 新增加班預設名稱為「加班」。
3. 人員、單位、班別、假別、班表加班及商品最前方統一使用拖曳把手。
4. 只有把手可開始拖曳；排序後重新整理及重新登入仍保留。
5. 拖曳前後保存頁面、彈窗及表格捲動位置，不得跳回頂端。
6. 人員是否在職依到職日與離職日；不以缺少於畫面名錄作為刪除依據。
7. 單位有人員、班別、班表、簽到或訂餐歷史時不得刪除。
8. 管理員可維護單位地址、座標、固定 IP 與打卡啟用；主管只能維護一般欄位。

## 6.4 例休與匯出

1. 每 7 天至少一日例假與一日休息日。
2. 連續上班不得超過 6 天，採滑動區間並包含前期延續。
3. 匯出休例假、請假與班表加班時先選日期範圍。
4. 匯出直接查詢正式 `schedule_entries`，不得只使用目前畫面資料。
5. 已離職但選定期間有正式班表資料的人員仍需匯出。

---

# 七、介面樣式統一規格

## 7.1 CSS 架構

正式 CSS 原始碼位於 `src/renderer/css/`：

1. `foundation.css`：全域基礎與主要結構。
2. `schedule.css`：班表專用布局。
3. `components.css`：共用按鈕、表單、頁籤、卡片、彈窗與表格。
4. `responsive.css`：手機與平板響應式規則。
5. `pages.css`：無法共用的頁面專屬規則。

不得新增依靠後載入覆蓋的 `fix`、`final`、`patch` 樣式檔。`app.css` 為建置產物，不直接修改。

## 7.2 共用元件

1. 一般控制項高度 40px，手機至少 42px。
2. 一般控制項圓角 12px，表格 14px，內容區 18px，主卡片及彈窗 24px。
3. 輸入框、日期、時間及下拉選單同列時等高。
4. 圖示按鈕提供 `title` 或 `aria-label`。
5. 聚焦狀態清楚可見；狀態不得只靠顏色表示。
6. 觸控主要操作區建議不小於 44×44 CSS px。

## 7.3 Chrome 式頁籤

1. 簽到簿與訂餐使用同一套瀏覽器籤頁式結構。
2. 頁籤固定單行，不足寬度時僅頁籤列水平滑動。
3. 選取文字 17px、粗體；未選取 15px、粗體，切換時高度不變。
4. 頁籤列與內容區緊密相接，內容區不重複顯示頁籤名稱。
5. 使用正確 `role="tablist"`、`role="tab"` 與 `aria-selected`。

## 7.4 表格

1. 今日訂餐、訂餐統計、訂餐設定、個人記錄及簽到審核共用表頭、資料列、分隔線與懸停規則。
2. 表頭不透明，高度約 42px；資料列最小高度約 44px。
3. 表格過寬只在表格容器內水平捲動，不造成整頁水平捲動。
4. 空資料狀態顯示在表格內。
5. 人員與班別設定在電腦版依彈窗寬度配置，不出現不必要水平捲軸。
6. 假別與加班設定在手機寬度不足時，可保留表格內水平捲動。

## 7.5 手機版主要頁面

適用首頁、簽到簿、班表與訂餐：

1. 寬度不超過 640px 時，上方與左右安全間距為 8px。
2. 內容由畫面上方開始，不得垂直置中造成大面積空白。
3. 最外層卡片移除背景、邊框、圓角、陰影與額外內距；內部功能卡片仍保留。
4. 641px 至 960px 的窄螢幕，上方外距統一 12px。
5. 篩選列與按鈕列可換行，但不得造成整頁溢出。
6. 班表底部預留浮動工具列高度，最後一列不得被遮住。
7. 返回首頁 X 按鈕尺寸與位置一致。

## 7.6 共通狀態

所有頁面必須處理載入、背景更新、空資料、無權限、讀取失敗、儲存中、儲存成功、儲存失敗、Session 失效、資料衝突及網路離線。

1. 讀取失敗不得顯示為空資料。
2. 儲存失敗保留輸入與目前畫面。
3. 超過 500 毫秒仍未完成的互動需提供可見進度。
4. 寫入結果不明時先重新讀取正式資料，再決定是否重送。

---

# 八、後端、安全與資料規則

## 8.1 架構分工

- 前端：畫面、輸入、一般格式提示與 API 呼叫。
- Edge Function：驗證 Token、角色、有效期間、伺服器時間、位置及業務規則。
- PostgreSQL／RLS／RPC：正式資料、限制、交易、冪等性與稽核。

前端不得保存 `service_role`、資料庫密碼或其他高權限 Secret。

## 8.2 正式 Edge Functions

正式部署清單：

- `member-auth-admin`
- `catalog-admin`
- `attendance-clock`
- `attendance-ledger`
- `attendance-ledger-export`
- `meal-order`
- `department-attendance-v2`
- `member-delete-v2`
- `member-order-v2`
- `meal-report-v2`
- `meal-cancel-v2`

`supabase/functions/` 資料夾與 `scripts/deploy-edge-functions.ps1` 必須一致。

## 8.3 冪等與交易

下列操作不得因重送產生重複資料或改寫第一次有效結果：

- 上班打卡。
- 下班打卡。
- 每日簽到修改。
- 簽到批次審核／退回。
- 訂單新增與修改。
- 班表批次保存。

下列操作必須具備交易一致性：

- 打卡與稽核寫入。
- 管理員修改與稽核寫入。
- 訂單新增、修改與整張取消。
- 商品價格同步更新今日訂單。
- 帳號與 Auth 刪除。
- 最後一位管理員保護。

## 8.4 資料分類

| 等級 | 例子 | 原則 |
|---|---|---|
| 公開部署資料 | Supabase URL、publishable／anon key、資源版本 | 可在前端，不具管理權限 |
| 公司共同資料 | 姓名、單位、班表、班別 | 有效登入者依功能取得 |
| 管理資料 | 工號、角色、排班規則 | 主管或管理員依權限取得 |
| 個人與稽核資料 | 打卡、GPS、IP、簽到快照 | 本人摘要或管理員取得 |
| Secret | service role、資料庫密碼、部署 Token | 僅伺服器或 CI Secret |

## 8.5 人員刪除

1. 有班表、`attendance_days`、`attendance_audit_logs`、訂餐或設定異動歷史時拒絕刪除。
2. 阻擋時不得把人員改為停用或從名錄消失。
3. 完全沒有歷史時，Auth 與 `set_employee` 在同一交易完成刪除。
4. 離職人員使用離職日，不使用刪除代替。

---

# 九、API 契約

## 9.1 共通成功回應

```json
{
  "ok": true,
  "data": {},
  "requestId": "optional"
}
```

現有端點可維持已發布契約，但每個端點只能有一套正式格式；前端 adapter 不得同時猜測多套舊格式。

## 9.2 共通錯誤回應

```json
{
  "ok": false,
  "code": "VALIDATION_FAILED",
  "message": "欄位內容不正確",
  "details": {},
  "requestId": "optional"
}
```

1. `code` 為穩定機器代碼。
2. `message` 使用繁體中文，不暴露 SQL、堆疊、Token 或 Secret。
3. 前端不得只比對中文全文決定流程。

## 9.3 HTTP 狀態

| 狀態 | 用途 |
|---:|---|
| 200／201 | 成功 |
| 400 | 格式或業務輸入錯誤 |
| 401 | 未登入或 Session 無效 |
| 403 | 權限不足 |
| 404 | 資源不存在 |
| 409 | 重複、衝突或狀態不允許 |
| 410 | 已淘汰端點 |
| 422 | 業務驗證失敗 |
| 429 | 請求過於頻繁 |
| 500 | 未預期錯誤 |
| 503 | 暫時無法服務 |

## 9.4 版本切換

1. 靜態前端、Edge Function 與資料庫異動協調發布。
2. 任務完成後只保留單一正式欄位、RPC 與端點。
3. 已淘汰但暫時無法刪除的端點只回傳 410，不轉送、不轉換、不寫入。
4. 不得長期維護新舊雙軌。

---

# 十、效能、備份、發布與測試

## 10.1 效能與容量

1. 一般讀取及單筆寫入，正常網路下 95% 於 3 秒內完成。
2. 報表、Excel 與大型班表讀取原則上 15 秒內完成，超過 3 秒顯示持續進度。
3. 至少支援 100 名有效人員、8 週班表、5,000 個班表格及 20 名同時在線使用者。
4. 超過 500 筆的歷史清單使用日期、分頁或伺服器篩選。
5. 每月可用性目標 99.5%，已公告維護時間除外。

## 10.2 備份與災難復原

- **RPO：** 最多可接受遺失 24 小時內資料。
- **RTO：** 重大故障後 8 小時內恢復登入、班表、簽到及訂餐核心功能。

1. 正式資料庫至少每日備份或建立等效匯出。
2. 高風險 SQL、批次清理及大規模資料轉換前完成可還原備份。
3. 至少每六個月進行還原演練。
4. 備份不得放入公開 GitHub 或 `docs/`。

## 10.3 發布

1. 修改前端來源後執行 `npm run web:publish`。
2. 執行 `npm run ci:check`。
3. 依第四章順序套用 SQL。
4. 部署正式 Edge Functions。
5. 合併至 `main`。
6. GitHub Pages 由內建 `pages-build-deployment` 發布 `main/docs`。
7. 發布後驗證員工、主管與管理員主要流程。
8. `.github/workflows/deploy-pages.yml` 是唯一正式 GitHub Actions 驗證流程。

## 10.4 必要測試

### 登入

- 未登入無法取得受保護資料。
- 未到職與離職逾期帳號被拒絕。
- 手機 48 小時、電腦／平板 30 分鐘 Session 規則正確。

### 簽到簿

- 今日列可上班及下班打卡。
- 非今日列前後端均拒絕打卡。
- 無上班卡仍可下班打卡。
- 重複請求不改寫第一次時間。
- 手機 GPS 299 公尺／精準度 299 公尺通過；301 公尺改驗證 IP。
- 員工只能修改本人今日未審時數及備註。
- 審核後員工不可修改。
- 管理員修改會寫入完整 before／after 稽核。
- 批次審核及退回具確認且結果一致。

### 訂餐

- 無上班卡或無打卡單位時不可訂餐。
- 有上班卡但無班表時可訂餐。
- 數量只接受 0 或正整數。
- 截止後停止新增、修改及取消。
- `meal-order` 只讀取 `attendance_days`。

### 班表與介面

- 三種角色看到相同班表。
- 週六日標題字色為黑色。
- 前一週／後一週不跨八週週期。
- 例假排班只將例假小區塊變黃。
- 手機主要頁面上方空白、左右安全間距與外層卡片規格一致。
- `src/renderer/`、bundle 與 `docs/` 完全同步。

## 10.5 發布門檻

1. 自動檢查全部通過。
2. 資料庫只剩單一正式簽到模型。
3. 正式部署清單不含舊出勤端點。
4. 權限異動測試員工、主管、管理員與未登入。
5. 高風險操作完成重複送出、交易失敗與回滾驗證。
6. 手機、桌面、鍵盤與主要瀏覽器完成抽測。


#### 個人記錄表格欄寬與打卡按鈕

1. 個人記錄表格在一般電腦寬度下應完整顯示到最右側審核欄，不應因打卡時間欄過寬而裁切。
2. 打卡時間欄以容納「上班／下班時間＋地點」單行內容為原則，不額外保留大面積左右空白。
3. 上班打卡按鈕使用棕橘色，下班打卡按鈕使用綠色，兩者需維持白色高對比文字。
