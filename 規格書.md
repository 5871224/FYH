# 規格書

**專案名稱：** 福圓號排班系統（FYH）  
**文件版本：** 2026-09-02  
**文件狀態：** 唯一正式規格  
**主要裝置：** 手機優先，支援電腦與平板  
**正式時區：** `Asia/Taipei`  
**適用程式版本：** GitHub `main` 分支  
**文件管理規則：** 後續需求、介面與技術規格直接更新本檔，不另建規格補充檔。

> 本系統尚未正式上線，只維護單一正式版本。本文件及兩個正式 SQL 定義唯一正式資料結構；舊資料表、舊欄位、舊端點、舊 payload、雙軌讀寫與補丁式覆寫均不得保留，且不進行資料遷移。

---

# 一、系統範圍與導覽

## 1.1 系統目的

系統提供：

1. 人員登入、自訂角色、功能權限及逐群組權限。
2. 多群組八週班表查看、編輯、設定、匯出與封存。
3. 每日上下班打卡、上班時數、加班時數、備註與簽到審核。
4. 依群組開關的今日訂餐、訂餐統計與商品設定。
5. 個人歷史、管理稽核及正式資料匯出。

不包含跨日夜班、一天多班、一天多次上下班、二級簽到審核或未來日期訂餐。

## 1.2 主要導覽

```text
登入
└─ 首頁
   ├─ 簽到簿
   │  ├─ 個人記錄
   │  └─ 簽到審核
   ├─ 班表
   │  ├─ 群組設定
   │  ├─ 權限設定
   │  ├─ 班表封存
   │  └─ 列印班表
   └─ 訂餐
      ├─ 今日訂餐
      ├─ 訂餐統計
      └─ 訂餐設定
```

首頁不顯示獨立「打卡」入口；打卡整合至簽到簿今日資料列。原「加班申請」、「加班審核」及「打卡管理」不再作為獨立頁面或資料流程。

---

# 二、群組、角色與權限

## 2.1 群組定位

群組是班表、單位、人員、簽到與訂餐資料的最上層範圍，例如「門市」、「工廠」。

1. 每個單位只能屬於一個群組。
2. 每位人員有一個所屬群組。
3. 班別綁定單位，群組由所屬單位決定；系統同步保存班別群組。
4. 假別為公司共用規則，所有群組共用。
5. 加班設定為公司共用規則，目前功能隱藏，權限併入假別設定。
6. 每筆班表直接保存群組 ID，不只依單位或人員即時計算。
7. 打卡、簽到與訂餐保存群組 ID 及必要名稱快照。

全新建置時建立預設群組：

```text
群組代碼：STORE
群組名稱：門市
可否訂餐：是
狀態：啟用
```

系統尚未正式上線，不保留舊版資料遷移規則；正式資料自建立時即直接歸屬其群組。

## 2.2 人員所屬群組

人員新增及編輯頁只增加「所屬群組」下拉選單，不增加常駐摘要、說明或提醒。

1. 所屬群組只影響可選的所屬單位及排班班別，與角色權限本身無關。
2. 選擇群組後，所屬單位只顯示該群組單位。
3. 排班班別只顯示該群組且符合所屬單位的班別。
4. 變更所屬群組時，清空原所屬單位及排班班別，必須重新選擇。
5. 若人員仍有未封存班表，不允許變更群組；已封存班表不構成限制。
6. 人員權限只由「權限」下拉選單所選角色決定。

## 2.3 角色管理

系統先建立三個角色：

- 管理員。
- 主管。
- 員工。

可新增自訂角色。角色本身只保存角色名稱、排序及「共用權限」；逐群組權限另存於角色＋群組的權限矩陣，不再保存單獨的「適用群組」清單。

角色列表欄位為：

| 欄位 | 規則 |
|---|---|
| 拖曳排序 | 每列最前方，只有「≡」把手可拖曳；拖曳完成後立即持久化儲存 |
| 角色名稱 | 不可空白或重複 |
| 共用權限 | 顯示 `settings`、`export`、`leave_settings` 中已勾選項目，各權限使用獨立膠囊標籤 |
| 群組權限／權限項目 | 主要寬欄；依群組顯示已勾選權限，每個權限使用獨立膠囊標籤顯示，空間不足時自動換行 |
| 操作 | 編輯、刪除 |

角色排序為公司共用排序；人員新增／修改表單的「權限」下拉選單必須依相同角色順序顯示。重新整理、重新登入或換裝置後仍依資料庫排序顯示。

角色不設狀態。若角色仍有人員使用，刪除前必須先更換角色。系統至少保留一個具有共用 `settings` 權限的角色，且至少保留一名有效、未刪除、使用該角色的人員；不得藉由修改、刪除角色或人員移除最後一個設定管理帳號。

新增或編輯群組本身由共用 `settings` 控制；新群組建立後是否可查看班表、管理班表、管理單位、審核簽到或管理訂餐，必須再由角色的逐群組權限矩陣明確設定，不能只靠角色名稱或舊式適用群組關聯取得權限。

## 2.4 權限項目

正式權限拆成「共用權限」與「群組權限」兩層；前端、RPC、RLS、Trigger 與 Edge Function 必須使用相同鍵值，不得再使用 `member_settings`、`permission_settings`、`access_roles.permissions` 或文字 `admin/manager` 作為授權來源。

### 2.4.1 共用權限

共用權限保存於 `access_roles.common_permissions`，不綁定單一群組：

| 權限鍵 | 畫面名稱 | 正式規則 |
|---|---|---|
| `settings` | 設定 | 管理群組設定、角色、共用／群組權限矩陣及其他明確歸入設定管理的功能；`access-control` 的角色新增、修改、刪除與排序均要求此權限 |
| `export` | 匯出 | 使用正式匯出功能；匯出資料列仍須受後端可存取群組與領域規則限制，不因具有 `export` 而取得全公司所有群組資料 |
| `leave_settings` | 假別設定 | 管理公司共用假別；目前隱藏的加班設定能力一併歸入此權限 |

### 2.4.2 群組權限

群組權限保存於 `access_role_group_permissions`，每筆以 `role_id + group_id` 對應該群組的權限陣列：

| 權限鍵 | 畫面名稱 | 正式規則 |
|---|---|---|
| `schedule_view` | 班表查看 | 可看到該群組並查看班表、封存班表及班表所需共用人員目錄 |
| `schedule_manage` | 班表管理 | 包含該群組班表編輯、班別、排班條件、人員設定／帳號管理、排序、自動排班及其他班表管理操作；勾選時必須同時具有 `schedule_view` |
| `department_settings` | 單位設定 | 管理該群組單位及其排序、打卡位置等單位設定 |
| `attendance_review` | 簽到審核 | 查詢、編輯、審核、退回、歷程及匯出該群組簽到審核資料 |
| `meal_admin` | 訂餐管理 | 查看及管理該群組訂餐統計與訂餐設定 |

班表權限連動規則：

1. 勾選 `schedule_manage` 時自動勾選 `schedule_view`。
2. 取消 `schedule_view` 時自動取消 `schedule_manage`。
3. 只有 `schedule_view` 時只能查看班表與封存資料，不得修改班表、人員或班別。
4. 班表匯出另受共用 `export` 控制；不得用 `schedule_manage` 取代共用匯出權限。
5. 沒有任何群組權限的群組，不因角色存在而自動成為可操作群組。

## 2.5 群組權限範圍

新版權限模型不再保存獨立「適用群組」清單。某角色能在某群組做什麼，直接由 `access_role_group_permissions.permissions` 決定。

1. `schedule_view` 決定班表群組選單及班表共同資料的可見範圍。
2. `schedule_manage`、`department_settings`、`attendance_review`、`meal_admin` 各自只授權其所在群組，不互相推導。
3. 共用 `settings` 的角色可取得全部群組與角色清單以進行設定管理，但不因此自動取得任何群組的 `schedule_view`、`schedule_manage`、`department_settings`、`attendance_review` 或 `meal_admin`。
4. 共用 `export` 只打開匯出能力；實際匯出資料仍由後端依可存取群組與領域條件篩選。
5. 前端隱藏不是安全邊界；RPC、RLS、Trigger 及 Edge Function 必須再次驗證共用權限或指定群組權限。
6. 舊 `access_role_groups`、`role.groupIds`、`applicableGroupIds` 與「角色適用群組」不得作為正式授權資料來源。

## 2.6 共用排班人員目錄

1. 所有有效登入者都可取得角色具有 `schedule_view` 的群組內人員工號、姓名、所屬單位、任職日期、計薪方式、排序及排班班別 ID。
2. 工號與排班班別屬班表共同顯示及排班所需資料，不另外要求人員管理權限。
3. 開放欄位不等於開放全公司資料；人員目錄仍由後端依 `schedule_view` 群組權限限制資料列。
4. 頁面入口與修改能力仍依功能權限控制；沒有目標群組 `schedule_manage` 者不得新增、編輯、刪除人員或變更排班班別。
5. 不得只靠前端隱藏頁面限制資料範圍，RPC、RLS 及 Edge Function 仍須執行群組與操作權限檢查。

---

# 三、登入、首頁與帳號

## 3.1 未登入

1. 登入介面使用全畫面。
2. 未登入不顯示班表、員工、打卡、加班與訂餐資料，也不載入簽到或權限資料。
3. 敏感資料不得先載入再以 CSS 隱藏。
4. Token 無效或 Session 逾時時，清除敏感畫面並重新登入。

## 3.2 首頁

首頁顯示使用者姓名、角色、簽到簿、班表、修改密碼及登出。

訂餐按鈕規則：

1. 人員所屬群組已啟用且勾選「可否訂餐」時顯示。
2. 群組未勾選、已停用或人員未設定群組時不顯示。
3. 隱藏按鈕之外，後端新增訂餐仍須驗證群組訂餐開關。
4. 關閉訂餐不刪除既有訂餐資料；訂餐管理者仍可查閱歷史。

## 3.3 任職與刪除

帳號有效期間為到職日 00:00 至離職日後第 5 日 23:59，以伺服器 `Asia/Taipei` 判定。

1. 人員不以 `is_active` 判斷任職。
2. 超過離職寬限期後不得使用受保護功能。
3. 人員仍有未封存班表時不可刪除。
4. 人員完全沒有班表、簽到、訂餐等需保留的歷史資料時，刪除必須實體刪除人員主檔與登入帳號。
5. 人員只有已封存班表或其他需保留的歷史資料時，保留人員主檔並設定 `deleted_at`，同時移除登入資格。
6. 刪除後不出現在人員頁、排班名錄及登入流程；需要保留的簽到、訂餐、稽核及封存快照仍保留。
7. 系統永遠保留至少一位具有最高管理能力的有效人員；「最高管理能力」以角色包含共用 `settings` 判定，不以 `admin` 文字角色判定。
8. `deleted_at` 一旦有值即立即失去所有受保護功能資格；即使 Auth Session、Access Token 或離職後 5 日寬限期尚未到期也不得再使用。

## 3.4 Session

1. 手機使用可跨關閉保存的登入狀態，48 小時滑動閒置期限。
2. 電腦、平板及觸控筆電使用分頁階段儲存，30 分鐘滑動閒置期限。
3. 裝置無法判斷時採電腦規則。
4. 閒置逾時後第一個受保護操作不得成功。

---

# 四、簽到簿

## 4.1 頁籤

簽到簿使用 Chrome 式頁籤：

1. 個人記錄：每位登入者查看自己的班表、打卡、時數、備註、異常、訂餐與審核狀態。
2. 簽到審核：具有各群組 `attendance_review` 權限者管理相應群組資料。

## 4.2 個人記錄

1. 預設最近 50 日，每頁 50 筆，日期預設降冪（新到舊）；點擊日期欄標題可切換升冪／降冪，切換後回到第 1 頁。
2. 日期條件變更立即查詢。
3. 欄位包含日期、班表圖示、班別、上下班時間與地點、上班時數、加班時數、異常、備註、訂餐及審核狀態。
4. 只有今日未審資料可修改上班時數、加班時數及備註。
5. 打卡、輸入或重繪期間必須保留視窗垂直位置及表格水平／垂直捲動位置。
6. 一般使用者只看地點摘要，不取得原始 GPS、IP、定位精準度、距離或完整 JSON。

## 4.3 今日打卡

1. 只有今日列顯示上班及下班打卡。
2. 每人每天最多一筆上班時間與一筆下班時間。
3. 沒有上班卡仍允許下班打卡。
4. 第一次成功時間為正式時間，重送不得覆寫。
5. 已審核資料不可再由員工修改。
6. 手機優先 GPS，只要定位座標距公司不超過 300 公尺即通過，不以 GPS 回報的定位精度數值作為阻擋條件；無法取得定位或距離超過 300 公尺時改驗證固定對外 IP。
7. 電腦及平板只驗證固定對外 IP。
8. GPS 與固定 IP 只能比對「本人所屬群組」內已啟用且未刪除的單位；其他群組即使位置或 IP 相符也不得通過打卡。
9. 已軟刪除或未設定所屬群組的人員不得打卡。

## 4.4 簽到審核

篩選列在結束日期後增加「群組」下拉選單：

```text
開始日期　結束日期　群組　人員　異常　狀態
```

1. 群組選項只顯示角色具有 `attendance_review` 的群組。
2. 「全部群組」只代表該角色具有 `attendance_review` 的群組全集。
3. 原班別欄改為「群組－單位」，例如「門市－大安」、「工廠－生產一課」。
4. 查詢、編輯、審核、退回、歷程與匯出都依 `attendance_review` 的逐群組權限驗證。
5. 管理異動寫入 `attendance_audit_logs`，保存操作人、原因、修改前及修改後快照。
6. `attendance-review-groups` 提供群組化簽到審核查詢與寫入橋接，不得只依前端篩選。
7. 日期、人員、異常與狀態等篩選欄位整齊排列，功能按鈕另列靠右排列。
8. 操作欄固定完整顯示編輯、審核／退回與歷程，三個 SVG 圖示固定同一列顯示。
9. 簽到審核匯出的正式唯一格式為 12 欄；一般加班時數採四碼 `HHMM`，例如 2.5 小時輸出 `0230`。
10. 若已審紀錄當日班表同時有「例假」或「休息日」與班別，該班別視為加班；此規則只適用簽到審核「匯出加班」，不改變班表頁「匯出加班」規則。若當日「加班時數」為空白或 0，匯出開始／結束時間直接使用班別上下班時間。
11. 上述「例假／休息日＋班別」若當日「加班時數」另有填值，該時數視為班別開始前的額外加班，匯出開始時間必須由班別開始時間向前扣除相同時數，結束時間仍使用班別結束時間；例如班別 `08:00-17:00`、加班 2 小時，匯出為 `06:00-17:00`。若開始時間向前跨過 `00:00`，匯出的前一天欄位設為 `1`。
12. 簽到審核清單讀取遇到 `502`、`503` 或 `504` 時，等待 300 毫秒後自動重試一次；編輯、審核、退回等寫入操作不得自動重送。
13. 單筆審核／退回及批次審核／退回，後端必須以集合式權限驗證、批次更新 `attendance_days`、批次寫入 `attendance_audit_logs`，不得逐筆串行執行同一組查詢。操作成功後前端直接依回傳結果局部更新目前簽到審核清單；只有目前頁碼因資料移出篩選結果而失效時才重新讀取該頁，不為每次審核額外重載完整個人記錄。
14. 簽到審核功能列提供「列印」。列印沿用目前篩選條件並載入全部符合資料，不受畫面分頁限制；固定使用 A4 直式且不增加額外頁首頁尾。列印欄位依序為日期、員工、圖示、班別、打卡時間、上班時數、加班時數、備註、異常、狀態，不列印勾選框與操作欄；每頁重複表頭，每個完整列印頁固定 40 筆，最後一頁依剩餘筆數顯示；版面應盡量沿用簽到審核頁的表格比例、文字對齊、班表圖示、表格線條與審核狀態樣式，只為滿足每頁至少 40 筆而縮小列高、間距與字級。
15. 日期預設降冪（新到舊）；點擊日期欄標題可切換升冪／降冪，排序必須在後端分頁前完成，切換後回到第 1 頁。
16. 「異常」欄正常紀錄保持空白；只有實際異常時才顯示異常文字，列印亦同。
17. 遲到寬限前 5 分鐘；自班別上班時間第 6 分鐘起才判定「遲到」。例如 09:00 班別，09:05 不算遲到，09:06 起算遲到。
18. 下班提早 30 分鐘（含）以內不判定「早退」；超過 30 分鐘才算早退。例如 18:00 下班，17:30 不算早退，17:29 算早退。

## 4.5 出勤資料模型

正式出勤只使用：

- `attendance_days`：每人每日一列，另保存群組 ID、群組名稱快照及單位名稱快照。
- `attendance_audit_logs`：管理異動完整稽核。

不建立舊打卡表、舊加班申請表或舊審核紀錄表。

---

# 五、訂餐

## 5.1 今日訂餐資格

1. 人員所屬群組勾選「可否訂餐」。
2. 今日已有上班打卡及明確打卡單位。
3. 尚未超過伺服器每日截止時間。
4. 不要求今日有班表。
5. 只處理今天，不處理未來日期。
6. 人員必須為未刪除且仍在有效帳號期間；軟刪除後即使仍持有舊 Session，也不可新增、修改或取消訂餐。

## 5.2 商品與訂單

1. 數量只接受 0 或正整數。
2. 第一次全部為 0 不建立訂單。
3. 已有訂單全部改為 0 時需確認後取消整張訂單。
4. 同一人、日期、商品唯一；同日多商品共用 `order_id`。
5. 保存商品名稱、單價、人員、單位及群組快照。
6. 停用品項對已訂者仍顯示，只能減少或取消。
7. 截止後停止新增、修改及取消。

## 5.3 訂餐管理

具有群組 `meal_admin` 權限者只能查看及操作相應群組的訂餐統計與設定資料。本人所屬群組未開放訂餐，不影響其管理其他已授權群組歷史資料。

---

# 六、班表、群組與設定

## 6.1 班表頁群組選單

在預設為「人員檢視」的下拉選單左側增加群組下拉選單：

```text
群組：[門市 ▼]　檢視：[人員檢視 ▼]
```

切換群組時：

1. 重新載入該群組的單位、人員、班別及班表。
2. 原單位篩選恢復預設值。
3. 清除目前班表選取範圍。
4. 復原、重做及剪貼簿狀態與其他群組隔離。
5. 記住使用者最後查看的群組。
6. 群組下拉選單只顯示角色具有 `schedule_view` 且啟用中的群組。

## 6.2 班表資料與操作

1. 預設顯示今天所在八週週期。
2. 檢視包含人員檢視、人員檢視－統計欄及班別檢視。
3. 每日格可同時保存班別、假別及班表加班，順序為班別、假別、加班。
4. 唯一限制為 `member_id + work_date`；空白格不保留資料列。
5. 每筆班表保存群組 ID，且必須與人員、支援單位及班別群組一致。
6. 現有複製貼上只使用鍵盤快速鍵，不新增「複製上週」或獨立複製按鈕。
7. 清空排班屬一般編輯，可由上一步復原，不顯示額外刪除確認。
8. 上一步、下一步最多保存最近 20 次操作，且不得跨群組作用。
9. 從一個群組複製的班表內容不得直接貼入不相符群組；後端仍需驗證。
10. 自動排班及批次操作只作用於目前群組。

## 6.3 功能選單

班表頁「功能」選單包含：

- 群組設定。
- 權限設定。
- 班表封存。
- 排班條件（位於「排班」子選單）。
- 列印班表。

「列印班表」規則：

1. 只有具有 `schedule_manage` 且為目前群組權限者可使用，權限規則與班表管理一致；若列印功能同時走正式匯出資料流，後端仍須驗證共用 `export`。
2. 點擊「列印班表」後先開啟日期區間視窗，欄位為開始日期、結束日期；預設帶入目前班表顯示的八週起訖日期。
3. 日期確認後才進入列印預覽；選定範圍若超出目前已載入八週，前端必須透過正式班表讀取 API 按需載入該日期區間，不得只列印目前記憶體已有格子。
4. 預覽與實際列印沿用目前班表「人員檢視」的表格結構與格內容；左側欄位固定依序為「單位、姓名」，同單位人員以單位欄合併顯示；班別、假別、加班文字與顏色以及單位、人員排序均沿用班表既有規則。
5. 預覽與實際列印紙張尺寸固定為 A4（直式 210×297mm、橫式 297×210mm），提供「自動、直式、橫式」；自動模式 14 日以內採直式，超過 14 日採橫式。直式每頁最多 14 個日期欄，橫式每頁最多 31 個日期欄；列印版面採緊湊邊界與列高，在維持班表格內容可辨識的前提下提高每頁可列印人數；日期或人員超出可列印範圍時由程式拆頁。
6. 人員跨頁時每頁都重新顯示「單位、姓名」與日期表頭；同一單位被切到下一頁時，下一頁必須再次顯示單位名稱。
7. 列印預覽不顯示班表的新增、編輯、拖曳、復原、篩選或其他操作元件；實際列印保留班別等背景顏色。列印紙張本體不額外顯示系統頁首、頁尾、群組標題、日期區間或頁碼，以保留最大班表空間。
8. 列印範圍只使用目前群組、選定日期區間內有效的人員與單位，並沿用目前班表的單位篩選範圍。

其他現有功能與排列保持原規格。

### 6.3.1 排班條件

1. 「排班條件」位於班表頁「功能 → 排班」子選單；只有具有 `schedule_manage` 的目前群組可看到及使用，不新增獨立權限。
2. 每筆條件固定綁定目前群組，不提供群組欄位；只能選擇目前群組人員。
3. 不設定條件名稱、啟用狀態、日期區間或優先權。欄位只有「條件類型、人員、限額、操作」。
4. 條件類型只有：
   - 「同班限制」：同一天、同一班別，條件有效人員最多只能安排到限額人數。
   - 「同休限制」：同一天，條件有效人員最多只能有「限額」人數被排任何假別；只要班表格有 `leave` 即算休，不區分假別、全天、半天或時間區間。
5. 新增／修改時至少選 2 人；限額必須為整數，且 `1 <= 限額 < 選取人數`。
   - 人員欄改用逐一追加的下拉選單：初始只顯示第一個選單；每選定一位人員後才顯示下一個空白選單；後續選單不得再列出已選人員。修改既有條件時依原 `member_ids` 順序還原，尚有可選人員時最後保留一個空白選單。
6. 所有排班條件只約束「自動排班」及「自動補班」，屬硬性條件；人工點選、拖曳、複製貼上、批次修改等人工班表操作完全不檢查、不阻擋、不提示排班條件。
7. 自動排班必須把既有人工班別與假別一併計入限額；既有人工班表若已違反條件，系統不得自行刪除或改動，且自動排班不得讓違反程度增加。
8. 條件資料使用單表 `schedule_conditions`，人員清單直接保存 `member_ids uuid[]`，不建立條件人員明細表。
9. 人員刪除或換群組時不即時掃描、清理條件；讀取時只把「仍存在、未刪除且仍屬該群組」的人員視為有效人員，其餘 ID 直接忽略。
10. 有效人員少於 2 人，或限額已大於等於有效人員數時，該條件目前不套用但資料仍保留，設定畫面顯示「目前未生效」，管理者可自行修改或刪除。
11. 沒有任何有效「同班限制」時，自動排班沿用原最小成本流演算法與既有班別順位；只有存在有效同班限制時才啟用條件式搜尋，以降低額外運算負擔。
12. 自動排班因硬性條件造成班別缺額或休假無法安排時保留缺額並列入預覽提醒，不得為完成排班而突破限額。
13. 前端隱藏按鈕不是安全邊界；排班條件讀取、新增、修改及刪除的 RPC 都必須再次驗證目前群組 `schedule_manage`。

## 6.4 群組設定頁

版面與單位設定表格一致。欄位依序為：

| 欄位 | 規則 |
|---|---|
| 拖曳排序 | 每列最前方，只有把手可拖曳 |
| 群組代碼 | 不可重複，使用英文字母、數字或底線；欄寬採緊湊顯示 |
| 群組名稱 | 不可空白；欄寬採緊湊顯示 |
| 單位 | 單位欄為主要寬欄；每個單位使用獨立膠囊標籤顯示，空間不足時自動換行 |
| 可否訂餐 | 勾選後該群組人員才看到首頁訂餐按鈕；欄寬採緊湊顯示 |
| 狀態 | 啟用或停用；欄寬採緊湊顯示 |
| 操作 | 編輯、停用／啟用、刪除，按鈕維持同列 |

群組設定彈窗應優先提供單位欄可用寬度，群組代碼、群組名稱、可否訂餐、狀態與操作欄只保留足夠內容的固定寬度。單位膠囊標籤使用細框、圓角及單行文字，標籤之間保留間距並可多列排列。

不提供複製功能。群組設定頁本身由共用 `settings` 控制；此權限允許管理群組主檔，但不自動授予該群組其他功能權限。

停用群組：

1. 一般班表群組選單不顯示。
2. 不可新增排班或將新單位、人員指定至該群組。
3. 歷史與封存資料保留，可由管理頁重新啟用。

刪除群組：

1. 不限制是否已有相關資料，但必須列出影響筆數並要求輸入群組名稱確認。
2. 未封存班表及現行群組關聯資料一併清除或邏輯刪除。
3. 人員主檔保留，清除其群組、單位及排班班別關聯。
4. 已封存班表、簽到、稽核及必要歷史快照保留。
5. 群組使用邏輯刪除；封存資料的必要授權／快照保留，供歷史查看判斷。
6. 整個操作使用資料庫交易，任一步失敗全部復原。

## 6.5 單位與班別

1. 單位只能屬於一個群組。
2. 從目前班表群組開啟「單位設定」並新增單位時，新增表單不顯示群組欄位；系統自動以開啟時的目前群組作為單位所屬群組。
3. 單位設定要求該群組 `department_settings`；班別設定要求該群組 `schedule_manage`。
4. 有人員、未封存班表或現行班別關聯時，不允許直接跨群組移動。
5. 單位或班別完全沒有班表或其他需保留的關聯資料時，刪除採實體刪除；若只有已封存班表或其他歷史關聯，則保留主檔並設定 `deleted_at`。
6. 單位或班別仍有未封存班表時不可刪除；只有封存班表歷史時才允許刪除。
7. 班別綁定單位，群組由單位自動同步。
8. 已刪除單位、班別不再出現在設定頁及新增選單；解除舊班表封存後若仍被歷史排班引用，必須可讀取並顯示「（已刪除）」。
9. 已刪除單位或班別不得重新指定給新資料；原有歷史引用未變更時可保留。
10. 班別、單位或群組改名只影響未封存及之後資料。

## 6.6 假別與加班設定

1. 假別為全公司共用，不增加群組欄位。
2. 加班設定為全公司共用，目前功能保持隱藏。
3. 假別或加班設定皆使用共用 `leave_settings`，權限頁不另列加班設定。
4. 假別或加班設定完全沒有任何班表引用時，刪除採實體刪除；若只有已封存班表引用，則保留主檔並設定 `deleted_at`。
5. 仍有未封存班表引用時不可刪除；只有封存班表歷史時才允許刪除。
6. 已刪除項目不出現在設定頁及新增選單；解除舊班表封存後若被歷史資料引用，仍可讀取並顯示「（已刪除）」，但不得重新選用。

---

# 七、班表封存

## 7.1 封存範圍

封存以「群組＋開始日期＋結束日期」為單位。

1. 封存前顯示群組、日期範圍、人員數及資料筆數。
2. 同一群組的封存日期範圍不可重疊。
3. 封存後不可新增、修改、清空、貼上、批次操作、復原或重做。
4. 班表畫面以唯讀狀態顯示封存範圍，不重複跳出提示。
5. 封存資料可查看及匯出。
6. 具有該群組 `schedule_manage` 者，可從封存清單執行「解除封存」。
7. 解除封存後，該日期範圍恢復一般班表，可修改後再次封存。
8. 解除封存不另留解除紀錄；解除時移除該次封存鎖定與快照。
9. 群組已刪除時不可解除封存。

## 7.2 快照資料

封存不能只在原班表增加 `is_archived`，必須建立獨立快照：

- `schedule_archives`：群組代碼與名稱快照、日期範圍、封存人、封存時間及筆數。
- `schedule_archive_entries`：人員工號與姓名、單位、班別、上下班時間、顏色、假別、加班、日期、排序及班表內容快照。

封存畫面只讀快照，不重新查詢現行名稱。因此人員、單位、班別或群組之後改名、調動、停用或刪除，都不影響封存班表。

## 7.3 封存與刪除

1. 人員、單位、班別、假別及加班設定仍有未封存班表時不可刪除。
2. 所有相關班表均已封存後，若主檔仍被封存或其他歷史資料引用，保留主檔並設定 `deleted_at`；若完全沒有任何需保留的歷史引用，則可實體刪除。不得實體刪除仍被歷史資料引用的主檔。
3. 封存明細保留原始 ID，同時保存完整顯示快照。
4. 被刪除的人員、單位、班別、假別或加班設定不再出現在現行設定及新增選單，但封存班表仍使用快照正常顯示。
5. 已刪除群組的封存班表仍依正式保存的群組權限／封存授權資料判斷查看權限，不使用已淘汰的 `access_role_groups`。
6. 解除封存後，若舊班表引用已刪除主檔，畫面仍保留原排班並標示「（已刪除）」。
7. 已刪除人員的舊排班只能清除，不得新增、換班或重新指定；已刪除單位、班別、假別及加班設定不得重新選用。
8. 修改完成後可再次封存；解除封存本身不建立另外的歷程紀錄。

---

# 八、介面共用規格

## 8.1 手機優先

1. 寬度不超過 640px 時，上方與左右安全間距為 8px。
2. 內容由畫面上方開始，不垂直置中留下大面積空白。
3. 最外層卡片移除背景、邊框、圓角、陰影及額外內距；內部功能卡片保留。
4. 641px 至 960px 的窄螢幕，上方外距為 12px。
5. 表格過寬只在表格容器內水平捲動，不造成整頁水平捲動。
6. 主要觸控區建議不小於 44×44 CSS px。

## 8.2 共用元件

1. 一般控制項高度 40px，手機至少 42px。
2. 一般控制項圓角 12px，表格 14px，內容區 18px，主卡片及彈窗 24px。
3. 輸入框、日期、時間及下拉選單同列時等高。
4. 圖示按鈕提供 `title` 或 `aria-label`。
5. 狀態不得只靠顏色表示。
6. Chrome 式頁籤固定單行，不足寬度時只讓頁籤列水平捲動。

## 8.3 班表既有樣式

1. 頂端單位下拉選單右側保留上一步、下一步圖示。
2. 浮動工具列收合按鈕加大並醒目。
3. 浮動班表加班區塊預設隱藏，功能保留。
4. 日期欄週六日文字與平日相同使用黑色。
5. 前一週、後一週只水平捲動 7 個日期欄，不變更目前八週資料範圍；不得重新載入或切換整段八週資料。只有「前八週」、「後八週」切換完整八週期間。
6. 例假同日有排班時只將例假小區塊改黃色，不改整格。

## 8.4 共通狀態

所有頁面處理載入、背景更新、空資料、無權限、讀取失敗、儲存中、儲存成功、儲存失敗、Session 失效、資料衝突及網路離線。

1. 讀取失敗不得顯示為空資料。
2. 儲存失敗保留輸入與目前畫面。
3. 寫入結果不明時先重讀正式資料，再決定是否重送。

---

# 九、後端、安全與資料規則

## 9.1 架構分工

- 前端：畫面、輸入、一般格式提示及 API 呼叫。
- Edge Function：驗證 Token、角色、共用權限、逐群組權限、有效期間、伺服器時間、位置及業務規則。
- PostgreSQL／RLS／RPC：正式資料、群組隔離、限制、交易、冪等性、封存及稽核。

前端不得保存 `service_role`、資料庫密碼或其他高權限 Secret。

## 9.2 群組資料安全

1. `set_departments`、`set_employee`、`set_shift` 及 `schedule_entries` 的 RLS 依相應逐群組權限限制。
2. 班表寫入 Trigger 強制人員、班別及支援單位群組一致。
3. 班表封存 Trigger 同時檢查更新前及更新後日期與群組，禁止把封存資料移出封存範圍。
4. 訂餐管理只能讀取角色具有 `meal_admin` 的群組訂單；一般使用者只能讀取本人訂單。
5. 安全限制不得只實作在前端。
6. RLS、Trigger、RPC 與 Edge Function 必須使用同一組共用權限／群組權限規則；不得由資料庫第二層防護退回 `admin/manager` 文字角色或舊適用群組判斷。
7. 本人功能也必須檢查 `deleted_at`；軟刪除後的舊 Token 不得成為繞過帳號刪除的途徑。

## 9.3 頁面與資料權限矩陣

頁面入口、API、RPC、RLS、Trigger 與 Edge Function 必須套用同一份正式權限定義：

| 頁面／資料 | 正式權限 | 群組範圍 |
|---|---|---|
| 首頁與個人記錄 | 所有有效登入者查看本人資料 | 本人資料 |
| 班表與共用排班人員目錄 | `schedule_view` | 權限所在群組 |
| 班表編輯、班別、排班條件、人員設定／帳號管理 | `schedule_manage` | 權限所在群組 |
| 單位設定 | `department_settings` | 權限所在群組 |
| 群組設定、權限設定 | 共用 `settings` | 公司共用設定；可管理全部群組／角色，但不自動取得班表等群組權限 |
| 假別與隱藏加班設定 | 共用 `leave_settings` | 公司共用 |
| 班表等正式匯出 | 共用 `export` | 資料列仍依後端可存取群組與領域條件限制 |
| 簽到審核 | `attendance_review` | 權限所在群組 |
| 今日訂餐 | 本人群組訂餐開關及今日打卡資格 | 本人群組 |
| 訂餐統計與設定 | `meal_admin` | 權限所在群組 |
| 封存班表查看 | `schedule_view` | 權限所在群組／正式封存授權 |
| 封存、解除封存與班表管理 | `schedule_manage` | 權限所在群組 |

共用權限不等於全資料存取權；群組權限也不互相包含，只有 `schedule_manage` 必須連動 `schedule_view`。

## 9.4 正式 SQL

全新資料庫固定依序執行：

```text
1. supabase/001_current_schema.sql
2. supabase/002_current_updates.sql
```

`001_current_schema.sql` 建立正式基礎模型；`002_current_updates.sql` 建立仍有效且可重複執行的群組、角色權限、封存、RLS、Trigger 及 RPC 更新。SQL Editor 任何區段失敗時立即停止，不可略過。

## 9.5 權限與資料存取架構

正式系統只保留最新版權限模型與單一路徑：

1. 瀏覽器不得直接對核心資料表執行 `SELECT / INSERT / UPDATE / DELETE`；正式讀寫走具名 RPC 或對應 Edge Function。
2. 人員角色只以 `set_employee.access_role_id` 指向 `access_roles`。
3. 共用權限唯一保存於 `access_roles.common_permissions text[]`；正式鍵值為 `settings`、`export`、`leave_settings`。
4. 群組權限唯一保存於 `access_role_group_permissions(role_id, group_id, permissions)`；正式鍵值為 `schedule_view`、`schedule_manage`、`department_settings`、`attendance_review`、`meal_admin`。
5. `schedule_manage` 必須包含 `schedule_view`；其他群組權限彼此獨立。
6. 舊 `access_roles.permissions`、`access_role_groups`、`role.groupIds`、`applicableGroupIds`、`member_settings`、`permission_settings`、`set_employee.role` 與 `access_roles.legacy_role` 均不得作為正式授權來源；已淘汰的資料表／欄位／RPC 必須從正式 SQL 移除。
7. 資料庫權限 helper 使用 `has_common_permission`、`has_group_permission`、`has_any_group_permission` 與必要的集合式群組判斷；不得退回 `is_manager()`、`is_admin()`、`has_access_permission`、`can_access_group` 或舊角色字串判斷。
8. 高頻 RPC 必須先一次解析 actor 的 `access_role_id` 與群組權限，再以 JOIN／EXISTS 集合式篩選，不得對每一列資料重複查角色。
9. `anon` 與 `authenticated` 不取得核心資料表直接 CRUD GRANT；RLS 保留為第二層防護，正式寫入仍走具名 RPC／Edge Function。
10. `access-control` 是瀏覽器權限 bundle 與角色權限管理的唯一 Edge Function。正式 action 為 `bundle`、`saveRole`、`deleteRole`、`reorderRoles`；`saveRole`／`deleteRole`／`reorderRoles` 均要求共用 `settings`。
11. `access-control bundle` 回傳 actor 的 `commonPermissions`、逐群組 `groupPermissions`、可見群組與可見角色；具有共用 `settings` 時可取得全部群組／角色供設定管理，否則只回傳實際有群組權限的群組及必要角色資料。
12. 舊瀏覽器權限 RPC，例如 `get_group_access_bundle_v1`、`save_access_role_v1`、`delete_access_role_v1`，不得與 `access-control` 並存為第二套權限寫入路徑。
13. 人員登入帳號新增、修改、重設密碼與刪除統一由 `member-auth-admin` 處理；人員設定以目標人員所屬群組的 `schedule_manage` 驗證。
14. 單位設定使用該群組的 `department_settings`；班表、班別、排班條件使用該群組的 `schedule_manage`；假別使用共用 `leave_settings`；正式匯出使用共用 `export`；簽到與訂餐管理分別使用 `attendance_review` 與 `meal_admin`。
15. 本人 Edge Function 必須同時驗證任職期間及 `deleted_at`；涉及單位、群組的操作必須先把候選資料限制在本人所屬群組或已授權群組內，再做 GPS、IP、日期或其他業務判斷。
16. 同一資料完整性規則只保留一個正式 Trigger；不得同時保留舊版與新版最後管理者保護或重複 `updated_at` Trigger。
17. 「最後一位最高管理者」以有效、未刪除且角色具有共用 `settings` 的人員判定，不以 `admin` 角色名稱判定。

目前唯一正式 Edge Functions：

- `access-control`：權限 bundle、角色共用權限／逐群組權限儲存、角色刪除與排序。
- `member-auth-admin`：人員登入帳號新增、修改、密碼重設與刪除。
- `attendance-clock`：本人打卡。
- `attendance-ledger`：本人簽到簿資料。
- `attendance-review-groups`：逐群組簽到審核、編輯、批次操作與歷程。
- `attendance-ledger-export`：依 `attendance_review` 群組權限匯出已審簽到資料。
- `meal-order`：本人訂餐與具 `meal_admin` 群組管理操作。
- `meal-report-v2`：具 `meal_admin` 群組權限的訂餐統計報表。
- `meal-cancel-v2`：本人訂餐取消。

`supabase/functions/`、`scripts/deploy-edge-functions.ps1`、正式 CI 的 Deno 檢查清單與 Supabase 遠端部署清單必須一致。`_shared/runtime.ts` 是共用任職、UUID、台北日期及共用／群組權限 helper；修改 `_shared` 後，所有依賴它的正式 Edge Function 都必須重新部署，不能只部署直接顯示為變更的資料夾。

## 9.6 交易與冪等

下列操作不得因重送產生重複資料或改寫第一次有效結果：

- 上班及下班打卡。
- 每日簽到修改與批次審核。
- 訂單新增、修改及取消。
- 班表批次保存。
- 群組排序及角色群組權限更新。
- 班表封存。

下列操作使用交易：

- 打卡與稽核。
- 管理員修改與稽核。
- 訂單新增、修改及整張取消。
- 群組刪除。
- 角色更新及逐群組權限重建。
- 班表封存主檔與全部快照明細。

## 9.7 備份與復原目標

1. 正式資料庫至少每日自動備份一次，並定期驗證可還原性。
2. RPO（可接受資料遺失上限）為 24 小時；涉及人員、班表、簽到或訂餐的大量異動前應額外建立可還原備份。
3. RTO（服務中斷後的復原目標）為 8 小時；資料庫故障、錯誤部署或重大資料異常均以恢復核心登入、班表、簽到及訂餐功能為優先。
4. 復原後必須核對角色權限、群組隔離、封存快照、班表筆數、簽到筆數與訂餐金額。

## 9.8 頁面資料按需載入

1. 登入階段只取得 Session、本人基本資料、角色共用權限、所屬群組、逐群組權限與首頁必要的訂餐開關；不得預載完整班表、簽到審核、訂餐統計或管理設定。
2. 第一次進入班表時，先讀 `scheduler_settings` 的正式週期設定，再以 `eight_week_start_date` 與今天計算今天所在的 56 天週期；首次畫面不得使用或相容舊的 `schedule_start_date` 瀏覽位置。
3. 確定八週起點後，單位目錄、人員目錄、班別、假別、加班設定、假日、人員排序與該八週含 7 日緩衝的班表資料平行讀取；同一首次載入不得再對同一可視區間補查第二次 `schedule_entries`。
4. 首頁已取得的 `access-control bundle` 在進入班表時直接沿用；班表只補載必要實體資料，不重複讀取相同權限 bundle。
5. 單位地址、GPS、固定 IP 與打卡開關只在管理者真正進入「單位設定」時載入，不列入班表首次載入。
6. 人員完整管理欄位只在真正進入「人員設定」時載入；共用班表人員目錄仍只提供班表需要的正式欄位。
7. 簽到簿只載入目前頁籤；預設個人記錄不預載簽到審核。訂餐今日、統計與設定各自於進入該頁籤時載入。
8. 同一登入階段已成功載入的頁面資料可在記憶體沿用；資料寫入後只失效或刷新相關資料範圍。登出或 Session 失效時清除全部頁面快取。
9. 按需載入是效能與資料最小化機制，不取代安全控制；RPC、RLS 與 Edge Function 仍必須驗證共用權限及指定群組權限。
10. 第一次進入班表且資料尚未完成時，畫面中央顯示圓形載入動畫；成功或失敗後自動收起。

# 十、發布與測試

## 10.1 發布流程

1. 修改正式來源並執行 `npm run web:publish`。
2. 在工作分支執行本機／臨時驗證後清除所有臨時 workflow 或臨時檔；正式分支只保留 `.github/workflows/deploy-pages.yml`。
3. 建立以 `main` 為 base 的正式 PR，讓唯一正式 `Validate Web App` workflow 對 PR 的最終 HEAD 執行完整 CI。
4. PR CI 全綠後，依正式資料庫程序套用本次 SQL，任何 SQL 失敗立即停止後續部署。
5. 部署 `scripts/deploy-edge-functions.ps1` 列出的正式 Edge Functions；若 `_shared` 有修改，必須重部署所有依賴函式。
6. 完成正式環境抽測後，以 **Squash merge** 合併 PR 到 `main`。
7. 確認 `main` 的 `Validate Web App` CI 成功，並確認 GitHub Pages `pages-build-deployment` 對同一個 `main` 提交部署成功。
8. 以員工、主管、管理員與自訂角色抽測登入、班表、群組隔離、簽到簿、封存、匯出及訂餐主要流程。

## 10.2 必要測試

### 群組與權限

- 班表群組下拉選單只顯示角色具有 `schedule_view` 的群組。
- 人員所屬群組只連動單位及班別，不影響角色。
- 共用人員目錄對所有有效登入者回傳工號及排班班別，且資料列不超出 `schedule_view` 群組。
- `access-control bundle` 正確回傳 `commonPermissions` 與逐群組 `groupPermissions`。
- `settings`、`export`、`leave_settings` 只存在於共用權限；`schedule_view`、`schedule_manage`、`department_settings`、`attendance_review`、`meal_admin` 只存在於逐群組權限。
- 班表查看與管理連動正確；`schedule_manage` 必須同時具 `schedule_view`。
- 群組設定及權限設定只由共用 `settings` 控制；具有 `settings` 不應自動取得任何群組班表權限。
- 權限角色拖曳排序完成後立即持久化，重新開啟仍維持排序；人員新增／修改的「權限」下拉選單使用同一排序。
- 只具有指定群組權限的角色無法從 REST、RPC 或 Edge Function 存取未授權群組。
- 群組訂餐關閉後首頁按鈕及後端新增訂餐都被禁止。
- RLS、Trigger 與 RPC 不得使用 `admin/manager`、`legacy_role`、舊 `access_role_groups` 或泛化 `is_manager/is_admin` 作為實際授權來源。
- 正式 SQL 必須移除 `access_roles.permissions` 與 `access_role_groups`，並建立 `access_roles.common_permissions` 與 `access_role_group_permissions`。
- `anon`／`authenticated` 對核心資料表直接 GRANT 為零；內部 helper 與 Trigger function 不開放為瀏覽器 RPC。
- 最後一名有效共用 `settings` 帳號不得因角色或人員異動而被移除。

### 班表與封存

- 不同群組的班表、復原、重做及剪貼簿互不影響。
- 人員、班別及支援單位群組不一致時，資料庫拒絕寫入。
- 封存範圍不可重疊。
- 封存日期不可新增、修改、移動或刪除班表。
- 人員、單位、班別及群組改名或刪除後，封存畫面仍顯示原快照。
- 未封存班表阻止人員或單位調動及刪除；只有封存歷史時允許邏輯刪除。
- 人員、單位、班別、假別及加班設定在完全沒有需保留歷史資料時採實體刪除；仍有封存或其他歷史引用時不得實體刪除。
- 班表頁「功能 → 列印班表」必須先顯示日期區間，預設帶入目前八週；只有目前群組 `schedule_manage` 可使用。
- 列印預覽左側固定為「單位、姓名」，班別／假別／加班內容與顏色沿用班表；日期與人員超出單頁時正確拆頁，跨頁後仍重複表頭與單位名稱。
- 列印日期超出目前已載入八週時，必須按需讀取選定日期資料，不得出現只因快取範圍不足而空白的班表。

### 簽到與訂餐

- 個人只能查看自己的簽到資料。
- 簽到審核只顯示具有 `attendance_review` 的群組。
- 「群組－單位」顯示正確。
- 簽到審核「匯出加班」遇到已審的例假／休息日且有排班時，以該班別上下班時間匯出加班；班表頁「匯出加班」不得套用此規則。
- 訂餐統計及設定只顯示具有 `meal_admin` 的群組。
- 軟刪除人員即使仍持有有效 Session，也無法打卡、讀寫個人簽到、訂餐或取消訂餐。
- 門市群組人員不得使用工廠群組單位的 GPS 或固定 IP 打卡，反之亦同。
- `attendance_days` 只有一個正式 `updated_at` Trigger，不得重複觸發同一功能。

### 資料載入生命週期

- 登入完成只取得身分、共用權限、逐群組權限及首頁必要群組資料，不呼叫完整班表、人員管理、簽到審核或訂餐管理資料。
- 第一次點班表才讀取完整班表資料；同一登入 Session 再次進入班表不因首頁切換而重新初始化。
- 進入簽到簿預設只查個人記錄；未點「簽到審核」前不得發出審核資料請求。
- 沒有 `attendance_review`、`meal_admin`、`schedule_manage` 等相應群組權限時，不發出對應管理頁的資料請求。
- 登出或 Session 失效後，前一位使用者已載入的頁面資料與載入旗標全部清除。
- 即使前端未顯示或未呼叫功能，直接呼叫 RPC／Edge Function 仍會依 RLS、共用權限與逐群組權限拒絕未授權操作。

### 介面與建置

- 手機首頁、簽到簿、班表及訂餐上方間距一致。
- 群組設定的單位欄最寬，各單位以可換行膠囊標籤顯示。
- 權限設定的共用權限與群組權限／權限項目欄為主要寬欄，各權限以可換行膠囊標籤顯示。
- 第一次載入班表期間顯示圓形載入動畫，完成或失敗後消失。
- 週六日標題字色為黑色。
- 前一週、後一週不跨八週週期。
- `src/renderer/`、bundle 與 `docs/` 完全同步。
- 正式 SQL、Edge Function 資料夾、部署清單、README 與本規格互相一致。
- 正式 CI 必須對 `access-control` 及其他所有正式 Edge Function 執行 Deno type check。

## 10.3 發布門檻

1. 自動檢查全部通過。
2. 群組、角色、`common_permissions`、`access_role_group_permissions`、封存表及 RPC 已存在於正式 SQL。
3. 資料庫群組一致性檢查為零筆異常。
4. Edge Function 正式部署清單完整，並包含 `access-control`。
5. 員工、主管、管理員及自訂角色完成跨群組權限抽測。
6. 高風險操作完成重複送出、交易失敗及回滾驗證。
7. PR CI、`main` CI 與 GitHub Pages 部署均成功。

### 效能與載入規範（2026-08-08）

1. 登入後首頁只取得身分與權限摘要，不載入班表、人員管理完整資料、簽到審核或 Excel 匯出套件。
2. 第一次進班表才取得班表 bootstrap 與目前週期資料；具有任何管理權限也不得因此預載人員管理目錄，人員完整管理資料只在實際開啟人員設定時載入。
3. 個人簽到簿只取得個人記錄；簽到審核在使用者實際切換到審核分頁時才載入，載入成功後同一 Session 可沿用快取，篩選／異動時才重查。
4. ExcelJS 僅在 XLSX 匯入或匯出時動態載入；CSV 匯出不得因此載入 ExcelJS。
5. 班表讀寫 RPC 必須一次解析 actor 的 `access_role_id` 與逐群組權限，再使用 JOIN／EXISTS 做集合式驗證；不得對每筆 `schedule_entries` 反覆查 `set_employee`、`access_roles`、`access_role_group_permissions`。
6. 核心表對 `anon/authenticated` 不提供直接 CRUD GRANT；正式寫入只走具名 RPC／Edge Function，因此 authenticated 不保留直接 INSERT/UPDATE/DELETE RLS policy。
7. RLS 唯讀 policy 中的 `auth.uid()/auth.jwt()` 應採 init-plan 寫法，並避免同一 role/action 疊加多個 permissive policy。
8. DDL、權限或高頻 RPC 調整後需檢查 Performance Advisor 與 `pg_stat_statements`；不可只以目前資料量小作為忽略效能問題的理由。

### Canonical 程式與資料模型簡化規範（2026-08-08）

1. 權限模型唯一來源為 `set_employee.access_role_id`、`access_roles.common_permissions` 與 `access_role_group_permissions.permissions`；資料庫與前端不得保留文字角色或舊適用群組相容欄位作為授權或顯示來源。
2. 班表 bootstrap 直接提供 `groupId`、`roleId`、`deleted` 等正式欄位；封存日期範圍以 `archiveRanges` 獨立提供，不建立重複 entity map。
3. 前端正式排班資料的人員主鍵必須為 UUID；遇到非 UUID 視為資料錯誤，不再以工號反查或猜測主鍵。
4. 被軟刪除但仍由未封存班表引用的主檔，後端必須明確回傳該歷史項目；若 ID 真正不存在，前端不得改套其他假別／加班／班別。
5. Edge Function 的台北日期、離職後五日帳號有效期、UUID 與共用／群組權限判斷採單一 `_shared` 實作，避免各功能規則漂移。
6. Excel／XLSX 的建立與格式全部由 exporter 管理；API transport 不直接操作 ExcelJS workbook。
7. SQL 正式來源只保留最後有效的 RLS policy；核心表 browser 寫入只走具名 RPC／Edge Function。
8. 權限管理只走 `access-control`，不保留第二套瀏覽器權限 RPC 或舊角色群組關聯表。

### 班表儲存一致性（2026-08-09）
- 空白格新增班別、假別或加班時，必須以正式班表 RPC 寫入資料庫；HTTP 成功但未回傳對應寫入列視為儲存失敗。
- 班表刪除只能由前端明確送出 `delete_entry=true`；不得因識別碼無效、資料轉換失敗或空 payload 自動推論為刪除。
- 直接修改單一班表格若伺服器未確認寫入，畫面必須立即回復該格修改前狀態，不得留下重整後才消失的假成功資料。

### 班表八週資料載入（2026-08-09）
- 班表頁一次只載入目前顯示的 8 週，共 56 天；不得額外預載上一週或下一週班表。
- 目前 56 天內的班表資料筆數不得受單次 REST／RPC 回傳列數上限截斷；`get_schedule_entries_v3` 必須以 `p_offset`／`p_limit` 明確分頁，每頁最多 1,000 筆，前端持續讀取直到不足一頁為止；不得依賴 HTTP Range header 作為 RPC 翻頁機制。
- 「前一週／後一週」只移動目前 8 週畫面的水平捲軸，不重新讀取班表。
- 「前八週／後八週」切換到另一個 56 天範圍時，才按需讀取該 8 週全部班表；已載入區間可保留快取，但不得預讀尚未切換的下一個 8 週。

### 假別停用後同代碼重新新增（2026-08-10）
- 假別代碼為唯一識別語意；啟用中的相同代碼不得重複新增。
- 新增假別時若代碼對應到已停用假別，系統須重新啟用原假別資料，不建立第二筆資料。
- 重新啟用時保留原假別 ID，並以本次新增表單的名稱、顏色、文字顏色、是否需填時間、是否需填原因、不顯示與排序設定覆寫原設定。
- 重新啟用後 `deleted_at` 清為 NULL；舊班表仍維持原假別 ID 關聯。

### 主檔刪除與歷史保留規則（2026-08-10）

- 人員若沒有班表、簽到或訂餐等需要保留的歷史資料，刪除時必須連同登入帳號實體刪除；若只有已封存班表或其他歷史資料，則保留主檔並標記刪除。
- 班別、假別、加班設定若完全沒有任何班表引用，刪除時直接從主檔實體刪除；若只有已封存班表引用，則保留主檔並標記刪除。
- 單位在沒有現職人員、沒有仍使用中的班別，且沒有班表或其他需保留的關聯資料時，刪除時直接從主檔實體刪除；若只有已封存班表或其他歷史關聯，則保留主檔並標記刪除。
- 任何主檔只要仍有未封存班表引用，刪除操作必須拒絕，提示先封存或清除相關班表。
