name: Temporary Attendance Review Pagination Rest Break V2

on:
  push:
    branches:
      - agent/attendance-review-pagination-rest-break-v2

permissions:
  contents: write

concurrency:
  group: temp-attendance-review-pagination-rest-break-v2
  cancel-in-progress: true

jobs:
  update:
    if: github.event.head_commit.message != 'Fix attendance review paging and overtime rest break'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/attendance-review-pagination-rest-break-v2
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - name: Apply source changes
        run: python3 scripts/temp-attendance-review-pagination-rest-break-v2.py
      - name: Install
        run: npm ci
      - name: Build static web
        run: npm run web:publish
      - name: Full validation
        shell: bash
        run: |
          mkdir -p .tmp-runner
          mv .github/workflows/temp-attendance-review-pagination-rest-break-v2.yml .tmp-runner/temp-workflow.yml
          mv scripts/temp-attendance-review-pagination-rest-break-v2.py .tmp-runner/temp-updater.py
          trap 'mv .tmp-runner/temp-workflow.yml .github/workflows/temp-attendance-review-pagination-rest-break-v2.yml 2>/dev/null || true; mv .tmp-runner/temp-updater.py scripts/temp-attendance-review-pagination-rest-break-v2.py 2>/dev/null || true' EXIT
          npm run ci:check
          mv .tmp-runner/temp-workflow.yml .github/workflows/temp-attendance-review-pagination-rest-break-v2.yml
          mv .tmp-runner/temp-updater.py scripts/temp-attendance-review-pagination-rest-break-v2.py
          rmdir .tmp-runner
          trap - EXIT
      - name: Commit source and generated files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/renderer docs tests
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Fix attendance review paging and overtime rest break"
          git push origin HEAD:agent/attendance-review-pagination-rest-break-v2
