name: Prepare Auth Request Split

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  prepare:
    if: github.actor != 'github-actions[bot]' && github.head_ref == 'agent/split-auth-request-helpers'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout branch
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: agent/split-auth-request-helpers

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Split auth and request helpers
        run: node scripts/split-auth-request-helpers.js

      - name: Build published web files
        run: npm run web:publish

      - name: Run complete checks without temporary workflow
        run: |
          set -o pipefail
          cp .github/workflows/prepare-auth-request-split.yml /tmp/prepare-auth-request-split.yml
          rm .github/workflows/prepare-auth-request-split.yml
          npm run ci:check 2>&1 | tee auth-request-split-ci.log
          cp /tmp/prepare-auth-request-split.yml .github/workflows/prepare-auth-request-split.yml

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: auth-request-split-ci-log
          path: auth-request-split-ci.log
          if-no-files-found: ignore

      - name: Commit split modules
        run: |
          rm -f scripts/split-auth-request-helpers.js
          rm -f auth-request-split-ci.log
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "拆分登入權限與申請共用工具"
          git push origin HEAD:agent/split-auth-request-helpers
