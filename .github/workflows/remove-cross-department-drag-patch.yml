name: Remove Cross Department Drag Patch

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  refactor:
    if: github.head_ref == 'agent/remove-cross-department-drag-patch'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout branch
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Merge cross-department drag patch
        run: node scripts/remove-cross-department-drag-patch.js

      - name: Build static web
        run: npm run web:publish

      - name: Run complete checks
        run: npm run ci:check

      - name: Commit canonical cross-department drag
        run: |
          rm -f scripts/remove-cross-department-drag-patch.js
          rm -f .github/workflows/remove-cross-department-drag-patch.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "整併跨單位人員拖曳補丁"
          git push origin HEAD:agent/remove-cross-department-drag-patch
