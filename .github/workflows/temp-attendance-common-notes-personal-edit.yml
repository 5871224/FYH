name: Temporary Attendance Common Notes Personal Edit Runner

on:
  push:
    branches:
      - agent/attendance-common-notes-personal-edit

permissions:
  contents: write

concurrency:
  group: temp-attendance-common-notes-personal-edit
  cancel-in-progress: true

jobs:
  update:
    if: github.event.head_commit.message != 'Implement attendance common notes and personal editing'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          ref: agent/attendance-common-notes-personal-edit
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - name: Apply source changes
        run: python3 scripts/temp-attendance-common-notes-personal-edit-v2.py
      - name: Install
        run: npm ci
      - name: Build static web
        run: npm run web:publish
      - name: Full validation
        shell: bash
        run: |
          mkdir -p .tmp-runner
          mv .github/workflows/temp-attendance-common-notes-personal-edit.yml .tmp-runner/temp-workflow.yml
          mv scripts/temp-attendance-common-notes-personal-edit.py .tmp-runner/temp-updater.py
          mv scripts/temp-attendance-common-notes-personal-edit-v2.py .tmp-runner/temp-updater-v2.py
          trap 'mv .tmp-runner/temp-workflow.yml .github/workflows/temp-attendance-common-notes-personal-edit.yml 2>/dev/null || true; mv .tmp-runner/temp-updater.py scripts/temp-attendance-common-notes-personal-edit.py 2>/dev/null || true; mv .tmp-runner/temp-updater-v2.py scripts/temp-attendance-common-notes-personal-edit-v2.py 2>/dev/null || true' EXIT
          npm run ci:check
          mv .tmp-runner/temp-workflow.yml .github/workflows/temp-attendance-common-notes-personal-edit.yml
          mv .tmp-runner/temp-updater.py scripts/temp-attendance-common-notes-personal-edit.py
          mv .tmp-runner/temp-updater-v2.py scripts/temp-attendance-common-notes-personal-edit-v2.py
          rmdir .tmp-runner
          trap - EXIT
      - name: Commit source and generated files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/renderer supabase tests docs
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Implement attendance common notes and personal editing"
          git push origin HEAD:agent/attendance-common-notes-personal-edit
