name: Apply regular holiday segment color

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  apply-build-validate-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply requested changes
        run: python3 scripts/agent-regular-holiday-segment-yellow.py

      - name: Remove temporary automation files
        run: rm -f .github/workflows/agent-regular-holiday-segment-yellow.yml scripts/agent-regular-holiday-segment-yellow.py

      - name: Install dependencies
        run: npm install

      - name: Build and publish static files
        run: npm run web:publish

      - name: Validate project
        run: npm run ci:check

      - name: Publish one complete commit to main
        shell: bash
        run: |
          git config user.name "ChatGPT"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git reset --soft origin/main
          git add -A
          git commit -m "調整例假排班黃色區塊"
          git push origin HEAD:main
