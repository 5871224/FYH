name: Temporary Vietnamese Import Function Labels

on:
  push:
    branches:
      - fix/vietnamese-import-function-20260901

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@v5
        with:
          ref: fix/vietnamese-import-function-20260901
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Apply translation changes
        run: node scripts/tmp-add-vietnamese-import-function.js

      - name: Remove temporary tooling
        run: rm -f scripts/tmp-add-vietnamese-import-function.js .github/workflows/tmp-add-vietnamese-import-function.yml

      - name: Build published web files
        run: npm run web:publish

      - name: Run validation
        run: |
          npm test
          node scripts/check-normalized-storage.js
          npm run renderer:check
          npm run css:architecture
          npm run js:architecture

      - name: Commit formal changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "補齊匯入與功能按鈕越文翻譯"
          git push origin HEAD:fix/vietnamese-import-function-20260901
